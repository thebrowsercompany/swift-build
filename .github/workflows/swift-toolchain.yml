name: Swift Toolchain Build
description: |
  This workflow builds the Swift toolchain for a single platform (Windows), with
  the standard library and SDK for a set of target platforms. The
  `build_android` input can be used to
  enable building the Android standard library and SDK as part of the toolchain.
  This workflow is meant to mimic what the `utils\build.ps1` script does in the
  Swift repository.
  The main difference is that the GHA build (this workflow) splits the build
  into multiple jobs, running in parallel where possible, to reduce overall
  build time. Typically, each individual job checks out a set of source
  repositories, downloads artifacts built by previous jobs, configures and
  builds a set of components and uploads the resulting artifacts.
  The "Configure CMake" steps are the cores of each job, they use the
  `configure-cmake-project` action, which mimics the `Build-CMakeProject` cmdlet
  in `build.ps1`. Each "Configure" step is preceded by a comment indicating
  which cmdlet in `build.ps1` it corresponds to. There may be a few additional
  comments to explain the differences between the GHA build and the `build.ps1`
  script.

on:
  workflow_call:
    inputs:
      build_os:
        required: true
        type: string

      build_arch:
        required: true
        type: string

      build_matrix:
        required: true
        type: string

      host_matrix:
        required: true
        type: string

      compilers_matrix:
        required: true
        type: string

      target_matrix:
        required: true
        type: string

      curl_revision:
        required: true
        type: string

      curl_version:
        required: true
        type: string

      ds2_revision:
        required: true
        type: string

      indexstore_db_revision:
        required: true
        type: string

      libxml2_revision:
        required: true
        type: string

      libxml2_version:
        required: true
        type: string

      llvm_project_revision:
        required: true
        type: string

      mimalloc_revision:
        required: true
        type: string

      sourcekit_lsp_revision:
        required: true
        type: string

      swift_argument_parser_revision:
        required: true
        type: string

      swift_asn1_revision:
        required: true
        type: string

      swift_atomics_revision:
        required: true
        type: string

      swift_build_revision:
        required: true
        type: string

      swift_certificates_revision:
        required: true
        type: string

      swift_cmark_revision:
        required: true
        type: string

      swift_cmark_version:
        required: true
        type: string

      swift_collections_revision:
        required: true
        type: string

      swift_corelibs_foundation_revision:
        required: true
        type: string

      swift_corelibs_libdispatch_revision:
        required: true
        type: string

      swift_corelibs_xctest_revision:
        required: true
        type: string

      swift_crypto_revision:
        required: true
        type: string

      swift_docc_render_artifact_revision:
        required: true
        type: string

      swift_docc_revision:
        required: true
        type: string

      swift_docc_symbolkit_revision:
        required: true
        type: string

      swift_driver_revision:
        required: true
        type: string

      swift_experimental_string_processing_revision:
        required: true
        type: string

      swift_format_revision:
        required: true
        type: string

      swift_foundation_revision:
        required: true
        type: string

      swift_foundation_icu_revision:
        required: true
        type: string

      swift_installer_scripts_revision:
        required: true
        type: string

      swift_llbuild_revision:
        required: true
        type: string

      swift_lmdb_revision:
        required: true
        type: string

      swift_markdown_revision:
        required: true
        type: string

      swift_package_manager_revision:
        required: true
        type: string

      swift_revision:
        required: true
        type: string

      swift_subprocess_revision:
        required: true
        type: string

      swift_syntax_revision:
        required: true
        type: string

      swift_system_revision:
        required: true
        type: string

      swift_testing_revision:
        required: true
        type: string

      swift_toolchain_sqlite_revision:
        required: true
        type: string

      swift_toolchain_sqlite_version:
        required: true
        type: string

      swift_tools_protocols_revision:
        required: true
        type: string

      swift_tools_support_core_revision:
        required: true
        type: string

      zlib_revision:
        required: true
        type: string

      zlib_version:
        required: true
        type: string

      brotli_revision:
        required: true
        type: string

      brotli_version:
        required: true
        type: string

      ci_build_revision:
        description: 'CI Build branch/ref'
        required: true
        type: string

      ANDROID_API_LEVEL:
        required: true
        type: string

      ANDROID_NDK_VERSION:
        required: true
        type: string

      ANDROID_CLANG_VERSION:
        required: true
        type: string

      debug_info:
        required: true
        type: boolean

      release:
        required: true
        type: boolean

      signed:
        required: true
        type: boolean

      swift_version:
        required: true
        type: string

      swift_tag:
        required: true
        type: string

      default_build_runner:
        required: true
        type: string

      compilers_build_runner:
        required: true
        type: string

      build_android:
        required: false
        default: false
        type: boolean

      python_version:
        required: true
        type: string
      python_download_locations:
        required: true
        type: string

      use_host_toolchain:
        required: false
        type: boolean
        default: true

    secrets:
      SYMBOL_SERVER_PAT:
        required: true
      CERTIFICATE:
        required: true
      PASSPHRASE:
        required: true
      R2_ACCOUNT_ID:
        required: true
      R2_ACCESS_KEY_ID:
        required: true
      R2_SECRET_ACCESS_KEY:
        required: true
      AZURE_SP_CREDENTIALS:
        required: false
      AZURE_KEY_VAULT_URI:
        required: false
      AZURE_KEY_VAULT_CERTIFICATE_NAME:
        required: false

env:
  PINNED_BOOTSTRAP_TOOLCHAIN_VERSION: DEVELOPMENT-SNAPSHOT-2026-03-16-a
  CMAKE_VERSION: 4.4.1

defaults:
  run:
    shell: pwsh

permissions:
  id-token: write
  attestations: write

jobs:
  stage0-sqlite:
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.build_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} Stage0 SQLite3

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Compute workspace hash
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-stage0-sqlite

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-toolchain-sqlite
          ref: ${{ inputs.swift_toolchain_sqlite_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-toolchain-sqlite
          show-progress: false

      # build.ps1: Build-SQLite
      - name: Configure SQLite
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Stage0 SQLite
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-toolchain-sqlite
          bin-dir: ${{ github.workspace }}/BinaryCache/stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }}
          install-dir: ${{ github.workspace }}/BuildRoot/Library/stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr
          use-msvc-host-toolchain: ${{ inputs.use_host_toolchain }}
          c-compiler: 'Host.C'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
            }
      - name: Build SQLite
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }}
      - name: Install SQLite
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }} --target install

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr

  stage0-libxml2:
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.build_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} Stage0 libxml2

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: ${{ matrix.os == 'Windows' }}
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Compute workspace hash
        id: workspace_hash
        shell: pwsh
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-stage0-libxml2

      - uses: actions/checkout@v4.2.2
        with:
          repository: gnome/libxml2
          ref: ${{ inputs.libxml2_revision }}
          path: ${{ github.workspace }}/SourceCache/libxml2
          show-progress: false

      # build.ps1: Build-XML2
      - name: Configure libxml2
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Stage0 libxml2
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/libxml2
          bin-dir: ${{ github.workspace }}/BinaryCache/stage0-libxml2-${{ inputs.libxml2_version }}
          install-dir: ${{ github.workspace }}/BuildRoot/Library/stage0-libxml2-${{ inputs.libxml2_version }}/usr
          use-msvc-host-toolchain: ${{ inputs.use_host_toolchain }}
          c-compiler: 'Host.C'
          cxx-compiler: 'Host.CXX'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_POSITION_INDEPENDENT_CODE' = "YES";
              'LIBXML2_WITH_C14N' = "NO";
              'LIBXML2_WITH_CATALOG' = "NO";
              'LIBXML2_WITH_DEBUG' = "NO";
              'LIBXML2_WITH_FTP' = "NO";
              'LIBXML2_WITH_HTML' = "NO";
              'LIBXML2_WITH_HTTP' = "NO";
              'LIBXML2_WITH_ICONV' = "NO";
              'LIBXML2_WITH_ICU' = "NO";
              'LIBXML2_WITH_ISO8859X' = "NO";
              'LIBXML2_WITH_LEGACY' = "NO";
              'LIBXML2_WITH_LZMA' = "NO";
              'LIBXML2_WITH_MEM_DEBUG' = "NO";
              'LIBXML2_WITH_MODULES' = "NO";
              'LIBXML2_WITH_OUTPUT' = "YES";
              'LIBXML2_WITH_PATTERN' = "NO";
              'LIBXML2_WITH_PROGRAMS' = "NO";
              'LIBXML2_WITH_PUSH' = "YES";
              'LIBXML2_WITH_PYTHON' = "NO";
              'LIBXML2_WITH_READER' = "NO";
              'LIBXML2_WITH_REGEXPS' = "YES";
              'LIBXML2_WITH_SAX1' = "NO";
              'LIBXML2_WITH_SCHEMAS' = "NO";
              'LIBXML2_WITH_SCHEMATRON' = "NO";
              'LIBXML2_WITH_TESTS' = "NO";
              'LIBXML2_WITH_THREAD_ALLOC' = "NO";
              'LIBXML2_WITH_THREADS' = "YES";
              'LIBXML2_WITH_TREE' = "YES";
              'LIBXML2_WITH_VALID' = "YES";
              'LIBXML2_WITH_WRITER' = "NO";
              'LIBXML2_WITH_XINCLUDE' = "NO";
              'LIBXML2_WITH_XPATH' = "YES";
              'LIBXML2_WITH_XPTR' = "NO";
              'LIBXML2_WITH_XPTR_LOCS' = "NO";
              'LIBXML2_WITH_ZLIB' = "NO";
            }
      - name: Build libxml2
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage0-libxml2-${{ inputs.libxml2_version }}
      - name: Install libxml2
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage0-libxml2-${{ inputs.libxml2_version }} --target install

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-stage0-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/stage0-libxml2-${{ inputs.libxml2_version }}/usr

  sqlite:
    needs: [stage1-compiler]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.host_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} SQLite3

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - name: Compute workspace hash
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-sqlite

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-toolchain-sqlite
          ref: ${{ inputs.swift_toolchain_sqlite_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-toolchain-sqlite
          show-progress: false

      # build.ps1: Build-SQLite
      - name: Configure SQLite
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: SQLite
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-toolchain-sqlite
          bin-dir: ${{ github.workspace }}/BinaryCache/sqlite-${{ inputs.swift_toolchain_sqlite_version }}
          install-dir: ${{ github.workspace }}/BuildRoot/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr
          c-compiler: 'Stage1.C'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
            }
      - name: Build SQLite
        run: cmake --build ${{ github.workspace }}/BinaryCache/sqlite-${{ inputs.swift_toolchain_sqlite_version }}
      - name: Install SQLite
        run: cmake --build ${{ github.workspace }}/BinaryCache/sqlite-${{ inputs.swift_toolchain_sqlite_version }} --target install

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-sqlite-${{ inputs.swift_toolchain_sqlite_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr

  ds2_tools:
    runs-on: ${{ inputs.default_build_runner }}

    name: ds2 Build Tools

    steps:
      - uses: actions/checkout@v4.2.2
        if: inputs.build_android
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        if: inputs.build_android
        with:
          setup-vs-dev-env: true
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Compute workspace hash
        if: inputs.build_android
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        if: ${{ inputs.build_android && inputs.use_host_toolchain }}
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 1M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ inputs.build_os }}-${{ inputs.build_arch }}-ds2_tools
      - uses: actions/checkout@v4.2.2
        # There is currently no Android NDK for Windows ARM64 so build ds2_tools only on Windows X64 host only
        if: inputs.build_android
        with:
          repository: compnerd/ds2
          ref: ${{ inputs.ds2_revision }}
          path: ${{ github.workspace }}/SourceCache/ds2
          show-progress: false

      # TODO(issues/205): Preload Chocolatey package manager on Azure images so we can remove this step.
      - uses: andrurogerz/ensure-chocolatey@v1
        if: inputs.build_android

      - name: Install Flex and Bison Tools
        run: choco install winflexbison3
        if: inputs.build_android

      # build.ps1: Build-RegsGen2
      - name: Configure RegsGen2
        if: inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: RegsGen2
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ inputs.build_os }}
          arch: ${{ inputs.build_arch }}
          src-dir: ${{ github.workspace }}/SourceCache/ds2/Tools/RegsGen2
          bin-dir: ${{ github.workspace }}/BinaryCache/RegsGen2
          use-msvc-host-toolchain: ${{ inputs.use_host_toolchain }}
          c-compiler: 'Host.C'
          cxx-compiler: 'Host.CXX'
          cache-script: ${{ github.workspace }}/SourceCache/ds2/cmake/caches/MSVCWarnings.cmake
      - name: Build RegsGen2
        if: inputs.build_android
        run: cmake --build ${{ github.workspace }}/BinaryCache/RegsGen2 --config Release

      - uses: actions/upload-artifact@v4
        if: inputs.build_android
        with:
          name: Windows-${{ inputs.build_arch }}-regsgen2
          path: |
            ${{ github.workspace }}/BinaryCache/RegsGen2/regsgen2.exe
  ds2:
    needs: [ds2_tools, stage1-compiler]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            os: Android
            module_triple: aarch64-unknown-linux-android
          - arch: armv7
            os: Android
            module_triple: armv7-unknown-linux-android
          - arch: i686
            os: Android
            module_triple: i686-unknown-linux-android
          - arch: x86_64
            os: Android
            module_triple: x86_64-unknown-linux-android

    name: ${{ matrix.os }} ${{ matrix.arch }} ds2

    steps:
      - uses: actions/checkout@v4.2.2
        if: inputs.build_android
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        if: inputs.build_android
        with:
          setup-vs-dev-env: ${{ matrix.os == 'Windows' }}
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        if: inputs.build_android
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - name: Compute workspace hash
        if: inputs.build_android
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        if: ${{ inputs.build_android && inputs.use_host_toolchain }}
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 1M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ inputs.build_os }}-${{ inputs.build_arch }}-ds2
      - uses: actions/checkout@v4.2.2
        # There is currently no Android NDK for Windows ARM64 so build ds2 only on Windows X64 host only
        if: inputs.build_android
        with:
          repository: compnerd/ds2
          ref: ${{ inputs.ds2_revision }}
          path: ${{ github.workspace }}/SourceCache/ds2
          show-progress: false

      # TODO(issues/205): Preload Chocolatey package manager on Azure images so we can remove this step.
      - uses: andrurogerz/ensure-chocolatey@v1
        if: inputs.build_android

      - name: Install Flex and Bison Tools
        if: inputs.build_android
        run: choco install winflexbison3

      - uses: actions/download-artifact@v4
        if: inputs.build_android
        with:
          name: Windows-${{ inputs.build_arch }}-regsgen2
          path: ${{ github.workspace }}/BinaryCache/RegsGen2

      - uses: nttld/setup-ndk@v1
        if: inputs.build_android
        id: setup-ndk
        with:
          ndk-version: ${{ inputs.ANDROID_NDK_VERSION }}
          local-cache: true

      # build.ps1: Build-DS2
      - name: Configure DS2
        if: inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: DS2
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/ds2
          bin-dir: ${{ github.workspace }}/BinaryCache/ds2
          install-dir: ${{ github.workspace }}/BinaryCache/Library/Developer/ds2/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          cxx-compiler: 'Stage1.GNUCXX'
          cmake-defines: |
            @{
              'DS2_REGSGEN2' = "${{ github.workspace }}/BinaryCache/RegsGen2/regsgen2.exe";
              'DS2_PROGRAM_PREFIX' = "${{ matrix.module_triple }}-";
            }
      - name: Build DS2
        if: inputs.build_android
        run: cmake --build ${{ github.workspace }}/BinaryCache/ds2
      - name: Install DS2
        if: inputs.build_android
        run: cmake --build ${{ github.workspace }}/BinaryCache/ds2 --target install

      - uses: actions/upload-artifact@v4
        if: inputs.build_android
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-ds2
          path: ${{ github.workspace }}/BinaryCache/Library/Developer

  cmark_gfm:
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.host_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} CMark GFM

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Compute workspace hash
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 1M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-cmark-gfm
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-cmark
          ref: ${{ inputs.swift_cmark_revision }}
          path: ${{ github.workspace }}/SourceCache/cmark-gfm
          show-progress: false

      # build.ps1: Build-CMark
      - name: Configure cmark-gfm
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: cmark-gfm
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/cmark-gfm
          bin-dir: ${{ github.workspace }}/BinaryCache/cmark-gfm-${{ inputs.swift_cmark_version }}
          install-dir: ${{ github.workspace }}/BuildRoot/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr
          use-msvc-host-toolchain: ${{ inputs.use_host_toolchain }}
          c-compiler: 'Host.C'
          cxx-compiler: 'Host.CXX'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'BUILD_TESTING' = "NO";
              'CMAKE_INSTALL_SYSTEM_RUNTIME_LIBS_SKIP' = "YES";
            }
      - name: Build cmark-gfm
        run: cmake --build ${{ github.workspace }}/BinaryCache/cmark-gfm-${{ inputs.swift_cmark_version }}
      - name: Install cmark-gfm
        run: cmake --build ${{ github.workspace }}/BinaryCache/cmark-gfm-${{ inputs.swift_cmark_version }} --target install
      - uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-cmark-gfm-${{ inputs.swift_cmark_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr
  build_tools:
    needs: [cmark_gfm]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.build_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} Compiler Build Tools

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Compute workspace hash
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-build_tools
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-cmark-gfm-${{ inputs.swift_cmark_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/llvm-project
          ref: ${{ inputs.llvm_project_revision }}
          path: ${{ github.workspace }}/SourceCache/llvm-project
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift
          ref: ${{ inputs.swift_revision }}
          path: ${{ github.workspace }}/SourceCache/swift
          show-progress: false

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python_version }}
          architecture: x64 # FIXME(#1004): Remove workaround installing x64 Python on ARM64 CI hosts

      # build.ps1: Build-BuildTools
      - name: Configure Tools
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Build Tools
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/llvm-project/llvm
          bin-dir: ${{ github.workspace }}/BinaryCache/BuildTools
          use-msvc-host-toolchain: ${{ inputs.use_host_toolchain }}
          c-compiler: 'Host.C'
          cxx-compiler: 'Host.CXX'
          use-asm-masm: ${{ inputs.use_host_toolchain }}
          cmake-defines: |
            @{
              'CMAKE_CROSSCOMPILING' = "NO";
              'CLANG_ENABLE_LIBXML2' = "NO";
              'LLDB_ENABLE_LIBXML2' = "NO";
              'LLDB_ENABLE_PYTHON' = "NO";
              'LLDB_INCLUDE_TESTS' = "NO";
              'LLDB_ENABLE_SWIFT_SUPPORT' = "NO";
              'LLVM_ENABLE_ASSERTIONS' = "NO";
              'LLVM_ENABLE_LIBEDIT' = "NO";
              'LLVM_ENABLE_LIBXML2' = "NO";
              'LLVM_ENABLE_PROJECTS' = "clang;clang-tools-extra;lldb";
              'LLVM_EXTERNAL_PROJECTS' = "swift";
              'LLVM_EXTERNAL_SWIFT_SOURCE_DIR' = "${{ github.workspace }}/SourceCache/swift";
              'SWIFT_BUILD_DYNAMIC_SDK_OVERLAY' = "NO";
              'SWIFT_BUILD_DYNAMIC_STDLIB' = "NO";
              'SWIFT_BUILD_HOST_DISPATCH' = "NO";
              'SWIFT_BUILD_LIBEXEC' = "NO";
              'SWIFT_BUILD_REGEX_PARSER_IN_COMPILER' = "NO";
              'SWIFT_BUILD_REMOTE_MIRROR' = "NO";
              'SWIFT_BUILD_SOURCEKIT' = "NO";
              'SWIFT_BUILD_STATIC_SDK_OVERLAY' = "NO";
              'SWIFT_BUILD_STATIC_STDLIB' = "NO";
              'SWIFT_BUILD_SWIFT_SYNTAX' = "NO";
              'SWIFT_ENABLE_DISPATCH' = "NO";
              'SWIFT_INCLUDE_APINOTES' = "NO";
              'SWIFT_INCLUDE_DOCS' = "NO";
              'SWIFT_INCLUDE_TESTS' = "NO";
              'cmark-gfm_DIR' = "${{ github.workspace }}/BuildRoot/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/lib/cmake";
            }
      - name: Build llvm-tblgen
        run: cmake --build ${{ github.workspace }}/BinaryCache/BuildTools --target llvm-tblgen
      - name: Build clang-tblgen
        run: cmake --build ${{ github.workspace }}/BinaryCache/BuildTools --target clang-tblgen
      - name: Build clang-tidy-confusable-chars-gen
        run: cmake --build ${{ github.workspace }}/BinaryCache/BuildTools --target clang-tidy-confusable-chars-gen
      - name: Build lldb-tblgen
        run: cmake --build ${{ github.workspace }}/BinaryCache/BuildTools --target lldb-tblgen
      - name: Build llvm-config
        run: cmake --build ${{ github.workspace }}/BinaryCache/BuildTools --target llvm-config
      - name: Build swift-def-to-strings-converter
        run: cmake --build ${{ github.workspace }}/BinaryCache/BuildTools --target swift-def-to-strings-converter
      - name: Build swift-serialize-diagnostics
        run: cmake --build ${{ github.workspace }}/BinaryCache/BuildTools --target swift-serialize-diagnostics
      - name: Build swift-compatibility-symbols
        run: cmake --build ${{ github.workspace }}/BinaryCache/BuildTools --target swift-compatibility-symbols

      - name: Copy binaries
        run: |
          $Suffix = if ( "${{ matrix.os }}" -eq "Windows" ) { ".exe" } else { "" }
          $Binaries = @(
            "llvm-tblgen",
            "clang-tblgen",
            "clang-tidy-confusable-chars-gen",
            "lldb-tblgen",
            "llvm-config",
            "swift-def-to-strings-converter",
            "swift-serialize-diagnostics",
            "swift-compatibility-symbols"
          )

          # Create the target folder.
          New-Item -ItemType Directory -Path "${{ github.workspace }}/BuildRoot/bin" -Force | Out-Null
          foreach ($Binary in $Binaries) {
            Copy-Item -Path "${{ github.workspace }}/BinaryCache/BuildTools/bin/${Binary}${Suffix}" -Destination "${{ github.workspace }}/BuildRoot/bin/${Binary}${Suffix}" -Force
          }

      - uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-build-tools
          path: ${{ github.workspace }}/BuildRoot/bin

  early_swift_driver:
    runs-on: ${{ inputs.default_build_runner }}
    needs: [stage0-sqlite]

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.build_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} early swift-driver

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        id: setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Compute workspace hash
        id: workspace_hash
        shell: pwsh
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 500M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-${{ matrix.variant }}-early-swift-driver
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-llbuild
          ref: ${{ inputs.swift_llbuild_revision }}
          path: ${{ github.workspace }}/SourceCache/llbuild
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-toolchain-sqlite
          ref: ${{ inputs.swift_toolchain_sqlite_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-toolchain-sqlite
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-tools-support-core
          ref: ${{ inputs.swift_tools_support_core_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-tools-support-core
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-argument-parser
          ref: ${{ inputs.swift_argument_parser_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-argument-parser
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-driver
          ref: ${{ inputs.swift_driver_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-driver
          show-progress: false

      - uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr

      - name: Setup SDK context
        id: setup-context
        run: |
          if ("${{ matrix.os }}" -eq "Windows") {
              $sdkRoot = Split-Path -Parent ${env:SDKROOT}
              $sdkRoot = Join-Path $sdkRoot "WindowsExperimental.sdk"
              $sdkRoot = cygpath -m ${sdkRoot}
          }
          Write-Output "sdkroot=${sdkRoot}" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      # build.ps1: Build-EarlySwiftDriver
      - name: Configure early swift-driver
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Compilers
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-driver
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-driver
          swift-sdk-path: ${{ steps.setup-context.outputs.sdkroot }}
          c-compiler: 'Pinned.C'
          cxx-compiler: 'Pinned.CXX'
          swift-compiler: 'Pinned.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'BUILD_TESTING' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              # TODO(compnerd) - remove `-Xfrontend -use-static-resource-dir` - this is inferred by the `-static-stdlib`.
              'CMAKE_Swift_FLAGS' = @("-static-stdlib", "-Xfrontend", "-use-static-resource-dir");
              'SWIFT_DRIVER_BUILD_TOOLS' = "NO";
              'SQLite3_INCLUDE_DIR' = "${{ github.workspace }}/BinaryCache/Library/stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/include";
              'SQLite3_LIBRARY' = "${{ github.workspace }}/BinaryCache/Library/stage0-sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/lib/SQLite3.lib";
              'FETCHCONTENT_SOURCE_DIR_ARGUMENTPARSER' = "${{ github.workspace }}/SourceCache/swift-argument-parser";
              'FETCHCONTENT_SOURCE_DIR_LLBUILD' = "${{ github.workspace }}/SourceCache/llbuild";
              'FETCHCONTENT_SOURCE_DIR_TOOLSSUPPORTCORE' = "${{ github.workspace }}/SourceCache/swift-tools-support-core";
            }

      - name: Build early swift-driver
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/swift-driver

      - name: Copy binaries
        run: |
          $ExeSuffix = if ("${{ matrix.os }}" -eq "Windows") { ".exe" } else { "" }
          $SourceBinDir = Join-Path "${{ github.workspace }}" "BinaryCache" "swift-driver" "bin"
          $InstallBinDir = Join-Path "${{ github.workspace }}" "BuildRoot" "bin"

          # Create the target folder.
          New-Item -ItemType Directory -Path $InstallBinDir -Force

          # Copy binaries.
          foreach ($bin in @("swift-driver", "swift-help")) {
              $binName = "${bin}${ExeSuffix}"
              $binPath = Join-Path $InstallBinDir $binName
              Copy-Item -Path "${SourceBinDir}/${binName}" -Destination $binPath -Force
          }

      - uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-early-swift-driver
          path: ${{ github.workspace }}/BuildRoot

  stage0-compiler:
    needs: [stage0-libxml2, cmark_gfm, build_tools, early_swift_driver]
    runs-on: ${{ inputs.compilers_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.build_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} Stage0 Compiler

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: ${{ matrix.os == 'Windows' }}
          host-arch: ${{ inputs.build_arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Compute workspace hash
        id: workspace_hash
        shell: pwsh
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 500M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-stage0-compiler
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-build-tools
          path: ${{ github.workspace }}/BinaryCache/BuildTools/bin
      - uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-stage0-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/stage0-libxml2-${{ inputs.libxml2_version }}/usr
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-cmark-gfm-${{ inputs.swift_cmark_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-early-swift-driver
          path: ${{ github.workspace }}/BinaryCache/swift-driver

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/llvm-project
          ref: ${{ inputs.llvm_project_revision }}
          path: ${{ github.workspace }}/SourceCache/llvm-project
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift
          ref: ${{ inputs.swift_revision }}
          path: ${{ github.workspace }}/SourceCache/swift
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-experimental-string-processing
          ref: ${{ inputs.swift_experimental_string_processing_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-experimental-string-processing
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-syntax
          ref: ${{ inputs.swift_syntax_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-syntax
          show-progress: false

      - uses: actions/setup-python@v5
        id: python
        with:
          python-version: ${{ inputs.python_version }}
          architecture: x64 # FIXME(#1004): Remove workaround installing x64 Python on ARM64 CI hosts

      - name: Setup context
        id: setup-context
        run: |
          $CLANG_LOCATION = cygpath -m $(Split-Path (Get-Command swiftc).Source)
          $SDKROOT = cygpath -m ${env:SDKROOT}

          # Resolve the runtime that belongs to the pinned Swift compiler.
          $SwiftcPath = (Get-Command swiftc -ErrorAction Stop).Source
          $ToolchainRoot = Split-Path -Parent (Split-Path -Parent (Split-Path -Parent $SwiftcPath))
          $ToolchainVersion = Split-Path -Leaf $ToolchainRoot
          $SwiftRoot = Split-Path -Parent (Split-Path -Parent $ToolchainRoot)
          $RuntimeVersion = $ToolchainVersion.Split('+')[0]
          $RuntimesRoot = Join-Path $SwiftRoot "Runtimes"
          $PINNED_RUNTIME_BIN = Join-Path $RuntimesRoot "$RuntimeVersion\usr\bin"
          $RuntimeDirs = @(Get-ChildItem -Path $RuntimesRoot -Directory -ErrorAction SilentlyContinue)
          if (-not (Test-Path $PINNED_RUNTIME_BIN)) {
            if ($RuntimeDirs.Count -eq 1) {
              $PINNED_RUNTIME_BIN = Join-Path $RuntimeDirs[0].FullName "usr\bin"
            } else {
              throw "Pinned runtime not found at '$PINNED_RUNTIME_BIN' (derived from '$SwiftcPath'); $($RuntimeDirs.Count) runtime dir(s) present"
            }
          }
          Write-Host "Pinned runtime: $PINNED_RUNTIME_BIN"

          $Cpu = switch ("${{ inputs.build_arch }}") {
            "amd64" { "x86_64" }
            "arm64" { "aarch64" }
            default { throw "Unsupported build_arch: ${{ inputs.build_arch }}" }
          }
          $Triple = "$Cpu-unknown-windows-msvc"

          $RamBytes = (Get-CimInstance -ClassName Win32_ComputerSystem).TotalPhysicalMemory
          $RamGB = [math]::Round($RamBytes / 1GB)
          $LinkJobs = [math]::Max(2, [math]::Floor($RamGB / 24))

          $Context = @"
          clang-location=${CLANG_LOCATION}
          sdkroot=${SDKROOT}
          pinned-runtime-bin=${PINNED_RUNTIME_BIN}
          cpu=${Cpu}
          triple=${Triple}
          link-jobs=${LinkJobs}
          "@
          Write-Output $Context | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Configure Stage 0 Compiler
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Stage0 Compiler
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/llvm-project/llvm
          bin-dir: ${{ github.workspace }}/BinaryCache/stage0/build
          install-dir: ${{ github.workspace }}/BinaryCache/stage0/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ steps.setup-context.outputs.sdkroot }}
          use-msvc-host-toolchain: ${{ inputs.use_host_toolchain }}
          c-compiler: 'Host.C'
          cxx-compiler: 'Host.CXX'
          swift-compiler: 'Pinned.Swift'
          cache-script: ${{ github.workspace }}/SourceCache/swift/cmake/caches/Windows-Bootstrap-Stage0-${{ steps.setup-context.outputs.cpu }}.cmake
          cmake-defines: |
            @{
              'SWIFT_BUILD_DYNAMIC_SDK_OVERLAY' = "NO";
              'SWIFT_BUILD_DYNAMIC_STDLIB' = "NO";
              'SWIFT_BUILD_LIBEXEC' = "NO";
              'SWIFT_BUILD_REMOTE_MIRROR' = "NO";
              'SWIFT_NATIVE_SWIFT_TOOLS_PATH' = "${{ github.workspace }}/BinaryCache/BuildTools/bin";

              'CLANG_TABLEGEN' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/clang-tblgen${ExeSuffix}";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'LibXml2_DIR' = "${{ github.workspace }}/BinaryCache/Library/stage0-libxml2-${{ inputs.libxml2_version }}/usr/lib/cmake/libxml2-${{ inputs.libxml2_version }}";
              'LLVM_CONFIG_PATH' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/llvm-config${ExeSuffix}";
              'LLVM_ENABLE_ASSERTIONS' = "YES";
              'LLVM_ENABLE_LLD' = "NO";
              'LLVM_EXTERNAL_SWIFT_SOURCE_DIR' = "${{ github.workspace }}/SourceCache/swift";
              'LLVM_HOST_TRIPLE' = "${{ steps.setup-context.outputs.triple }}";
              'LLVM_NATIVE_TOOL_DIR' = "${{ github.workspace }}/BinaryCache/BuildTools/bin";
              'LLVM_TABLEGEN' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/llvm-tblgen${ExeSuffix}";
              'LLVM_USE_HOST_TOOLS' = "NO";
              'Python3_EXECUTABLE' = "${{ steps.python.outputs.python-path }}";
              'SWIFT_TOOLCHAIN_VERSION' = "${{ inputs.swift_version }} ${{ inputs.swift_tag }}";
              'SWIFT_BUILD_SWIFT_SYNTAX' = "YES";
              'SWIFT_CLANG_LOCATION' = "${{ steps.setup-context.outputs.clang-location }}";
              'SWIFT_EARLY_SWIFT_DRIVER_BUILD' = "${{ github.workspace }}/BinaryCache/swift-driver/bin";
              'SWIFT_ENABLE_EXPERIMENTAL_CONCURRENCY' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_CXX_INTEROP' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_DIFFERENTIABLE_PROGRAMMING' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_DISTRIBUTED' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_OBSERVATION' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_STRING_PROCESSING' = "YES";
              'SWIFT_ENABLE_RUNTIME_MODULE' = "YES";
              'SWIFT_ENABLE_BACKTRACING' = "YES";
              'SWIFT_ENABLE_SYNCHRONIZATION' = "YES";
              'SWIFT_ENABLE_VOLATILE' = "YES";
              'SWIFT_PATH_TO_STRING_PROCESSING_SOURCE' = "${{ github.workspace }}/SourceCache/swift-experimental-string-processing";
              'SWIFT_PATH_TO_SWIFT_SDK' = "${{ steps.setup-context.outputs.sdkroot }}";
              'SWIFT_PATH_TO_SWIFT_SYNTAX_SOURCE' = "${{ github.workspace }}/SourceCache/swift-syntax";
              'SWIFT_STDLIB_ASSERTIONS' = "NO";
              'SWIFTSYNTAX_ENABLE_ASSERTIONS' = "NO";
              'cmark-gfm_DIR' = "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/lib/cmake";

              'LLVM_PARALLEL_LINK_JOBS' = "${{ steps.setup-context.outputs.link-jobs }}";
              'SWIFT_PARALLEL_LINK_JOBS' = "${{ steps.setup-context.outputs.link-jobs }}";
            }

      - name: Build Stage 0 Compiler Distribution
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage0/build --target distribution

      - name: Install Stage 0 Compiler Distribution
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage0/build --target install-distribution

      - name: Copy cmark-gfm shared libraries
        run: |
          Copy-Item -Path "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/bin/*.dll" -Destination "${{ github.workspace }}/BinaryCache/stage0/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/bin"

      # build.ps1: Copy-WindowsRuntimeToToolchain
      - name: Copy Windows Runtime to Toolchain
        run: |
          $Stage0Bin = "${{ github.workspace }}/BinaryCache/stage0/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/bin"
          New-Item -ItemType Directory -Path $Stage0Bin -Force | Out-Null
          $RuntimeSource = "${{ steps.setup-context.outputs.pinned-runtime-bin }}"
          if (-not (Test-Path $RuntimeSource)) {
            throw "Pinned runtime not found at '$RuntimeSource'."
          }
          $RuntimeFiles = @(Get-ChildItem -Path $RuntimeSource -File | Sort-Object Name)
          if (-not ($RuntimeFiles | Where-Object { $_.Extension -ieq ".dll" })) {
            throw "No DLLs found under '$RuntimeSource'"
          }
          foreach ($File in $RuntimeFiles) {
            Copy-Item -Force -Path $File.FullName -Destination $Stage0Bin
          }
          Write-Host "Copied $($RuntimeFiles.Count) runtime file(s) from '$RuntimeSource' to '$Stage0Bin'."

      - name: Upload Stage 0 Compilers
        uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage0-compiler
          path: ${{ github.workspace }}/BinaryCache/stage0/Library

  bootstrap-sdk:
    needs: [stage0-compiler]
    runs-on: ${{ inputs.default_build_runner }}

    name: Bootstrap SDK

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ inputs.build_arch }}

      - name: Clear inherited SDKROOT
        run: |
          "SDKROOT=" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python_version }}
          architecture: x64 # FIXME(#1004): Remove workaround installing x64 Python on ARM64 CI hosts

      - name: Compute workspace hash
        id: workspace_hash
        shell: pwsh
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 500M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ inputs.build_os }}-${{ inputs.build_arch }}-bootstrap-sdk

      - name: Download Stage 0 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage0-compiler
          path: ${{ github.workspace }}/BinaryCache/stage0/Library

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift
          ref: ${{ inputs.swift_revision }}
          path: ${{ github.workspace }}/SourceCache/swift
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-corelibs-libdispatch
          ref: ${{ inputs.swift_corelibs_libdispatch_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-experimental-string-processing
          ref: ${{ inputs.swift_experimental_string_processing_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-experimental-string-processing
          show-progress: false

      - name: Run Resync
        run: |
          cd ${{ github.workspace }}/SourceCache/swift/Runtimes
          cmake -P Resync.cmake

      # build.ps1: Build-CDispatch
      - name: Configure Bootstrap CDispatch
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Bootstrap CDispatch
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ inputs.build_os }}
          arch: ${{ inputs.build_arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch
          bin-dir: ${{ github.workspace }}/BinaryCache/bootstrap/CDispatch
          c-compiler: 'Stage0.GNUC'
          cxx-compiler: 'Stage0.GNUCXX'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'BUILD_TESTING' = "NO";
              'ENABLE_SWIFT' = "NO";
            }
      - name: Build Bootstrap CDispatch
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/CDispatch

      # build.ps1: Build-SDK
      - name: Configure Bootstrap Runtime
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Bootstrap Runtime
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ inputs.build_os }}
          arch: ${{ inputs.build_arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Core
          bin-dir: ${{ github.workspace }}/BinaryCache/bootstrap/Runtime
          install-dir: ${{ github.workspace }}/BuildRoot/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk/usr
          c-compiler: 'Stage0.GNUC'
          cxx-compiler: 'Stage0.GNUCXX'
          swift-compiler: 'Stage0.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'dispatch_DIR' = "${{ github.workspace }}/BinaryCache/bootstrap/CDispatch/cmake/modules";
              'SwiftCore_ENABLE_BACKTRACING' = "YES";
              'SwiftCore_ENABLE_CONCURRENCY' = "YES";
              'SwiftCore_ENABLE_REMOTE_MIRROR' = "YES";
              'SwiftCore_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build Bootstrap Runtime
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/Runtime
      - name: Install Bootstrap Runtime
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/Runtime --target install

      # build.ps1: Build-SDK
      - name: Configure Bootstrap Overlay
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Bootstrap Overlay
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ inputs.build_os }}
          arch: ${{ inputs.build_arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Overlay
          bin-dir: ${{ github.workspace }}/BinaryCache/bootstrap/Overlay
          install-dir: ${{ github.workspace }}/BuildRoot/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk/usr
          c-compiler: 'Stage0.GNUC'
          cxx-compiler: 'Stage0.GNUCXX'
          swift-compiler: 'Stage0.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/bootstrap/Runtime/cmake/SwiftCore";
              'SwiftOverlay_ENABLE_CXX_INTEROP' = "YES";
              'SwiftOverlay_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build Bootstrap Overlay
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/Overlay
      - name: Install Bootstrap Overlay
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/Overlay --target install

      # build.ps1: Build-SDK
      - name: Configure Bootstrap String Processing
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Bootstrap String Processing
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ inputs.build_os }}
          arch: ${{ inputs.build_arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Supplemental/StringProcessing
          bin-dir: ${{ github.workspace }}/BinaryCache/bootstrap/StringProcessing
          install-dir: ${{ github.workspace }}/BuildRoot/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk/usr
          c-compiler: 'Stage0.GNUC'
          cxx-compiler: 'Stage0.GNUCXX'
          swift-compiler: 'Stage0.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/bootstrap/Runtime/cmake/SwiftCore";
              'SwiftStringProcessing_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build Bootstrap String Processing
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/StringProcessing
      - name: Install Bootstrap String Processing
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/StringProcessing --target install

      # build.ps1: Build-SDK
      - name: Configure Bootstrap Dispatch
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Bootstrap Dispatch
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ inputs.build_os }}
          arch: ${{ inputs.build_arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch
          bin-dir: ${{ github.workspace }}/BinaryCache/bootstrap/Dispatch
          install-dir: ${{ github.workspace }}/BuildRoot/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk/usr
          c-compiler: 'Stage0.GNUC'
          cxx-compiler: 'Stage0.GNUCXX'
          swift-compiler: 'Stage0.Swift'
          swift-sdk-path: ${{ github.workspace }}/BuildRoot/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'BUILD_TESTING' = "NO";
              'CMAKE_FIND_PACKAGE_PREFER_CONFIG' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'ENABLE_SWIFT' = "YES";
              'dispatch_INSTALL_ARCH_SUBDIR' = "YES";
            }
      - name: Build Bootstrap Dispatch
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/Dispatch
      - name: Install Bootstrap Dispatch
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/Dispatch --target install

      - name: Repair SDK Headers
        shell: pwsh
        run: |
          $SDKRoot = "${{ github.workspace }}/BuildRoot/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk"
          foreach ($Module in ("Block", "dispatch", "os")) {
            foreach ($ResourceType in ("swift", "swift_static")) {
              $ModuleDirectory = "$SDKRoot/usr/lib/$ResourceType/$Module"
              if (Test-Path $ModuleDirectory) {
                New-Item -ItemType Directory -Force "$SDKRoot/usr/include" | Out-Null
                Move-Item -Path $ModuleDirectory -Destination "$SDKRoot/usr/include" -Force
              }
            }
          }

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-bootstrap-sdk
          path: ${{ github.workspace }}/BuildRoot/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform

  stage1-compiler:
    needs: [bootstrap-sdk]
    runs-on: ${{ inputs.compilers_build_runner }}

    name: Stage1 Compiler

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ inputs.build_arch }}

      - name: Clear inherited SDKROOT
        run: |
          "SDKROOT=" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage 0 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage0-compiler
          path: ${{ github.workspace }}/BinaryCache/stage0/Library
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-build-tools
          path: ${{ github.workspace }}/BinaryCache/BuildTools/bin
      - uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage0-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/stage0-libxml2-${{ inputs.libxml2_version }}/usr
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-cmark-gfm-${{ inputs.swift_cmark_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-early-swift-driver
          path: ${{ github.workspace }}/BinaryCache/swift-driver
      - uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-bootstrap-sdk
          path: ${{ github.workspace }}/BinaryCache/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/llvm-project
          ref: ${{ inputs.llvm_project_revision }}
          path: ${{ github.workspace }}/SourceCache/llvm-project
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift
          ref: ${{ inputs.swift_revision }}
          path: ${{ github.workspace }}/SourceCache/swift
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-experimental-string-processing
          ref: ${{ inputs.swift_experimental_string_processing_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-experimental-string-processing
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-syntax
          ref: ${{ inputs.swift_syntax_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-syntax
          show-progress: false

      - uses: actions/setup-python@v5
        id: python
        with:
          python-version: ${{ inputs.python_version }}
          architecture: x64 # FIXME(#1004): Remove workaround installing x64 Python on ARM64 CI hosts

      - name: Setup context
        id: setup-context
        run: |
          $Cpu = switch ("${{ inputs.build_arch }}") {
            "amd64" { "x86_64" }
            "arm64" { "aarch64" }
            default { throw "Unsupported build_arch: ${{ inputs.build_arch }}" }
          }
          $Triple = "$Cpu-unknown-windows-msvc"

          $RamBytes = (Get-CimInstance -ClassName Win32_ComputerSystem).TotalPhysicalMemory
          $RamGB = [math]::Round($RamBytes / 1GB)
          $LinkJobs = [math]::Max(2, [math]::Floor($RamGB / 24))

          $Context = @"
          cpu=${Cpu}
          triple=${Triple}
          link-jobs=${LinkJobs}
          "@
          Write-Output $Context | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Configure Stage1 Compiler
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Stage1 Compiler
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ inputs.build_os }}
          arch: ${{ inputs.build_arch }}
          src-dir: ${{ github.workspace }}/SourceCache/llvm-project/llvm
          bin-dir: ${{ github.workspace }}/BinaryCache/stage1/build
          install-dir: ${{ github.workspace }}/BinaryCache/stage1/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk
          c-compiler: 'Stage0.C'
          cxx-compiler: 'Stage0.CXX'
          swift-compiler: 'Stage0.Swift'
          cache-script: ${{ github.workspace }}/SourceCache/swift/cmake/caches/Windows-Bootstrap-Stage1-${{ steps.setup-context.outputs.cpu }}.cmake
          cmake-defines: |
            @{
              'SWIFT_BUILD_DYNAMIC_SDK_OVERLAY' = "NO";
              'SWIFT_BUILD_DYNAMIC_STDLIB' = "NO";
              'SWIFT_BUILD_LIBEXEC' = "NO";
              'SWIFT_BUILD_REMOTE_MIRROR' = "NO";
              'SWIFT_NATIVE_SWIFT_TOOLS_PATH' = "${{ github.workspace }}/BinaryCache/BuildTools/bin";

              'CLANG_TABLEGEN' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/clang-tblgen${ExeSuffix}";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'LibXml2_DIR' = "${{ github.workspace }}/BinaryCache/Library/stage0-libxml2-${{ inputs.libxml2_version }}/usr/lib/cmake/libxml2-${{ inputs.libxml2_version }}";
              'LLVM_CONFIG_PATH' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/llvm-config${ExeSuffix}";
              'LLVM_ENABLE_ASSERTIONS' = "YES";
              'LLVM_ENABLE_LLD' = "NO";
              'LLVM_EXTERNAL_SWIFT_SOURCE_DIR' = "${{ github.workspace }}/SourceCache/swift";
              'LLVM_HOST_TRIPLE' = "${{ steps.setup-context.outputs.triple }}";
              'LLVM_NATIVE_TOOL_DIR' = "${{ github.workspace }}/BinaryCache/BuildTools/bin";
              'LLVM_TABLEGEN' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/llvm-tblgen${ExeSuffix}";
              'LLVM_USE_HOST_TOOLS' = "NO";
              'Python3_EXECUTABLE' = "${{ steps.python.outputs.python-path }}";
              'SWIFT_TOOLCHAIN_VERSION' = "${{ inputs.swift_version }} ${{ inputs.swift_tag }}";
              'SWIFT_BUILD_SWIFT_SYNTAX' = "YES";
              'SWIFT_EARLY_SWIFT_DRIVER_BUILD' = "${{ github.workspace }}/BinaryCache/swift-driver/bin";
              'SWIFT_ENABLE_EXPERIMENTAL_CONCURRENCY' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_CXX_INTEROP' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_DIFFERENTIABLE_PROGRAMMING' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_DISTRIBUTED' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_OBSERVATION' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_STRING_PROCESSING' = "YES";
              'SWIFT_ENABLE_RUNTIME_MODULE' = "YES";
              'SWIFT_ENABLE_BACKTRACING' = "YES";
              'SWIFT_ENABLE_SYNCHRONIZATION' = "YES";
              'SWIFT_ENABLE_VOLATILE' = "YES";
              'SWIFT_PATH_TO_STRING_PROCESSING_SOURCE' = "${{ github.workspace }}/SourceCache/swift-experimental-string-processing";
              'SWIFT_PATH_TO_SWIFT_SDK' = "${{ github.workspace }}/BinaryCache/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk";
              'SWIFT_PATH_TO_SWIFT_SYNTAX_SOURCE' = "${{ github.workspace }}/SourceCache/swift-syntax";
              'SWIFT_STDLIB_ASSERTIONS' = "NO";
              'SWIFTSYNTAX_ENABLE_ASSERTIONS' = "NO";
              'cmark-gfm_DIR' = "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/lib/cmake";

              'LLVM_PARALLEL_LINK_JOBS' = "${{ steps.setup-context.outputs.link-jobs }}";
              'SWIFT_PARALLEL_LINK_JOBS' = "${{ steps.setup-context.outputs.link-jobs }}";
            }

      - name: Build Stage1 Compiler Distribution
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage1/build --target distribution

      - name: Install Stage1 Compiler Distribution
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage1/build --target install-distribution

      - name: Copy cmark-gfm shared libraries
        run: |
          Copy-Item -Path "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/bin/*.dll" -Destination "${{ github.workspace }}/BinaryCache/stage1/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/bin"

      - name: Copy Windows Runtime to Toolchain
        run: |
          $Stage1Bin = "${{ github.workspace }}/BinaryCache/stage1/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/bin"
          New-Item -ItemType Directory -Path $Stage1Bin -Force | Out-Null
          $RuntimeSource = "${{ github.workspace }}/BinaryCache/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk/usr/bin"
          if (-not (Test-Path $RuntimeSource)) {
            throw "Bootstrap runtime not found at '$RuntimeSource'."
          }
          $RuntimeFiles = @(Get-ChildItem -Path $RuntimeSource -File | Sort-Object Name)
          if (-not ($RuntimeFiles | Where-Object { $_.Extension -ieq ".dll" })) {
            throw "No DLLs found under '$RuntimeSource'"
          }
          foreach ($File in $RuntimeFiles) {
            Copy-Item -Force -Path $File.FullName -Destination $Stage1Bin
          }
          Write-Host "Copied $($RuntimeFiles.Count) runtime file(s) from '$RuntimeSource' to '$Stage1Bin'."

      - name: Upload Stage1 Compiler
        uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - name: Extract Stage1 SwiftSyntax
        run: |
          $SwiftSyntaxRoot = "${{ github.workspace }}/BinaryCache/stage1/swift-syntax"
          $SwiftSyntaxHostLib = "$SwiftSyntaxRoot/lib/swift/host"
          New-Item -Path $SwiftSyntaxHostLib -ItemType Directory -Force | Out-Null

          $ToolchainRoot = "${{ github.workspace }}/BinaryCache/stage1/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts"
          Copy-Item -Path "$ToolchainRoot/usr/lib/*.lib" -Destination "$SwiftSyntaxRoot/lib"
          Copy-Item -Path "$ToolchainRoot/usr/lib/*.lib" -Destination $SwiftSyntaxHostLib
          Copy-Item -Path "$ToolchainRoot/usr/lib/swift/host/*.swiftmodule" -Destination $SwiftSyntaxHostLib -Recurse

          $BinaryDir = cygpath -m "${{ github.workspace }}/BinaryCache/stage1/build"
          $Module = "${{ github.workspace }}/BinaryCache/stage1/build/cmake/modules/SwiftSyntaxConfig.cmake"
          (Get-Content $Module).Replace($BinaryDir, '<BINARY_DIR>') | Set-Content $Module
          $ModulesDir = "$SwiftSyntaxRoot/cmake/modules"
          New-Item -Path $ModulesDir -ItemType Directory -Force | Out-Null
          Copy-Item -Path $Module -Destination $ModulesDir

      - name: Upload Stage1 SwiftSyntax
        uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-swift-syntax
          path: ${{ github.workspace }}/BinaryCache/stage1/swift-syntax

  bootstrap-macros:
    needs: [stage1-compiler]
    runs-on: ${{ inputs.default_build_runner }}

    name: Bootstrap Macros

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ inputs.build_arch }}

      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage 0 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage0-compiler
          path: ${{ github.workspace }}/BinaryCache/stage0/Library
      - name: Download Stage1 SwiftSyntax
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-swift-syntax
          path: ${{ github.workspace }}/BinaryCache/stage1/swift-syntax
      - name: Download Bootstrap SDK
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-bootstrap-sdk
          path: ${{ github.workspace }}/BinaryCache/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform

      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-foundation
          ref: ${{ inputs.swift_foundation_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-foundation
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-testing
          ref: ${{ inputs.swift_testing_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-testing
          show-progress: false

      - name: Add Bootstrap runtime to PATH
        run: |
          $RuntimePath = cygpath -w ${{ github.workspace }}/BinaryCache/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk/usr/bin
          $RuntimePath | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Relocate SwiftSyntax
        run: |
          $Module = "${{ github.workspace }}/BinaryCache/stage1/swift-syntax/cmake/modules/SwiftSyntaxConfig.cmake"
          $BinaryDir = cygpath -m ${{ github.workspace }}/BinaryCache/stage1/swift-syntax
          (Get-Content $Module).Replace('<BINARY_DIR>', $BinaryDir) | Set-Content $Module

      - name: Configure Bootstrap Foundation Macros
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Bootstrap Foundation Macros
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ inputs.build_os }}
          arch: ${{ inputs.build_arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-foundation/Sources/FoundationMacros
          bin-dir: ${{ github.workspace }}/BinaryCache/bootstrap/swift-foundation-macros
          install-dir: ${{ github.workspace }}/BuildRoot/bootstrap/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk
          swift-compiler: 'Stage0.Swift'
          cmake-defines: |
            @{
              'SwiftSyntax_DIR' = "${{ github.workspace }}/BinaryCache/stage1/swift-syntax/cmake/modules";
            }

      - name: Build Bootstrap Foundation Macros
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/swift-foundation-macros

      - name: Configure Bootstrap Testing Macros
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Bootstrap Testing Macros
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ inputs.build_os }}
          arch: ${{ inputs.build_arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-testing/Sources/TestingMacros
          bin-dir: ${{ github.workspace }}/BinaryCache/bootstrap/swift-testing-macros
          install-dir: ${{ github.workspace }}/BuildRoot/bootstrap/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/bootstrap/Library/Developer/Platforms/${{ inputs.build_os }}.platform/Developer/SDKs/${{ inputs.build_os }}Bootstrap.sdk
          swift-compiler: 'Stage0.Swift'
          cmake-defines: |
            @{
              'SwiftSyntax_DIR' = "${{ github.workspace }}/BinaryCache/stage1/swift-syntax/cmake/modules";
            }

      - name: Build Bootstrap Testing Macros
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/swift-testing-macros

      - name: Install Bootstrap Foundation Macros
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/swift-foundation-macros --target install
      - name: Install Bootstrap Testing Macros
        run: cmake --build ${{ github.workspace }}/BinaryCache/bootstrap/swift-testing-macros --target install

      - name: Upload Bootstrap Macros
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-bootstrap-macros
          path: ${{ github.workspace }}/BuildRoot/bootstrap/Library

  stage2-libxml2:
    needs: [stage1-compiler]
    runs-on: ${{ inputs.default_build_runner }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.host_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} Stage2 libxml2

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - uses: actions/checkout@v4.2.2
        with:
          repository: gnome/libxml2
          ref: ${{ inputs.libxml2_revision }}
          path: ${{ github.workspace }}/SourceCache/libxml2
          show-progress: false

      # build.ps1: Build-XML2
      - name: Configure Stage2 libxml2
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Stage2 libxml2
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/libxml2
          bin-dir: ${{ github.workspace }}/BinaryCache/stage2-libxml2-${{ inputs.libxml2_version }}
          install-dir: ${{ github.workspace }}/BuildRoot/Library/stage2-libxml2-${{ inputs.libxml2_version }}/usr
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_POSITION_INDEPENDENT_CODE' = "YES";
              'LIBXML2_WITH_C14N' = "NO";
              'LIBXML2_WITH_CATALOG' = "NO";
              'LIBXML2_WITH_DEBUG' = "NO";
              'LIBXML2_WITH_FTP' = "NO";
              'LIBXML2_WITH_HTML' = "NO";
              'LIBXML2_WITH_HTTP' = "NO";
              'LIBXML2_WITH_ICONV' = "NO";
              'LIBXML2_WITH_ICU' = "NO";
              'LIBXML2_WITH_ISO8859X' = "NO";
              'LIBXML2_WITH_LEGACY' = "NO";
              'LIBXML2_WITH_LZMA' = "NO";
              'LIBXML2_WITH_MEM_DEBUG' = "NO";
              'LIBXML2_WITH_MODULES' = "NO";
              'LIBXML2_WITH_OUTPUT' = "YES";
              'LIBXML2_WITH_PATTERN' = "NO";
              'LIBXML2_WITH_PROGRAMS' = "NO";
              'LIBXML2_WITH_PUSH' = "YES";
              'LIBXML2_WITH_PYTHON' = "NO";
              'LIBXML2_WITH_READER' = "NO";
              'LIBXML2_WITH_REGEXPS' = "YES";
              'LIBXML2_WITH_SAX1' = "NO";
              'LIBXML2_WITH_SCHEMAS' = "NO";
              'LIBXML2_WITH_SCHEMATRON' = "NO";
              'LIBXML2_WITH_TESTS' = "NO";
              'LIBXML2_WITH_THREAD_ALLOC' = "NO";
              'LIBXML2_WITH_THREADS' = "YES";
              'LIBXML2_WITH_TREE' = "YES";
              'LIBXML2_WITH_VALID' = "YES";
              'LIBXML2_WITH_WRITER' = "NO";
              'LIBXML2_WITH_XINCLUDE' = "NO";
              'LIBXML2_WITH_XPATH' = "YES";
              'LIBXML2_WITH_XPTR' = "NO";
              'LIBXML2_WITH_XPTR_LOCS' = "NO";
              'LIBXML2_WITH_ZLIB' = "NO";
            }
      - name: Build Stage2 libxml2
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage2-libxml2-${{ inputs.libxml2_version }}
      - name: Install Stage2 libxml2
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage2-libxml2-${{ inputs.libxml2_version }} --target install

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-stage2-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/stage2-libxml2-${{ inputs.libxml2_version }}/usr

  stage2-compiler:
    needs: [stage1-compiler, sdk, stage2-libxml2, build_tools, cmark_gfm, early_swift_driver]
    runs-on: ${{ inputs.compilers_build_runner }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.compilers_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} ${{ matrix.variant }} Stage2 Compiler

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}

      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-build-tools
          path: ${{ github.workspace }}/BinaryCache/BuildTools/bin
      - uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-stage2-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/stage2-libxml2-${{ inputs.libxml2_version }}/usr
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-cmark-gfm-${{ inputs.swift_cmark_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-early-swift-driver
          path: ${{ github.workspace }}/BinaryCache/swift-driver
      - uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-shared-sdk
          path: ${{ github.workspace }}/BinaryCache/SDK/Library/Developer/Platforms/${{ matrix.os }}.platform
      - name: Download Host CDispatch
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-cdispatch
          path: ${{ github.workspace }}/BinaryCache/libdispatch-c

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/llvm-project
          ref: ${{ inputs.llvm_project_revision }}
          path: ${{ github.workspace }}/SourceCache/llvm-project
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift
          ref: ${{ inputs.swift_revision }}
          path: ${{ github.workspace }}/SourceCache/swift
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-experimental-string-processing
          ref: ${{ inputs.swift_experimental_string_processing_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-experimental-string-processing
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-syntax
          ref: ${{ inputs.swift_syntax_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-syntax
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-corelibs-libdispatch
          ref: ${{ inputs.swift_corelibs_libdispatch_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch
          show-progress: false

      - name: Add Stage1 runtime to PATH
        run: |
          $Stage1Bin = cygpath -w ${{ github.workspace }}/BinaryCache/stage1/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/bin
          $Stage1Bin | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - uses: actions/setup-python@v5
        id: python
        with:
          python-version: ${{ inputs.python_version }}
          architecture: x64 # FIXME(#1004): Remove workaround installing x64 Python on ARM64 CI hosts

      - name: Download and expand Python
        env:
          INPUT_PYTHON_DOWNLOAD_LOCATIONS: ${{ inputs.python_download_locations }}
          INPUT_PYTHON_VERSION: ${{ inputs.python_version }}
        run: |
          function DownloadAndVerify($URL, $Destination, $Hash) {
            New-Item -ItemType Directory (Split-Path -Path $Destination -Parent) -ErrorAction Ignore | Out-Null
            $WebClient = New-Object Net.WebClient
            $WebClient.DownloadFile($URL, $Destination)
            $ExpectedHash = $Hash.ToUpperInvariant()
            $SHA256 = (Get-FileHash -Path $Destination -Algorithm SHA256).Hash.ToUpperInvariant()
            if ($SHA256 -ne $ExpectedHash) {
              throw "SHA256 mismatch ($SHA256 vs $Hash) for $URL"
            }
          }

          $KnownPythons = $env:INPUT_PYTHON_DOWNLOAD_LOCATIONS | ConvertFrom-Json
          $Version = $env:INPUT_PYTHON_VERSION
          $ArchKey = "${{ matrix.arch }}".ToUpper()
          $PythonInfo = $KnownPythons."$Version"."$ArchKey"
          if ($null -eq $PythonInfo) {
            throw "No Python download info for version $Version and architecture $ArchKey"
          }

          $PackagePath = "${{ github.workspace }}\BinaryCache\Python-$ArchKey-$Version.zip"
          DownloadAndVerify -URL $PythonInfo.URL -Destination $PackagePath -Hash $PythonInfo.SHA256

          $ExtractRoot = "${{ github.workspace }}\BuildRoot\Library\Developer\Python-$ArchKey-$Version"
          New-Item -ItemType Directory -Path $ExtractRoot -ErrorAction Ignore | Out-Null
          Expand-Archive -Path $PackagePath -DestinationPath $ExtractRoot -Force
          "PYTHON_LOCATION=$ExtractRoot" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Setup context
        id: setup-context
        env:
          INPUT_PYTHON_VERSION: ${{ inputs.python_version }}
        run: |
          $PythonVersion = [System.Version]$env:INPUT_PYTHON_VERSION
          $PythonRoot = "${env:PYTHON_LOCATION}/tools"

          $RamBytes = (Get-CimInstance -ClassName Win32_ComputerSystem).TotalPhysicalMemory
          $RamGB = [math]::Round($RamBytes / 1GB)
          $LinkJobs = if ("${{ inputs.debug_info }}" -eq "true") {
            2
          } else {
            [math]::Max(2, [math]::Floor($RamGB / 24))
          }

          $Context = @"
          python-root=${PythonRoot}
          python-include-dir=${PythonRoot}/include
          libpython-path=${PythonRoot}/libs/python$($PythonVersion.Major)$($PythonVersion.Minor).lib
          link-jobs=${LinkJobs}
          "@
          Write-Output $Context | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      # build.ps1: Build-Compilers
      - name: Configure Stage2 Compiler
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Stage2 Compiler
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/llvm-project/llvm
          bin-dir: ${{ github.workspace }}/BinaryCache/stage2/build
          install-dir: ${{ github.workspace }}/BinaryCache/stage2/Library/Developer/Toolchains/${{ inputs.swift_version }}+${{ matrix.variant }}/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/SDK/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cache-script: ${{ github.workspace }}/SourceCache/swift/cmake/caches/${{ matrix.os }}-${{ matrix.cpu }}.cmake
          cmake-defines: |
            @{
              'SWIFT_BUILD_DYNAMIC_SDK_OVERLAY' = "NO";
              'SWIFT_BUILD_DYNAMIC_STDLIB' = "NO";
              'SWIFT_BUILD_LIBEXEC' = "NO";
              'SWIFT_BUILD_REMOTE_MIRROR' = "NO";
              'SWIFT_NATIVE_SWIFT_TOOLS_PATH' = "${{ github.workspace }}/BinaryCache/BuildTools/bin";

              'CLANG_TABLEGEN' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/clang-tblgen${ExeSuffix}";
              'CLANG_TIDY_CONFUSABLE_CHARS_GEN' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/clang-tidy-confusable-chars-gen${ExeSuffix}";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'LibXml2_DIR' = "${{ github.workspace }}/BinaryCache/Library/stage2-libxml2-${{ inputs.libxml2_version }}/usr/lib/cmake/libxml2-${{ inputs.libxml2_version }}";
              'LLDB_LIBXML2_VERSION' = "${{ inputs.libxml2_version }}";
              'LLDB_PYTHON_EXE_RELATIVE_PATH' = "python.exe";
              'LLDB_PYTHON_EXT_SUFFIX' = ".pyd";
              'LLDB_PYTHON_RELATIVE_PATH' = "lib/site-packages";
              'LLDB_PYTHON_DLL_RELATIVE_PATH' = "../../../../Python-${{ inputs.python_version }}/usr/bin";
              'LLDB_TABLEGEN' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/lldb-tblgen${ExeSuffix}";
              'LLVM_CONFIG_PATH' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/llvm-config${ExeSuffix}";
              'LLVM_ENABLE_ASSERTIONS' = ${{ matrix.variant == 'Asserts' && '"YES"' || '"NO"' }};
              'LLVM_ENABLE_LLD' = "NO";
              'LLVM_ENABLE_LTO' = "OFF";
              'LLVM_EXTERNAL_SWIFT_SOURCE_DIR' = "${{ github.workspace }}/SourceCache/swift";
              'LLVM_HOST_TRIPLE' = "${{ matrix.triple }}";
              'LLVM_NATIVE_TOOL_DIR' = "${{ github.workspace }}/BinaryCache/BuildTools/bin";
              'LLVM_TABLEGEN' = "${{ github.workspace }}/BinaryCache/BuildTools/bin/llvm-tblgen${ExeSuffix}";
              'LLVM_USE_HOST_TOOLS' = "NO";
              'Python3_EXECUTABLE' = "${{ steps.python.outputs.python-path }}";
              'Python3_INCLUDE_DIR' = "${{ steps.setup-context.outputs.python-include-dir }}";
              'Python3_LIBRARY' = "${{ steps.setup-context.outputs.libpython-path }}";
              'Python3_ROOT_DIR' = "${{ steps.setup-context.outputs.python-root }}";
              'Python3_VERSION' = "${{ inputs.python_version }}";
              'SWIFT_TOOLCHAIN_VERSION' = "${{ inputs.swift_version }} ${{ inputs.swift_tag }}";
              'SWIFT_BUILD_SWIFT_SYNTAX' = "YES";
              'SWIFT_CLANG_LOCATION' = "${{ github.workspace }}/BinaryCache/stage1/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/bin";
              'SWIFT_EARLY_SWIFT_DRIVER_BUILD' = "${{ github.workspace }}/BinaryCache/swift-driver/bin";
              'SWIFT_ENABLE_EXPERIMENTAL_CONCURRENCY' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_CXX_INTEROP' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_DIFFERENTIABLE_PROGRAMMING' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_DISTRIBUTED' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_OBSERVATION' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_STRING_PROCESSING' = "YES";
              'SWIFT_ENABLE_RUNTIME_MODULE' = "YES";
              'SWIFT_ENABLE_BACKTRACING' = "YES";
              'SWIFT_ENABLE_SYNCHRONIZATION' = "YES";
              'SWIFT_ENABLE_VOLATILE' = "YES";
              'SWIFT_PATH_TO_LIBDISPATCH_SOURCE' = "${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch";
              'SWIFT_PATH_TO_STRING_PROCESSING_SOURCE' = "${{ github.workspace }}/SourceCache/swift-experimental-string-processing";
              'SWIFT_PATH_TO_SWIFT_SDK' = "${{ github.workspace }}/BinaryCache/SDK/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk";
              'SWIFT_PATH_TO_SWIFT_SYNTAX_SOURCE' = "${{ github.workspace }}/SourceCache/swift-syntax";
              'SWIFT_STDLIB_ASSERTIONS' = "NO";
              'SWIFTSYNTAX_ENABLE_ASSERTIONS' = "NO";
              'cmark-gfm_DIR' = "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/lib/cmake";
              'dispatch_DIR' = "${{ github.workspace }}/BinaryCache/libdispatch-c/cmake/modules";

              'CLANG_VENDOR' = 'compnerd.org';
              'CLANG_VENDOR_UTI' = 'org.compnerd.dt';
              'LLVM_APPEND_VC_REV' = "NO";
              'LLVM_VERSION_SUFFIX' = "";
              'LLVM_PARALLEL_LINK_JOBS' = "${{ steps.setup-context.outputs.link-jobs }}";
              'SWIFT_PARALLEL_LINK_JOBS' = "${{ steps.setup-context.outputs.link-jobs }}";
            }

      - name: Install Stage2 Compiler Distribution
        run: cmake --build ${{ github.workspace }}/BinaryCache/stage2/build --target install-distribution

      - name: Copy cmark-gfm shared libraries
        run: |
          Copy-Item -Path "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/bin/*.dll" -Destination "${{ github.workspace }}/BinaryCache/stage2/Library/Developer/Toolchains/${{ inputs.swift_version }}+${{ matrix.variant }}/usr/bin"

      - uses: jannekem/run-python-script-action@v1
        with:
          script: |
            import os
            import plistlib

            info_plist = r'${{ github.workspace }}/BinaryCache/stage2/Library/Developer/Toolchains/${{ inputs.swift_version }}+${{ matrix.variant }}/ToolchainInfo.plist'
            with open(os.path.normpath(info_plist), 'wb') as plist:
              plistlib.dump({ 'Identifier': '${{ inputs.swift_tag }}.${{ matrix.variant }}', 'FallbackLibrarySearchPaths': ['usr/bin'], 'Version': '${{ inputs.swift_version }}' }, plist)

      - name: Upload Stage2 Compiler
        uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-${{ matrix.variant }}-compilers
          path: ${{ github.workspace }}/BinaryCache/stage2/Library

      - name: Extract Stage2 SwiftSyntax
        run: |
          $SwiftSyntaxRoot = "${{ github.workspace }}/BinaryCache/stage2/swift-syntax"
          $SwiftSyntaxHostLib = "$SwiftSyntaxRoot/lib/swift/host"
          New-Item -Path $SwiftSyntaxHostLib -ItemType Directory -Force | Out-Null

          $ToolchainRoot = "${{ github.workspace }}/BinaryCache/stage2/Library/Developer/Toolchains/${{ inputs.swift_version }}+${{ matrix.variant }}"
          Copy-Item -Path "$ToolchainRoot/usr/lib/*.lib" -Destination "$SwiftSyntaxRoot/lib"
          Copy-Item -Path "$ToolchainRoot/usr/lib/*.lib" -Destination $SwiftSyntaxHostLib
          Copy-Item -Path "$ToolchainRoot/usr/lib/swift/host/*.swiftmodule" -Destination $SwiftSyntaxHostLib -Recurse

          $BinaryDir = cygpath -m "${{ github.workspace }}/BinaryCache/stage2/build"
          $Module = "${{ github.workspace }}/BinaryCache/stage2/build/cmake/modules/SwiftSyntaxConfig.cmake"
          (Get-Content $Module).Replace($BinaryDir, '<BINARY_DIR>') | Set-Content $Module
          $ModulesDir = "$SwiftSyntaxRoot/cmake/modules"
          New-Item -Path $ModulesDir -ItemType Directory -Force | Out-Null
          Copy-Item -Path $Module -Destination $ModulesDir

      - name: Upload Stage2 SwiftSyntax
        uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-${{ matrix.variant }}-swift-syntax
          path: ${{ github.workspace }}/BinaryCache/stage2/swift-syntax

      - name: Upload PDBs to Azure
        uses: thebrowsercompany/action-publish-symbols@d5f4b06266463bf9a71ef6a4e3b0b8d79eaa82a4
        if: ${{ inputs.debug_info }}
        with:
          accountName: ${{ vars.SYMBOL_SERVER_ACCOUNT }}
          personalAccessToken: ${{ secrets.SYMBOL_SERVER_PAT }}
          symbolsFolder: ${{ github.workspace }}/BinaryCache/stage2/build
          searchPattern: '**/*.pdb'

      - name: Upload PDBs to Public Symbol Server
        uses: ./SourceCache/ci-build/.github/actions/publish-symbols-r2
        if: ${{ inputs.debug_info }}
        with:
          symbolsFolder: ${{ github.workspace }}/BinaryCache/stage2/build
          searchPattern: '*.pdb'
          swift-version: ${{ inputs.swift_version }}
          arch: ${{ matrix.arch }}
          variant: ${{ matrix.variant }}
          build-revision: ${{ inputs.swift_build_revision }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID }}

  compiler-runtime:
    needs: [stage1-compiler]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.target_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} Compiler Runtime

    steps:
      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        if: matrix.os != 'Android' || inputs.build_android
        with:
          setup-vs-dev-env: ${{ matrix.os == 'Windows' }}
          host-arch: ${{ matrix.arch }}

      - name: Install CMake ${{ env.CMAKE_VERSION }}
        if: matrix.os != 'Android' || inputs.build_android
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        if: matrix.os != 'Android' || inputs.build_android
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          repository: swiftlang/llvm-project
          ref: ${{ inputs.llvm_project_revision }}
          path: ${{ github.workspace }}/SourceCache/llvm-project
          show-progress: false

      - uses: nttld/setup-ndk@v1
        if: matrix.os == 'Android' && inputs.build_android
        id: setup-ndk
        with:
          ndk-version: ${{ inputs.ANDROID_NDK_VERSION }}
          local-cache: true

      - uses: actions/setup-python@v5
        if: matrix.os != 'Android' || inputs.build_android
        with:
          python-version: ${{ inputs.python_version }}
          architecture: x64 # FIXME(#1004): Remove workaround installing x64 Python on ARM64 CI hosts

      - name: Configure Compiler Runtime LLVM
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Compiler Runtime LLVM
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/llvm-project/llvm
          bin-dir: ${{ github.workspace }}/BinaryCache/compiler-runtime-llvm
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          assembler: 'Stage1'
          c-compiler: ${{ matrix.os == 'Windows' && 'Stage1.C' || 'Stage1.GNUC' }}
          cxx-compiler: ${{ matrix.os == 'Windows' && 'Stage1.CXX' || 'Stage1.GNUCXX' }}
          cmake-defines: |
            @{
              'LLVM_ENABLE_ASSERTIONS' = "YES";
              'LLVM_HOST_TRIPLE' = "${{ matrix.triple }}";
            }

      - name: Get LLVM major version
        if: matrix.os != 'Android' || inputs.build_android
        id: llvm-version
        run: |
          $LLVMConfigVersion = "${{ github.workspace }}/BinaryCache/compiler-runtime-llvm/lib/cmake/llvm/LLVMConfigVersion.cmake"
          $Version = Select-String -Path $LLVMConfigVersion -Pattern 'set\(PACKAGE_VERSION "([0-9]+)\.'
          if (-not $Version) {
            throw "Could not read the LLVM version from $LLVMConfigVersion"
          }
          "major=$($Version.Matches[0].Groups[1].Value)" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      # build.ps1: Build-CompilerRuntime
      - name: Configure Compiler Runtime Builtins
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Compiler Runtime Builtins
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/llvm-project/compiler-rt/lib/builtins
          bin-dir: ${{ github.workspace }}/BinaryCache/compiler-runtime-builtins
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/lib/clang/${{ steps.llvm-version.outputs.major }}
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          assembler: 'Stage1'
          c-compiler: ${{ matrix.os == 'Windows' && 'Stage1.C' || 'Stage1.GNUC' }}
          cxx-compiler: ${{ matrix.os == 'Windows' && 'Stage1.CXX' || 'Stage1.GNUCXX' }}
          cmake-defines: |
            @{
              'LLVM_DIR' = "${{ github.workspace }}/BinaryCache/compiler-runtime-llvm/lib/cmake/llvm";
              'LLVM_ENABLE_PER_TARGET_RUNTIME_DIR' = "YES";
              'COMPILER_RT_DEFAULT_TARGET_ONLY' = "YES";
            }
      - name: Install Compiler Runtime Builtins
        if: matrix.os != 'Android' || inputs.build_android
        run: cmake --build ${{ github.workspace }}/BinaryCache/compiler-runtime-builtins --target install-compiler-rt

      - name: Configure Compiler Runtime
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Compiler Runtime
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/llvm-project/compiler-rt
          bin-dir: ${{ github.workspace }}/BinaryCache/compiler-runtime
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/lib/clang/${{ steps.llvm-version.outputs.major }}
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          assembler: 'Stage1'
          c-compiler: ${{ matrix.os == 'Windows' && 'Stage1.C' || 'Stage1.GNUC' }}
          cxx-compiler: ${{ matrix.os == 'Windows' && 'Stage1.CXX' || 'Stage1.GNUCXX' }}
          cmake-defines: |
            @{
              'LLVM_DIR' = "${{ github.workspace }}/BinaryCache/compiler-runtime-llvm/lib/cmake/llvm";
              'LLVM_ENABLE_PER_TARGET_RUNTIME_DIR' = "YES";
              'COMPILER_RT_DEFAULT_TARGET_ONLY' = "YES";
              'COMPILER_RT_BUILD_BUILTINS' = "NO";
              'COMPILER_RT_BUILD_CRT' = "NO";
              'COMPILER_RT_BUILD_LIBFUZZER' = "NO";
              'COMPILER_RT_BUILD_ORC' = "NO";
              'COMPILER_RT_BUILD_XRAY' = "NO";
              'COMPILER_RT_BUILD_PROFILE' = "YES";
              'COMPILER_RT_BUILD_SANITIZERS' = "YES";
            }
      - name: Install Compiler Runtime
        if: matrix.os != 'Android' || inputs.build_android
        run: cmake --build ${{ github.workspace }}/BinaryCache/compiler-runtime --target install-compiler-rt

      - name: Upload Compiler Runtime
        if: matrix.os != 'Android' || inputs.build_android
        uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-compiler-runtime
          path: ${{ github.workspace }}/BuildRoot/Library

  zlib:
    needs: [stage1-compiler]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.target_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} zlib

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: ${{ matrix.os == 'Windows' }}
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - name: Compute workspace hash
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-zlib
      - uses: actions/checkout@v4.2.2
        with:
          repository: madler/zlib
          ref: ${{ inputs.zlib_revision }}
          path: ${{ github.workspace }}/SourceCache/zlib
          show-progress: false

      - uses: nttld/setup-ndk@v1
        if: matrix.os == 'Android'
        id: setup-ndk
        with:
          ndk-version: ${{ inputs.ANDROID_NDK_VERSION }}
          local-cache: true

      # build.ps1: Build-Zlib
      - name: Configure zlib
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: zlib
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/zlib
          bin-dir: ${{ github.workspace }}/BinaryCache/zlib-${{ inputs.zlib_version }}
          install-dir: ${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: ${{ matrix.os == 'Windows' && 'Stage1.C' || 'Stage1.GNUC' }}
          cxx-compiler: ${{ matrix.os == 'Windows' && 'Stage1.CXX' || 'Stage1.GNUCXX' }}
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_POSITION_INDEPENDENT_CODE' = "YES";
            }
      - name: Build zlib
        run: cmake --build ${{ github.workspace }}/BinaryCache/zlib-${{ inputs.zlib_version }}
      - name: Install zlib
        run: cmake --build ${{ github.workspace }}/BinaryCache/zlib-${{ inputs.zlib_version }} --target install

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr

  brotli:
    needs: [stage1-compiler]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.target_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} brotli

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: ${{ matrix.os == 'Windows' }}
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - name: Compute workspace hash
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-brotli
      - uses: actions/checkout@v4.2.2
        with:
          repository: google/brotli
          ref: ${{ inputs.brotli_revision }}
          path: ${{ github.workspace }}/SourceCache/brotli
          show-progress: false

      - uses: nttld/setup-ndk@v1
        if: matrix.os == 'Android'
        id: setup-ndk
        with:
          ndk-version: ${{ inputs.ANDROID_NDK_VERSION }}
          local-cache: true

      # build.ps1: Build-Brotli
      - name: Configure brotli
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: brotli
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/brotli
          bin-dir: ${{ github.workspace }}/BinaryCache/brotli-${{ inputs.brotli_version }}
          install-dir: ${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: ${{ matrix.os == 'Windows' && 'Stage1.C' || 'Stage1.GNUC' }}
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_POSITION_INDEPENDENT_CODE' = "YES";
            }
      - name: Build brotli
        run: cmake --build ${{ github.workspace }}/BinaryCache/brotli-${{ inputs.brotli_version }}
      - name: Install brotli
        run: cmake --build ${{ github.workspace }}/BinaryCache/brotli-${{ inputs.brotli_version }} --target install

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr

  curl:
    needs: [stage1-compiler, zlib, brotli]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.target_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} curl

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false

      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: ${{ matrix.os == 'Windows' }}
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - name: Compute workspace hash
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-curl
      - uses: actions/checkout@v4.2.2
        with:
          repository: curl/curl
          ref: ${{ inputs.curl_revision }}
          path: ${{ github.workspace }}/SourceCache/curl
          show-progress: false

      - uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr

      - uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr

      - uses: nttld/setup-ndk@v1
        if: matrix.os == 'Android'
        id: setup-ndk
        with:
          ndk-version: ${{ inputs.ANDROID_NDK_VERSION }}
          local-cache: true

      # build.ps1: Build-Curl
      - name: Configure curl
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: curl
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/curl
          bin-dir: ${{ github.workspace }}/BinaryCache/curl-${{ inputs.curl_version }}
          install-dir: ${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: ${{ matrix.os == 'Windows' && 'Stage1.C' || 'Stage1.GNUC' }}
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'BUILD_TESTING' = "NO";
              'CMAKE_POSITION_INDEPENDENT_CODE' = "YES";
              'BROTLI_INCLUDE_DIR' = "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr/include";
              'BROTLICOMMON_LIBRARY' = "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr/lib/$(if ("${{ matrix.os }}" -eq "Windows") { "brotlicommon.lib" } else { "libbrotlicommon.a" })";
              'BROTLIDEC_LIBRARY' = "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr/lib/$(if ("${{ matrix.os }}" -eq "Windows") { "brotlidec.lib" } else { "libbrotlidec.a" })";
              'BUILD_CURL_EXE' = "NO";
              'BUILD_LIBCURL_DOCS' = "NO";
              'BUILD_MISC_DOCS' = "NO";
              'CURL_CA_BUNDLE' = "none";
              'CURL_CA_FALLBACK' = "NO";
              'CURL_CA_PATH' = "none";
              'CURL_BROTLI' = "YES";
              'CURL_DISABLE_ALTSVC' = "NO";
              'CURL_DISABLE_AWS' = "YES";
              'CURL_DISABLE_BASIC_AUTH' = "NO";
              'CURL_DISABLE_BEARER_AUTH' = "NO";
              'CURL_DISABLE_BINDLOCAL' = "NO";
              'CURL_DISABLE_COOKIES' = "NO";
              'CURL_DISABLE_DICT' = "YES";
              'CURL_DISABLE_DIGEST_AUTH' = "NO";
              'CURL_DISABLE_DOH' = "NO";
              'CURL_DISABLE_FILE' = "YES";
              'CURL_DISABLE_FORM_API' = "NO";
              'CURL_DISABLE_FTP' = "YES";
              'CURL_DISABLE_GETOPTIONS' = "NO";
              'CURL_DISABLE_GOPHER' = "YES";
              'CURL_DISABLE_HEADERS_API' = "YES";
              'CURL_DISABLE_HSTS' = "NO";
              'CURL_DISABLE_HTTP' = "NO";
              'CURL_DISABLE_HTTP_AUTH' = "NO";
              'CURL_DISABLE_IMAP' = "YES";
              'CURL_DISABLE_KERBEROS_AUTH' = "NO";
              'CURL_DISABLE_LDAP' = "YES";
              'CURL_DISABLE_LDAPS' = "YES";
              'CURL_DISABLE_MIME' = "NO";
              'CURL_DISABLE_MQTT' = "YES";
              'CURL_DISABLE_NEGOTIATE_AUTH' = "NO";
              'CURL_DISABLE_NETRC' = "NO";
              'CURL_DISABLE_NTLM' = "NO";
              'CURL_DISABLE_PARSEDATE' = "NO";
              'CURL_DISABLE_POP3' = "YES";
              'CURL_DISABLE_PROGRESS_METER' = "YES";
              'CURL_DISABLE_PROXY' = "NO";
              'CURL_DISABLE_RTSP' = "YES";
              'CURL_DISABLE_SHUFFLE_DNS' = "YES";
              'CURL_DISABLE_SMB' = "YES";
              'CURL_DISABLE_SMTP' = "YES";
              'CURL_DISABLE_SOCKETPAIR' = "YES";
              'CURL_DISABLE_SRP' = "NO";
              'CURL_DISABLE_TELNET' = "YES";
              'CURL_DISABLE_TFTP' = "YES";
              'CURL_DISABLE_VERBOSE_STRINGS' = "NO";
              'CURL_LTO' = "NO";
              'CURL_USE_BEARSSL' = "NO";
              'CURL_USE_GNUTLS' = "NO";
              'CURL_USE_GSSAPI' = "NO";
              'CURL_USE_LIBPSL' = "NO";
              'CURL_USE_LIBSSH' = "NO";
              'CURL_USE_LIBSSH2' = "NO";
              'CURL_USE_MBEDTLS' = "NO";
              'CURL_USE_OPENSSL' = "NO";
              'CURL_USE_SCHANNEL' = if ("${{ matrix.os }}" -eq "Windows") { "YES" } else { "NO" };
              'CURL_USE_WOLFSSL' = "NO";
              'CURL_WINDOWS_SSPI' = if ("${{ matrix.os }}" -eq "Windows") { "YES" } else { "NO" };
              'CURL_ZLIB' = "YES";
              'CURL_ZSTD' = "NO";
              'ENABLE_ARES' = "NO";
              'ENABLE_CURLDEBUG' = "NO";
              'ENABLE_CURL_MANUAL' = "NO";
              'ENABLE_DEBUG' = "NO";
              'ENABLE_IPV6' = "YES";
              'ENABLE_THREADED_RESOLVER' = "NO";
              'ENABLE_UNICODE' = "YES";
              'ENABLE_UNIX_SOCKETS' = "NO";
              'ENABLE_WEBSOCKETS' = "YES";
              'HAVE_POLL_FINE' = "NO";
              'USE_ECH' = "NO";
              'USE_HTTPSRR' = "NO";
              'USE_IDN2' = "NO";
              'USE_MSH3' = "NO";
              'USE_NGHTTP2' = "NO";
              'USE_NGTCP2' = "NO";
              'USE_QUICHE' = "NO";
              'USE_OPENSSL_QUIC' = "NO";
              'USE_WIN32_IDN' = if ("${{ matrix.os }}" -eq "Windows") { "YES" } else { "NO" };
              'USE_WIN32_LARGE_FILES' = if ("${{ matrix.os }}" -eq "Windows") { "YES" } else { "NO" };
              'USE_WIN32_LDAP' = "NO";
              'ZLIB_ROOT' = "${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr";
              'ZLIB_LIBRARY' = "${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr/lib/zlibstatic.lib";
            }
      - name: Build curl
        run: cmake --build ${{ github.workspace }}/BinaryCache/curl-${{ inputs.curl_version }}
      - name: Install curl
        run: cmake --build ${{ github.workspace }}/BinaryCache/curl-${{ inputs.curl_version }} --target install

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-curl-${{ inputs.curl_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr

  libxml2:
    needs: [stage1-compiler]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.target_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} libxml2

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: ${{ matrix.os == 'Windows' }}
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - name: Compute workspace hash
        id: workspace_hash
        shell: pwsh
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-libxml2

      - uses: actions/checkout@v4.2.2
        with:
          repository: gnome/libxml2
          ref: ${{ inputs.libxml2_revision }}
          path: ${{ github.workspace }}/SourceCache/libxml2
          show-progress: false

      - uses: nttld/setup-ndk@v1
        if: matrix.os == 'Android'
        id: setup-ndk
        with:
          ndk-version: ${{ inputs.ANDROID_NDK_VERSION }}
          local-cache: true

      # build.ps1: Build-XML2
      - name: Configure libxml2
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: libxml2
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/libxml2
          bin-dir: ${{ github.workspace }}/BinaryCache/libxml2-${{ inputs.libxml2_version }}
          install-dir: ${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: ${{ matrix.os == 'Windows' && 'Stage1.C' || 'Stage1.GNUC' }}
          cxx-compiler: ${{ matrix.os == 'Windows' && 'Stage1.CXX' || 'Stage1.GNUCXX' }}
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_POSITION_INDEPENDENT_CODE' = "YES";
              'LIBXML2_WITH_ICONV' = "NO";
              'LIBXML2_WITH_ICU' = "NO";
              'LIBXML2_WITH_LZMA' = "NO";
              'LIBXML2_WITH_PYTHON' = "NO";
              'LIBXML2_WITH_TESTS' = "NO";
              'LIBXML2_WITH_THREADS' = "YES";
              'LIBXML2_WITH_ZLIB' = "NO";
            }
      - name: Build libxml2
        run: cmake --build ${{ github.workspace }}/BinaryCache/libxml2-${{ inputs.libxml2_version }}
      - name: Install libxml2
        run: cmake --build ${{ github.workspace }}/BinaryCache/libxml2-${{ inputs.libxml2_version }} --target install

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr

  macros:
    if: inputs.build_os == 'Windows'
    needs: [stage2-compiler, sdk]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.host_matrix) }}

    name: Windows ${{ matrix.arch }} Macros

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library
      - name: Download Stage2 SwiftSyntax
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Windows-${{ matrix.arch }}-Asserts-swift-syntax
          path: ${{ github.workspace }}/BinaryCache/stage2/swift-syntax
      - name: Download Target SDK
        uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-shared-sdk
          path: ${{ github.workspace }}/BinaryCache/SDK/Library/Developer/Platforms/Windows.platform
      - name: Download Host SDK
        uses: actions/download-artifact@v4
        if: matrix.arch != inputs.build_arch
        with:
          name: Windows-${{ inputs.build_arch }}-shared-sdk
          path: ${{ github.workspace }}/BinaryCache/HostSDK/Library/Developer/Platforms/Windows.platform
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-foundation
          ref: ${{ inputs.swift_foundation_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-foundation
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-testing
          ref: ${{ inputs.swift_testing_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-testing
          show-progress: false

      - name: Add Host SDK runtime to PATH
        run: |
          $SDKCache = if ("${{ matrix.arch }}" -eq "${{ inputs.build_arch }}") { "SDK" } else { "HostSDK" }
          $RuntimePath = cygpath -w "${{ github.workspace }}/BinaryCache/$SDKCache/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/bin"
          $RuntimePath | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Relocate SwiftSyntax
        run: |
          $Module = "${{ github.workspace }}/BinaryCache/stage2/swift-syntax/cmake/modules/SwiftSyntaxConfig.cmake"
          $BinaryDir = cygpath -m ${{ github.workspace }}/BinaryCache/stage2/swift-syntax
          (Get-Content $Module).Replace('<BINARY_DIR>', $BinaryDir) | Set-Content $Module

      # build.ps1: Build-FoundationMacros
      - name: Configure Foundation Macros
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with: 
          project-name: swift-foundation-macros
          swift-version: ${{ inputs.swift_version }}
          enable-caching: true
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-foundation/Sources/FoundationMacros
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-foundation-macros
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/SDK/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SwiftSyntax_DIR' = "${{ github.workspace }}/BinaryCache/stage2/swift-syntax/cmake/modules";
            }

      - name: Build Foundation Macros
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-foundation-macros

      # build.ps1: Build-TestingMacros
      - name: Configure Testing Macros
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with: 
          project-name: swift-testing-macros
          swift-version: ${{ inputs.swift_version }}
          enable-caching: true
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-testing/Sources/TestingMacros
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-testing-macros
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/SDK/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SwiftSyntax_DIR' = "${{ github.workspace }}/BinaryCache/stage2/swift-syntax/cmake/modules";
            }

      - name: Build Testing Macros
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-testing-macros

      - name: Install Foundation Macros
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-foundation-macros --target install
      - name: Install Testing Macros
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-testing-macros --target install

      - name: Upload macros
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-macros
          path: ${{ github.workspace }}/BuildRoot/Library

      - name: Upload PDBs to Azure
        uses: thebrowsercompany/action-publish-symbols@d5f4b06266463bf9a71ef6a4e3b0b8d79eaa82a4
        if: ${{ inputs.debug_info }}
        with:
          accountName: ${{ vars.SYMBOL_SERVER_ACCOUNT }}
          personalAccessToken: ${{ secrets.SYMBOL_SERVER_PAT }}
          symbolsFolder: ${{ github.workspace }}/BinaryCache/swift-foundation-macros
          searchPattern: '**/*.pdb'

      - name: Upload PDBs to Public Symbol Server
        uses: ./SourceCache/ci-build/.github/actions/publish-symbols-r2
        if: ${{ inputs.debug_info }}
        with:
          symbolsFolder: ${{ github.workspace }}/BinaryCache/swift-foundation-macros
          searchPattern: '*.pdb'
          swift-version: ${{ inputs.swift_version }}
          arch: ${{ matrix.arch }}
          variant: ${{ matrix.variant }}
          build-revision: ${{ inputs.swift_build_revision }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID }}

      - name: Upload DLLs to Azure
        uses: thebrowsercompany/action-publish-symbols@d5f4b06266463bf9a71ef6a4e3b0b8d79eaa82a4
        if: ${{ inputs.debug_info }}
        with:
          accountName: ${{ vars.SYMBOL_SERVER_ACCOUNT }}
          personalAccessToken: ${{ secrets.SYMBOL_SERVER_PAT }}
          symbolsFolder: ${{ github.workspace }}/BinaryCache/swift-foundation-macros
          searchPattern: '**/*.dll'

  sdk:
    needs: [stage1-compiler, bootstrap-macros, zlib, brotli, curl, libxml2]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: Windows
            arch: amd64
            triple: x86_64-unknown-windows-msvc
            static: true
          - os: Windows
            arch: amd64
            triple: x86_64-unknown-windows-msvc
            static: false
          - os: Windows
            arch: arm64
            triple: aarch64-unknown-windows-msvc
            static: true
          - os: Windows
            arch: arm64
            triple: aarch64-unknown-windows-msvc
            static: false
          - os: Windows
            arch: x86
            triple: i686-unknown-windows-msvc
            static: true
          - os: Windows
            arch: x86
            triple: i686-unknown-windows-msvc
            static: false
          - os: Android
            arch: arm64
            triple: 'aarch64-unknown-linux-android${{ inputs.ANDROID_API_LEVEL }}'
            static: true
          - os: Android
            arch: armv7
            triple: 'armv7-unknown-linux-androideabi${{ inputs.ANDROID_API_LEVEL }}'
            static: true
          - os: Android
            arch: i686
            triple: 'i686-unknown-linux-android${{ inputs.ANDROID_API_LEVEL }}'
            static: true
          - os: Android
            arch: x86_64
            triple: 'x86_64-unknown-linux-android${{ inputs.ANDROID_API_LEVEL }}'
            static: true
          - os: Android
            arch: arm64
            triple: 'aarch64-unknown-linux-android${{ inputs.ANDROID_API_LEVEL }}'
            static: false
          - os: Android
            arch: armv7
            triple: 'armv7-unknown-linux-androideabi${{ inputs.ANDROID_API_LEVEL }}'
            static: false
          - os: Android
            arch: i686
            triple: 'i686-unknown-linux-android${{ inputs.ANDROID_API_LEVEL }}'
            static: false
          - os: Android
            arch: x86_64
            triple: 'x86_64-unknown-linux-android${{ inputs.ANDROID_API_LEVEL }}'
            static: false

    name: ${{ matrix.os }} ${{ matrix.arch }} ${{ matrix.static && 'static' || 'shared' }} SDK

    steps:
      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        if: matrix.os != 'Android' || inputs.build_android
        with:
          setup-vs-dev-env: ${{ matrix.os == 'Windows' }}
          host-arch: ${{ matrix.arch }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}
      - uses: actions/setup-python@v5
        if: matrix.os != 'Android' || inputs.build_android
        with:
          python-version: ${{ inputs.python_version }}
          architecture: x64 # FIXME(#1004): Remove workaround installing x64 Python on ARM64 CI hosts

      - uses: actions/download-artifact@v4
        if: matrix.os != 'Android' || inputs.build_android
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr
      - uses: actions/download-artifact@v4
        if: matrix.os != 'Android' || inputs.build_android
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-curl-${{ inputs.curl_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr
      - uses: actions/download-artifact@v4
        if: matrix.os != 'Android' || inputs.build_android
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr
      - uses: actions/download-artifact@v4
        if: matrix.os != 'Android' || inputs.build_android
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr

      - name: Download Stage1 Compiler
        if: matrix.os != 'Android' || inputs.build_android
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library
      - uses: actions/download-artifact@v4
        if: matrix.os != 'Android' || inputs.build_android
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-bootstrap-macros
          path: ${{ github.workspace }}/BinaryCache/Library

      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          repository: swiftlang/swift
          ref: ${{ inputs.swift_revision }}
          path: ${{ github.workspace }}/SourceCache/swift
          show-progress: false
      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          repository: swiftlang/llvm-project
          ref: ${{ inputs.llvm_project_revision }}
          path: ${{ github.workspace }}/SourceCache/llvm-project
          show-progress: false
      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          repository: apple/swift-collections
          ref: ${{ inputs.swift_collections_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-collections
          show-progress: false
      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          repository: apple/swift-corelibs-libdispatch
          ref: ${{ inputs.swift_corelibs_libdispatch_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch
          show-progress: false
      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          repository: swiftlang/swift-corelibs-foundation
          ref: ${{ inputs.swift_corelibs_foundation_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-corelibs-foundation
          show-progress: false
      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          repository: apple/swift-foundation
          ref: ${{ inputs.swift_foundation_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-foundation
          show-progress: false
      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          repository: apple/swift-foundation-icu
          ref: ${{ inputs.swift_foundation_icu_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-foundation-icu
          show-progress: false
      - uses: actions/checkout@v4.2.2
        if: matrix.os != 'Android' || inputs.build_android
        with:
          repository: swiftlang/swift-experimental-string-processing
          ref: ${{ inputs.swift_experimental_string_processing_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-experimental-string-processing
          show-progress: false
      - uses: actions/checkout@v4.2.2
        if: ${{ !matrix.static && (matrix.os != 'Android' || inputs.build_android) }}
        with:
          repository: swiftlang/swift-testing
          ref: ${{ inputs.swift_testing_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-testing
          show-progress: false
      - uses: actions/checkout@v4.2.2
        if: ${{ !matrix.static && (matrix.os != 'Android' || inputs.build_android) }}
        with:
          repository: swiftlang/swift-corelibs-xctest
          ref: ${{ inputs.swift_corelibs_xctest_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-corelibs-xctest
          show-progress: false

      - name: Add Stage1 runtime to PATH
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          $RTLPath = cygpath -w ${{ github.workspace }}/BinaryCache/stage1/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/bin
          echo ${RTLPath} | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - uses: nttld/setup-ndk@v1
        if: matrix.os == 'Android' && inputs.build_android
        id: setup-ndk
        with:
          ndk-version: ${{ inputs.ANDROID_NDK_VERSION }}
          local-cache: true

      - name: Run Resync
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cd ${{ github.workspace }}/SourceCache/swift/Runtimes
          cmake -P Resync.cmake

      # build.ps1: Build-CDispatch
      - name: Configure CDispatch
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: libdispatch-c
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch 
          bin-dir: ${{ github.workspace }}/BinaryCache/libdispatch-c 
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: ${{ matrix.os == 'Windows' && 'Stage1.C' || 'Stage1.GNUC' }}
          cxx-compiler: ${{ matrix.os == 'Windows' && 'Stage1.CXX' || 'Stage1.GNUCXX' }}
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'BUILD_TESTING' = "NO";
              'ENABLE_SWIFT' = "NO";
            }
      - name: Build CDispatch
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/libdispatch-c

      - name: Upload Host CDispatch
        uses: thebrowsercompany/gha-upload-tar-artifact@e18c33b1cd416d0d96a91dc6dce06219f98e4e27 # main
        if: ${{ matrix.os == inputs.build_os && (matrix.arch == 'amd64' || matrix.arch == 'arm64') && !matrix.static }}
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-cdispatch
          path: ${{ github.workspace }}/BinaryCache/libdispatch-c

      # build.ps1: Build-SDK
      - name: Configure Runtime
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Runtime
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Core
          bin-dir: ${{ github.workspace }}/BinaryCache/Runtime 
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'dispatch_DIR' = "${{ github.workspace }}/BinaryCache/libdispatch-c/cmake/modules";
              # FIXME(hjyamauchi) Should dynamic to libdispatch https://github.com/swiftlang/swift/issues/87548
              'CMAKE_CXX_FLAGS' = ${{ matrix.static && '@("-Ddispatch_STATIC")' || '@()' }};
              'CMAKE_Swift_FLAGS' = ${{ matrix.static && '@("-Xcc", "-static-libclosure")' || '@()' }};
              # FIXME(compnerd) remove this once the default option is flipped to `ON`.
              'SwiftCore_ENABLE_BACKTRACING' = "YES";
              # FIXME(compnerd) remove this once the default option is flipped to `ON`.
              'SwiftCore_ENABLE_CONCURRENCY' = "YES";
              # FIXME(compnerd) remove this once the default option is flipped to `ON`.
              'SwiftCore_ENABLE_REMOTE_MIRROR' = "YES";
              # FIXME(compnerd) this currently causes a build failure on Windows, but
              # this should be enabled when building the dynamic runtime.
              'SwiftCore_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build Runtime
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Runtime
      - name: Install Runtime
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Runtime --target install

      # build.ps1: Build-SDK
      - name: Configure Overlay
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Overlay
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Overlay
          bin-dir: ${{ github.workspace }}/BinaryCache/Overlay
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/Runtime/cmake/SwiftCore";
              # FIXME(compnerd) this currently causes a build failure on Windows, but
              # this should be enabled when building the dynamic runtime.
              'SwiftOverlay_ENABLE_LIBRARY_EVOLUTION' = "NO";
              'SwiftOverlay_ENABLE_CXX_INTEROP' = "YES";
            }
      - name: Build Overlay
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Overlay
      - name: Install Overlay
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Overlay --target install

      # build.ps1: Build-SDK
      - name: Configure String Processing
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: StringProcessing
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Supplemental/StringProcessing
          bin-dir: ${{ github.workspace }}/BinaryCache/StringProcessing
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/Runtime/cmake/SwiftCore";
              # FIXME(compnerd) this currently causes a build failure on Windows, but
              # this should be enabled when building the dynamic runtime.
              'SwiftStringProcessing_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build String Processing
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/StringProcessing
      - name: Install String Processing
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/StringProcessing --target install

      # build.ps1: Build-SDK
      - name: Configure Synchronization
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Synchronization
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Supplemental/Synchronization
          bin-dir: ${{ github.workspace }}/BinaryCache/Synchronization
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/Runtime/cmake/SwiftCore";
              'SwiftOverlay_DIR' = "${{ github.workspace }}/BinaryCache/Overlay/cmake/SwiftOverlay";
              # FIXME(compnerd) this currently causes a build failure on Windows, but
              # this should be enabled when building the dynamic runtime.
              'SwiftSynchronization_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build Synchronization
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Synchronization
      - name: Install Synchronization
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Synchronization --target install

      # build.ps1: Build-SDK
      - name: Configure Distributed
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Distributed
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Supplemental/Distributed 
          bin-dir: ${{ github.workspace }}/BinaryCache/Distributed
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
              'CMAKE_CXX_FLAGS' = "-I${{ github.workspace }}/BinaryCache/Runtime/include";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/Runtime/cmake/SwiftCore";
              'SwiftOverlay_DIR' = "${{ github.workspace }}/BinaryCache/Overlay/cmake/SwiftOverlay";
              # FIXME(compnerd) this currently causes a build failure on Windows, but
              # this should be enabled when building the dynamic runtime.
              'SwiftDistributed_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build Distributed
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Distributed
      - name: Install Distributed
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Distributed --target install

      # build.ps1: Build-SDK
      - name: Configure Observation
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Observation
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Supplemental/Observation
          bin-dir: ${{ github.workspace }}/BinaryCache/Observation
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
               # FIXME(#83449): avoid using `SwiftCMakeConfig.h`
              'CMAKE_CXX_FLAGS' = "-I${{ github.workspace }}/BinaryCache/Runtime/include"
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/Runtime/cmake/SwiftCore";
              'SwiftOverlay_DIR' = "${{ github.workspace }}/BinaryCache/Overlay/cmake/SwiftOverlay";
              # FIXME(compnerd) this currently causes a build failure on Windows, but
              # this should be enabled when building the dynamic runtime.
              'SwiftObservation_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build Observation
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Observation
      - name: Install Observation
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Observation --target install

      # build.ps1: Build-SDK
      - name: Configure Differentiation
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Differentiation
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Supplemental/Differentiation
          bin-dir: ${{ github.workspace }}/BinaryCache/Differentiation
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/Runtime/cmake/SwiftCore";
              'SwiftOverlay_DIR' = "${{ github.workspace }}/BinaryCache/Overlay/cmake/SwiftOverlay";
              # FIXME(compnerd) this currently causes a build failure on Windows, but
              # this should be enabled when building the dynamic runtime.
              'SwiftDifferentiation_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build Differentiation
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Differentiation
      - name: Install Differentiation
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Differentiation --target install

      # build.ps1: Build-SDK
      - name: Configure Volatile
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Volatile
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Supplemental/Volatile
          bin-dir: ${{ github.workspace }}/BinaryCache/Volatile
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
              'CMAKE_FIND_PACKAGE_PREFER_CONFIG' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/Runtime/cmake/SwiftCore";
              'SwiftOverlay_DIR' = "${{ github.workspace }}/BinaryCache/Overlay/cmake/SwiftOverlay";
              # FIXME(compnerd) this currently causes a build failure on Windows, but
              # this should be enabled when building the dynamic runtime.
              'SwiftVolatile_ENABLE_LIBRARY_EVOLUTION' = "NO";
            }
      - name: Build Volatile
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Volatile
      - name: Install Volatile
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Volatile --target install

      # build.ps1: Build-SDK
      - name: Configure RuntimeModule
        if: matrix.os != 'Android' # Disabled for Android https://github.com/swiftlang/swift/issues/87445
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: RuntimeModule
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Supplemental/Runtime
          bin-dir: ${{ github.workspace }}/BinaryCache/RuntimeModule
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
              'CMAKE_FIND_PACKAGE_PREFER_CONFIG' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/Runtime/cmake/SwiftCore";
              'SwiftOverlay_DIR' = "${{ github.workspace }}/BinaryCache/Overlay/cmake/SwiftOverlay";
              'SwiftCxxOverlay_DIR' = "${{ github.workspace }}/BinaryCache/Overlay/Cxx/cmake/SwiftCxxOverlay";
              # FIXME(compnerd) this currently causes a build failure on Windows, but
              # this should be enabled when building the dynamic runtime.
              'SwiftRuntime_ENABLE_LIBRARY_EVOLUTION' = "NO";
              'SwiftRuntime_ENABLE_BACKTRACING' = "YES";
            }
      - name: Build RuntimeModule
        if: matrix.os != 'Android' # Disabled for Android https://github.com/swiftlang/swift/issues/87445
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/RuntimeModule
      - name: Install RuntimeModule
        if: matrix.os != 'Android' # Disabled for Android https://github.com/swiftlang/swift/issues/87445
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/RuntimeModule --target install

      # build.ps1: Build-SDK
      - name: Configure Backtrace
        # NOTE: we only build this if static variants are enabled to ensure that we
        # can statically link the runtime.
        # Disabled for Windows x86 https://github.com/swiftlang/swift/issues/87499
        if: matrix.static && matrix.os == 'Windows' && matrix.arch != 'x86'
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: RuntimeBacktrace
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/Runtimes/Supplemental/StackWalker
          bin-dir: ${{ github.workspace }}/BinaryCache/RuntimeBacktrace
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'SWIFT_PUBLIC_KEY_TOKEN' = "0000000000000000";
              'SWIFT_ASSEMBLY_VERSION' = "${{ inputs.swift_version }}";
              'CMAKE_Swift_FLAGS' = @("-static-stdlib");
              'SwiftCore_DIR' = "${{ github.workspace }}/BinaryCache/Runtime/cmake/SwiftCore";
              'SwiftOverlay_DIR' = "${{ github.workspace }}/BinaryCache/Overlay/cmake/SwiftOverlay";
              'SwiftCxxOverlay_DIR' = "${{ github.workspace }}/BinaryCache/Overlay/Cxx/cmake/SwiftCxxOverlay";
              'SwiftRuntime_DIR' = "${{ github.workspace }}/BinaryCache/RuntimeModule/cmake/SwiftRuntime";
            }
      - name: Build Runtime Backtrace
        # NOTE: we only build this if static variants are enabled to ensure that we
        # can statically link the runtime.
        # Disabled for Windows x86 https://github.com/swiftlang/swift/issues/87499
        if: matrix.static && matrix.os == 'Windows' && matrix.arch != 'x86'
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/RuntimeBacktrace
      - name: Install Runtime Backtrace
        # NOTE: we only build this if static variants are enabled to ensure that we
        # can statically link the runtime.
        # Disabled for Windows x86 https://github.com/swiftlang/swift/issues/87499
        if: matrix.static && matrix.os == 'Windows' && matrix.arch != 'x86'
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/RuntimeBacktrace --target install

      # build.ps1: Build-SDK
      - name: Configure Dispatch
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Dispatch
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch
          bin-dir: ${{ github.workspace }}/BinaryCache/Dispatch
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          swift-sdk-path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          cmake-defines: |
            @{
               'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
               'BUILD_TESTING' = "NO";
               'CMAKE_FIND_PACKAGE_PREFER_CONFIG' = "YES";
               'CMAKE_Swift_FLAGS' = ${{ matrix.static && '@("-static-stdlib", "-Xfrontend", "-use-static-resource-dir")' || '@()' }};
               'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
               'ENABLE_SWIFT' = "YES";
               'dispatch_INSTALL_ARCH_SUBDIR' = "YES";
              }
      - name: Build Dispatch
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Dispatch
      - name: Install Dispatch
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Dispatch --target install

      # build.ps1: Build-SDK
      - name: Configure Foundation
        if: matrix.os != 'Android' || inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Foundation
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-corelibs-foundation
          bin-dir: ${{ github.workspace }}/BinaryCache/Foundation
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          c-compiler: 'Stage1.GNUC'
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          swift-sdk-path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "${{ matrix.static && 'NO' || 'YES' }}";
              'CMAKE_NINJA_FORCE_RESPONSE_FILE' = "YES";
              'CMAKE_Swift_FLAGS' = ${{ matrix.static && '@("-static-stdlib", "-Xfrontend", "-use-static-resource-dir")' || '@()' }};
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'ENABLE_TESTING' = "NO";
              'FOUNDATION_BUILD_TOOLS' = "${{ matrix.static && matrix.os == 'Windows' && 'YES' || 'NO' }}";
              'CURL_DIR' = "${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr/lib/cmake/CURL";
              'BROTLI_INCLUDE_DIR' = "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr/include";
              'BROTLICOMMON_LIBRARY' = "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr/lib/$(if ("${{ matrix.os }}" -eq "Windows") { "brotlicommon.lib" } else { "libbrotlicommon.a" })";
              'BROTLIDEC_LIBRARY' = "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr/lib/$(if ("${{ matrix.os }}" -eq "Windows") { "brotlidec.lib" } else { "libbrotlidec.a" })";
              'LibXml2_DIR' = "${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr/lib/cmake/libxml2-${{ inputs.libxml2_version }}";
              'ZLIB_INCLUDE_DIR' = "${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr/include";
              'ZLIB_LIBRARY' = "${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr/lib/${{ matrix.os == 'Windows' && 'zlibstatic.lib' || 'libz.a' }}";
              'dispatch_DIR' = "${{ github.workspace }}/BinaryCache/Dispatch/cmake/modules";
              '_SwiftFoundation_SourceDIR' = "${{ github.workspace }}/SourceCache/swift-foundation";
              '_SwiftFoundationICU_SourceDIR' = "${{ github.workspace }}/SourceCache/swift-foundation-icu";
              '_SwiftCollections_SourceDIR' = "${{ github.workspace }}/SourceCache/swift-collections";
              'SwiftFoundation_MACRO' = "${{ github.workspace }}/BinaryCache/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/bin";
            }
      - name: Build Foundation
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Foundation
      - name: Install Foundation
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          cmake --build ${{ github.workspace }}/BinaryCache/Foundation --target install

      - name: Repair SDK Headers
        run: |
          $SDKRoot = cygpath -w "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk"
          foreach ($Module in ("Block", "dispatch", "os", "_foundation_unicode", "_FoundationCShims", "_FoundationInternationalizationData")) {
            foreach ($ResourceType in ("swift", "swift_static")) {
              $ModuleDirectory = "$SDKRoot\usr\lib\$ResourceType\$Module"
              if (Test-Path $ModuleDirectory) {
                New-Item -ItemType Directory -Force "$SDKRoot\usr\include" | Out-Null
                $Destination = "$SDKRoot\usr\include\$Module"
                if (Test-Path $Destination) { Remove-Item -Recurse -Force $Destination }
                Move-Item -Path $ModuleDirectory -Destination $Destination -Force
              }
            }
          }

      - name: Repair Dispatch VFS Overlay
        run: |
          $DispatchVFS = "${{ github.workspace }}/BinaryCache/Dispatch/dispatch-vfs-overlay.yaml"
          if (Test-Path $DispatchVFS) {
            $DispatchSrc = "${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch/dispatch".Replace('\', '/')
            $InstalledMap = "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr/include/dispatch/module.modulemap".Replace('\', '/')
            Set-Content -Path $DispatchVFS -Encoding UTF8 -Value @(
              '---',
              'version: 0',
              'case-sensitive: false',
              'use-external-names: false',
              'roots:',
              "  - name: '$DispatchSrc'",
              '    type: directory',
              '    contents:',
              '      - name: module.modulemap',
              '        type: file',
              "        external-contents: '$InstalledMap'"
            )
          }

      - uses: actions/setup-python@v5
      - uses: jannekem/run-python-script-action@v1
        if: matrix.os != 'Android' || inputs.build_android
        with:
          script: |
            import os
            import plistlib

            info_plist = r'${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Info.plist'
            with open(os.path.normpath(info_plist), 'wb') as plist:
              # TODO(compnerd) derive this from the install directory
              plistlib.dump({ 'DefaultProperties': { 'XCTEST_VERSION': '${{ inputs.swift_version }}', 'SWIFT_TESTING_VERSION': '${{ inputs.swift_version }}', 'SWIFTC_FLAGS': ['-use-ld=lld'] } }, plist)

      - name: Write SDK Settings
        if: matrix.os != 'Android' || inputs.build_android
        run: |
          $SDKSettings = @{
            CanonicalName = "${{ matrix.os }}".ToLower()
            DisplayName = "${{ matrix.os }}"
            IsBaseSDK = "YES"
            Version = "${{ inputs.swift_version }}"
            VersionMap = @{}
            HeaderSearchPaths = @( "usr/include" )
            LibrarySearchPaths = @()
            DefaultProperties = @{
              PLATFORM_NAME = "${{ matrix.os }}"
            }
            SupportedTargets = @{
              "${{ matrix.os }}".ToLower() = @{
                PlatformDisplayName = "${{ matrix.os }}"
                PlatformFamilyName = "${{ matrix.os }}"
                Archs = @("$(("${{ matrix.triple }}" -split '-')[0])")
              }
            }
          }
          switch ("${{ matrix.os }}") {
            "Windows" {
              $SDKSettings.DefaultProperties.DEFAULT_USE_RUNTIME = "MD"
              $SDKSettings.SupportedTargets.windows.LLVMTargetVendor = "unknown"
              $SDKSettings.SupportedTargets.windows.LLVMTargetSys = "windows"
              $SDKSettings.SupportedTargets.windows.LLVMTargetTripleEnvironment = "msvc"
            }
            "Android" {
              $SDKSettings.SupportedTargets.android.LLVMTargetVendor = "unknown"
              $SDKSettings.SupportedTargets.android.LLVMTargetSys = "linux"
              $SDKSettings.SupportedTargets.android.LLVMTargetTripleEnvironment = "android${{ inputs.ANDROID_API_LEVEL }}"
            }
            default {
              throw "Unsupported OS: ${{ matrix.os }}"
            }
          }
          New-Item -ItemType Directory -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk" -Force | Out-Null
          $SDKSettings | ConvertTo-JSON -Depth 4 | Out-File -FilePath "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/SDKSettings.json"

          $PListFile = "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/SDKSettings.plist"
          python3 -c "import plistlib; print(str(plistlib.dumps($(($SDKSettings | ConvertTo-JSON -Compress -Depth 4) -replace '"', "'")), encoding='utf-8'))" | Out-File -FilePath $PListFile -Encoding utf8

      # build.ps1: Build-SDK -> Build-Testing (shared SDK only).
      - name: Configure Testing
        if: ${{ !matrix.static && (matrix.os != 'Android' || inputs.build_android) }}
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Testing
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-testing
          bin-dir: ${{ github.workspace }}/BinaryCache/Testing
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/Library/Testing-${{ inputs.swift_version }}/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          cxx-compiler: 'Stage1.GNUCXX'
          swift-compiler: 'Stage1.Swift'
          swift-sdk-path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_INSTALL_BINDIR' = "${{ (matrix.arch == 'amd64' || matrix.arch == 'x86_64') && 'bin64' || matrix.arch == 'arm64' && 'bin64a' || matrix.arch == 'armv7' && 'bin32a' || 'bin32' }}";
              'CMAKE_Swift_FLAGS' = ${{ (matrix.os == 'Android') && format('@("-I{0}/BuildRoot/Library/Developer/Platforms/{1}.platform/Developer/SDKs/{1}.sdk/usr/include")', github.workspace, matrix.os) || '@()' }};
              'SwiftTesting_MACRO' = "${{ github.workspace }}/BinaryCache/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr/bin/TestingMacros.dll";
              'SwiftTesting_INSTALL_NESTED_SUBDIR' = "YES";
            }
      - name: Build Testing
        if: ${{ !matrix.static && (matrix.os != 'Android' || inputs.build_android) }}
        run: cmake --build ${{ github.workspace }}/BinaryCache/Testing
      - name: Install Testing
        if: ${{ !matrix.static && (matrix.os != 'Android' || inputs.build_android) }}
        run: cmake --build ${{ github.workspace }}/BinaryCache/Testing --target install

      # build.ps1: Build-SDK -> Build-XCTest (shared SDK only).
      # TODO(compnerd) correctly version XCTest
      - name: Configure XCTest
        if: ${{ !matrix.static && (matrix.os != 'Android' || inputs.build_android) }}
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: XCTest
          swift-version: ${{ inputs.swift_version }}
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-corelibs-xctest
          bin-dir: ${{ github.workspace }}/BinaryCache/XCTest
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/Library/XCTest-${{ inputs.swift_version }}/usr
          android-api-level: ${{ inputs.ANDROID_API_LEVEL }}
          android-clang-version: ${{ inputs.ANDROID_CLANG_VERSION }}
          ndk-path: ${{ steps.setup-ndk.outputs.ndk-path }}
          swift-compiler: 'Stage1.Swift'
          swift-sdk-path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_INSTALL_BINDIR' = "${{ (matrix.arch == 'amd64' || matrix.arch == 'x86_64') && 'bin64' || matrix.arch == 'arm64' && 'bin64a' || matrix.arch == 'armv7' && 'bin32a' || 'bin32' }}";
              'CMAKE_Swift_FLAGS' = ${{ (matrix.os == 'Android') && format('@("-I{0}/BuildRoot/Library/Developer/Platforms/{1}.platform/Developer/SDKs/{1}.sdk/usr/include")', github.workspace, matrix.os) || '@()' }};
              'ENABLE_TESTING' = "NO";
              'XCTest_INSTALL_NESTED_SUBDIR' = "YES";
              'SwiftTesting_DIR' = "${{ github.workspace }}/BinaryCache/Testing/cmake/modules";
            }
      - name: Build XCTest
        if: ${{ !matrix.static && (matrix.os != 'Android' || inputs.build_android) }}
        run: cmake --build ${{ github.workspace }}/BinaryCache/XCTest
      - name: Install XCTest
        if: ${{ !matrix.static && (matrix.os != 'Android' || inputs.build_android) }}
        run: cmake --build ${{ github.workspace }}/BinaryCache/XCTest --target install

      - uses: actions/upload-artifact@v4
        if: matrix.os != 'Android' || inputs.build_android
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-${{ matrix.static && 'static' || 'shared' }}-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/${{ matrix.os }}.platform

      - name: Upload PDBs to Azure
        uses: thebrowsercompany/action-publish-symbols@d5f4b06266463bf9a71ef6a4e3b0b8d79eaa82a4
        if: ${{ inputs.debug_info && matrix.os == 'Windows' }}
        with:
          accountName: ${{ vars.SYMBOL_SERVER_ACCOUNT }}
          personalAccessToken: ${{ secrets.SYMBOL_SERVER_PAT }}
          symbolsFolder: ${{ github.workspace }}/BinaryCache
          searchPattern: '**/*.pdb'

      - name: Upload PDBs to Public Symbol Server
        uses: ./SourceCache/ci-build/.github/actions/publish-symbols-r2
        if: ${{ inputs.debug_info && matrix.os == 'Windows' }}
        with:
          symbolsFolder: ${{ github.workspace }}/BinaryCache
          searchPattern: '*.pdb'
          swift-version: ${{ inputs.swift_version }}
          arch: ${{ matrix.arch }}
          variant: ${{ matrix.static && 'static' || 'shared' }}
          build-revision: ${{ inputs.swift_build_revision }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID }}

      - name: Upload DLLs to Azure
        uses: thebrowsercompany/action-publish-symbols@d5f4b06266463bf9a71ef6a4e3b0b8d79eaa82a4
        if: ${{ inputs.debug_info && matrix.os == 'Windows' }}
        with:
          accountName: ${{ vars.SYMBOL_SERVER_ACCOUNT }}
          personalAccessToken: ${{ secrets.SYMBOL_SERVER_PAT }}
          symbolsFolder: ${{ github.workspace }}/BinaryCache
          searchPattern: '**/*.dll'

  devtools:
    if: inputs.build_os == 'Windows'
    needs: [sqlite, stage2-compiler, macros, sdk]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.host_matrix) }}

    name: Windows ${{ matrix.arch }} Developer Tools

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Compute workspace hash
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-devtools
      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-sqlite-${{ inputs.swift_toolchain_sqlite_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library
      - name: Download Target SDK
        uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-shared-sdk
          path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/Windows.platform
      - name: Download Host SDK
        uses: actions/download-artifact@v4
        if: matrix.arch != inputs.build_arch
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-shared-sdk
          path: ${{ github.workspace }}/BinaryCache/HostSDK/Library/Developer/Platforms/Windows.platform
      - name: Download Host Macros
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-macros
          path: ${{ github.workspace }}/BinaryCache/stage1/Library
      - name: Download Stage2 SwiftSyntax
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-Asserts-swift-syntax
          path: ${{ github.workspace }}/BinaryCache/swift-syntax
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Windows-${{ matrix.arch }}-cmark-gfm-${{ inputs.swift_cmark_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/indexstore-db
          ref: ${{ inputs.indexstore_db_revision }}
          path: ${{ github.workspace }}/SourceCache/indexstore-db
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/sourcekit-lsp
          ref: ${{ inputs.sourcekit_lsp_revision }}
          path: ${{ github.workspace }}/SourceCache/sourcekit-lsp
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-argument-parser
          ref: ${{ inputs.swift_argument_parser_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-argument-parser
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-asn1
          ref: ${{ inputs.swift_asn1_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-asn1
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-certificates
          ref: ${{ inputs.swift_certificates_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-certificates
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-collections
          ref: ${{ inputs.swift_collections_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-collections
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-crypto
          ref: ${{ inputs.swift_crypto_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-crypto
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-docc-render-artifact
          ref: ${{ inputs.swift_docc_render_artifact_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-docc-render-artifact
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-docc
          ref: ${{ inputs.swift_docc_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-docc
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-docc-symbolkit
          ref: ${{ inputs.swift_docc_symbolkit_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-docc-symbolkit
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-driver
          ref: ${{ inputs.swift_driver_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-driver
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-format
          ref: ${{ inputs.swift_format_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-format
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-llbuild
          ref: ${{ inputs.swift_llbuild_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-llbuild
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-lmdb
          ref: ${{ inputs.swift_lmdb_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-lmdb
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-markdown
          ref: ${{ inputs.swift_markdown_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-markdown
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-tools-protocols
          ref: ${{ inputs.swift_tools_protocols_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-tools-protocols
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-build
          ref: ${{ inputs.swift_build_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-build
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-package-manager
          ref: ${{ inputs.swift_package_manager_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-package-manager
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-system
          ref: ${{ inputs.swift_system_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-system
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-subprocess
          ref: ${{ inputs.swift_subprocess_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-subprocess
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-tools-support-core
          ref: ${{ inputs.swift_tools_support_core_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-tools-support-core
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift
          ref: ${{ inputs.swift_revision }}
          path: ${{ github.workspace }}/SourceCache/swift
          show-progress: false

      - name: Add Host SDK runtime to PATH
        run: |
          $RuntimeLibrary = if ("${{ matrix.arch }}" -eq "${{ inputs.build_arch }}") {
            "${{ github.workspace }}/BinaryCache/Library"
          } else {
            "${{ github.workspace }}/BinaryCache/HostSDK/Library"
          }
          $RTLPath = cygpath -w "$RuntimeLibrary/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/bin"
          $RTLPath | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

          $SDKRoot = cygpath -w ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk
          echo "SDKROOT=${SDKRoot}" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      # build.ps1: Build-ArgumentParser
      - name: Configure swift-argument-parser
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-argument-parser
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-argument-parser
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-argument-parser
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'BUILD_TESTING' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
            }
      - name: Build swift-argument-parser
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-argument-parser

      # build.ps1: Build-Collections
      - name: Configure swift-collections
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-collections
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-collections
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-collections
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
            }
      - name: Build swift-collections
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-collections

      # build.ps1: Build-ASN1
      - name: Configure swift-asn1
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-asn1
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-asn1
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-asn1
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
            }
      - name: Build swift-asn1
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-asn1

      # build.ps1: Build-Crypto
      - name: Configure swift-crypto
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-crypto
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-crypto
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-crypto
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          assembler: 'Stage1'
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftASN1_DIR' = "${{ github.workspace }}/BinaryCache/swift-asn1/cmake/modules";
            }
      - name: Build swift-crypto
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-crypto

      # build.ps1: Build-LLBuild
      - name: Configure swift-llbuild
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-llbuild
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-llbuild
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-llbuild
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'LLBUILD_SUPPORT_BINDINGS' = "Swift";
              'SQLite3_INCLUDE_DIR' = "${{ github.workspace }}/BinaryCache/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/include";
              'SQLite3_LIBRARY' = "${{ github.workspace }}/BinaryCache/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/lib/SQLite3.lib";
            }  
      - name: Build swift-llbuild
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-llbuild

      # build.ps1: Build-System
      - name: Configure swift-system
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-system
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-system
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-system
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
            }
      - name: Build swift-system
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-system

      # build.ps1: Build-Subprocess
      - name: Configure swift-subprocess
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-subprocess
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-subprocess
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-subprocess
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftSystem_DIR' = "${{ github.workspace }}/BinaryCache/swift-system/cmake/modules";
            }
      - name: Build swift-subprocess
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-subprocess

      # build.ps1: Build-ToolsSupportCore
      - name: Configure swift-tools-support-core
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-tools-support-core
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-tools-support-core
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-tools-support-core
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'Foundation_DIR' = "${{ github.workspace }}/BinaryCache/foundation/cmake/modules";
              'XCTest_DIR' = "${{ github.workspace }}/BinaryCache/xctest/cmake/modules";
            }
      - name: Build swift-tools-support-core
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-tools-support-core

      # build.ps1: Build-Driver
      - name: Configure swift-driver
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-driver
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-driver
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-driver
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'TSC_DIR'="${{ github.workspace }}/BinaryCache/swift-tools-support-core/cmake/modules";
              'LLBuild_DIR'="${{ github.workspace }}/BinaryCache/swift-llbuild/cmake/modules";
              'ArgumentParser_DIR'="${{ github.workspace }}/BinaryCache/swift-argument-parser/cmake/modules";
              'SQLite3_INCLUDE_DIR'="${{ github.workspace }}/BinaryCache/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/include";
              'SQLite3_LIBRARY'="${{ github.workspace }}/BinaryCache/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/lib/SQLite3.lib";
              'SWIFT_DRIVER_BUILD_TOOLS' = "NO";
            }
      - name: Build swift-driver
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-driver

      # build.ps1: Build-Certificates
      - name: Configure swift-certificates
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-certificates
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-certificates
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-certificates
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftCrypto_DIR'="${{ github.workspace }}/BinaryCache/swift-crypto/cmake/modules";
              'SwiftASN1_DIR'="${{ github.workspace }}/BinaryCache/swift-asn1/cmake/modules";
            }
      - name: Build swift-certificates
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-certificates

      # build.ps1: Build-ToolsProtocols
      - name: Configure swift-tools-protocols
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-tools-protocols
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-tools-protocols
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-tools-protocols
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
            }
      - name: Build swift-tools-protocols
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-tools-protocols

      # build.ps1: Build-Build
      - name: Configure swift-build
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-build
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-build
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-build
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'ArgumentParser_DIR' = "${{ github.workspace }}/BinaryCache/swift-argument-parser/cmake/modules";
              'LLBuild_DIR' = "${{ github.workspace }}/BinaryCache/swift-llbuild/cmake/modules";
              'SwiftDriver_DIR' = "${{ github.workspace }}/BinaryCache/swift-driver/cmake/modules";
              'SwiftSystem_DIR' = "${{ github.workspace }}/BinaryCache/swift-system/cmake/modules";
              'SwiftSubprocess_DIR' = "${{ github.workspace }}/BinaryCache/swift-subprocess/cmake/modules";
              'TSC_DIR' = "${{ github.workspace }}/BinaryCache/swift-tools-support-core/cmake/modules";
              'SwiftToolsProtocols_DIR' = "${{ github.workspace }}/BinaryCache/swift-tools-protocols/cmake/modules";
              'SQLite3_INCLUDE_DIR' = "${{ github.workspace }}/BinaryCache/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/include";
              'SQLite3_LIBRARY' = "${{ github.workspace }}/BinaryCache/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/lib/SQLite3.lib";
              'CMAKE_Swift_FLAGS' = "${{ matrix.arch == 'arm64' && '-use-ld=lld-link' || '' }}";
            }
      - name: Build swift-build
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-build

      - name: Relocate Stage2 SwiftSyntax
        run: |
          $Module = "${{ github.workspace }}/BinaryCache/swift-syntax/cmake/modules/SwiftSyntaxConfig.cmake"
          $BinaryDir = cygpath -m ${{ github.workspace }}/BinaryCache/swift-syntax
          (Get-Content $Module).Replace('<BINARY_DIR>', $BinaryDir) | Set-Content $Module

      # build.ps1: Build-PackageManager
      - name: Configure swift-package-manager
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-package-manager
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-package-manager
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-package-manager
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_Swift_FLAGS' = @("-DCRYPTO_v2");
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftSystem_DIR' = "${{ github.workspace }}/BinaryCache/swift-system/cmake/modules";
              'SwiftSubprocess_DIR' = "${{ github.workspace }}/BinaryCache/swift-subprocess/cmake/modules";
              'TSC_DIR' = "${{ github.workspace }}/BinaryCache/swift-tools-support-core/cmake/modules";
              'LLBuild_DIR' = "${{ github.workspace }}/BinaryCache/swift-llbuild/cmake/modules";
              'ArgumentParser_DIR' = "${{ github.workspace }}/BinaryCache/swift-argument-parser/cmake/modules";
              'SwiftDriver_DIR' = "${{ github.workspace }}/BinaryCache/swift-driver/cmake/modules";
              'SwiftBuild_DIR' = "${{ github.workspace }}/BinaryCache/swift-build/cmake/modules";
              'SwiftToolsProtocols_DIR' = "${{ github.workspace }}/BinaryCache/swift-tools-protocols/cmake/modules";
              'SwiftCrypto_DIR' = "${{ github.workspace }}/BinaryCache/swift-crypto/cmake/modules";
              'SwiftCollections_DIR' = "${{ github.workspace }}/BinaryCache/swift-collections/cmake/modules";
              'SwiftASN1_DIR' = "${{ github.workspace }}/BinaryCache/swift-asn1/cmake/modules";
              'SwiftCertificates_DIR' = "${{ github.workspace }}/BinaryCache/swift-certificates/cmake/modules";
              'SwiftSyntax_DIR' = "${{ github.workspace }}/BinaryCache/swift-syntax/cmake/modules"
              'SQLite3_INCLUDE_DIR' = "${{ github.workspace }}/BinaryCache/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/include";
              'SQLite3_LIBRARY' = "${{ github.workspace }}/BinaryCache/Library/sqlite-${{ inputs.swift_toolchain_sqlite_version }}/usr/lib/SQLite3.lib";
            }
      - name: Build swift-package-manager
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-package-manager

      # build.ps1: Build-Markdown
      - name: Configure Markdown
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-markdown
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-markdown
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-markdown
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'ArgumentParser_DIR' = "${{ github.workspace }}/BinaryCache/swift-argument-parser/cmake/modules";
              'cmark-gfm_DIR' = "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/lib/cmake";
            }
      - name: Build Markdown
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-markdown

      # build.ps1: Build-Format
      - name: Configure swift-format
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-format
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-format
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-format
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'ArgumentParser_DIR' = "${{ github.workspace }}/BinaryCache/swift-argument-parser/cmake/modules";
              'SwiftSyntax_DIR' = "${{ github.workspace }}/BinaryCache/swift-syntax/cmake/modules";
              'SwiftMarkdown_DIR' = "${{ github.workspace }}/BinaryCache/swift-markdown/cmake/modules";
              'cmark-gfm_DIR' = "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/lib/cmake";
              'CMAKE_C_FLAGS' = "${{ !inputs.use_host_toolchain && '-fuse-ld=lld' || '' }}";
            }
      - name: Build swift-format
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-format

      # build.ps1: Build-LMDB
      - name: Configure swift-lmdb
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-lmdb
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-lmdb
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-lmdb
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          cmake-defines: |
            @{
              'CMAKE_C_FLAGS' = "${{ !inputs.use_host_toolchain && '-fuse-ld=lld' || '' }}";
            }
      - name: Build swift-lmdb
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-lmdb

      # build.ps1: Build-IndexStoreDB
      - name: Configure indexstore-db
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: indexstore-db
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/indexstore-db
          bin-dir: ${{ github.workspace }}/BinaryCache/indexstore-db
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'CMAKE_C_FLAGS' =  @(
                "-I${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr/include",
                "-I${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr/include/Block");
              'CMAKE_CXX_FLAGS' = @(
                "-I${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr/include",
                "-I${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr/include/Block");
              'LMDB_DIR' = "${{ github.workspace }}/BinaryCache/swift-lmdb/cmake/modules";
            }
      - name: Build indexstore-db
        run: cmake --build ${{ github.workspace }}/BinaryCache/indexstore-db

      # build.ps1: Build-SourceKitLSP
      - name: Configure sourcekit-lsp
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: sourcekit-lsp
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/sourcekit-lsp
          bin-dir: ${{ github.workspace }}/BinaryCache/sourcekit-lsp
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'SwiftSyntax_DIR' = "${{ github.workspace }}/BinaryCache/swift-syntax/cmake/modules";
              'TSC_DIR' = "${{ github.workspace }}/BinaryCache/swift-tools-support-core/cmake/modules";
              'LLBuild_DIR' = "${{ github.workspace }}/BinaryCache/swift-llbuild/cmake/modules";
              'ArgumentParser_DIR' = "${{ github.workspace }}/BinaryCache/swift-argument-parser/cmake/modules";
              'SwiftASN1_DIR' = "${{ github.workspace }}/BinaryCache/swift-asn1/cmake/modules";
              'SwiftCrypto_DIR' = "${{ github.workspace }}/BinaryCache/swift-crypto/cmake/modules";
              'SwiftCollections_DIR' = "${{ github.workspace }}/BinaryCache/swift-collections/cmake/modules";
              'SwiftBuild_DIR' = "${{ github.workspace }}/BinaryCache/swift-build/cmake/modules";
              'SwiftPM_DIR' = "${{ github.workspace }}/BinaryCache/swift-package-manager/cmake/modules";
              'LMDB_DIR' = "${{ github.workspace }}/BinaryCache/swift-lmdb/cmake/modules";
              'IndexStoreDB_DIR' = "${{ github.workspace }}/BinaryCache/indexstore-db/cmake/modules";
              'SwiftToolsProtocols_DIR' = "${{ github.workspace }}/BinaryCache/swift-tools-protocols/cmake/modules";
            }
      - name: Build sourcekit-lsp
        run: cmake --build ${{ github.workspace }}/BinaryCache/sourcekit-lsp

      # build.ps1: built with SPM as part of swift-docc.
      # These is no CI for DocC in swift.org, which is why it is not built with CMake in build.ps1.
      - name: Configure swift-docc-symbolkit
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-docc-symbolkit
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-docc-symbolkit
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-docc-symbolkit
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "NO";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
            }
      - name: Build swift-docc-symbolkit
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-docc-symbolkit

      # build.ps1: built with SPM.
      # These is no CI for DocC in swift.org, which is why it is not built with CMake in build.ps1.
      - name: Configure swift-docc
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-docc
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift-docc
          bin-dir: ${{ github.workspace }}/BinaryCache/swift-docc
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'BUILD_SHARED_LIBS' = "YES";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'ArgumentParser_DIR' = "${{ github.workspace }}/BinaryCache/swift-argument-parser/cmake/modules";
              'SwiftASN1_DIR' = "${{ github.workspace }}/BinaryCache/swift-asn1/cmake/modules";
              'SwiftCrypto_DIR' = "${{ github.workspace }}/BinaryCache/swift-crypto/cmake/modules";
              'SwiftMarkdown_DIR' = "${{ github.workspace }}/BinaryCache/swift-markdown/cmake/modules";
              'LMDB_DIR' = "${{ github.workspace }}/BinaryCache/swift-lmdb/cmake/modules";
              'IndexStoreDB_DIR' = "${{ github.workspace }}/BinaryCache/indexstore-db/cmake/modules";
              'SymbolKit_DIR' = "${{ github.workspace }}/BinaryCache/swift-docc-symbolkit/cmake/modules";
              'cmark-gfm_DIR' = "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/lib/cmake";
            }
      - name: Build swift-docc
        #TODO: temporary until DocC has its own CI: https://github.com/compnerd/swift-build/issues/1086
        id: build-swift-docc
        continue-on-error: true
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-docc

      - name: Install swift-argument-parser
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-argument-parser --target install
      - name: Install swift-collections
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-collections --target install
      - name: Install swift-llbuild
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-llbuild --target install
      - name: Install swift-system
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-system --target install
      - name: Install swift-tools-support-core
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-tools-support-core --target install
      - name: Install swift-driver
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-driver --target install
      - name: Install swift-tools-protocols
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-tools-protocols --target install
      - name: Install swift-build
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-build --target install
      - name: Install swift-subprocess
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-subprocess --target install
      - name: Install swift-package-manager
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-package-manager --target install
      - name: Install SourceKit-LSP
        run: cmake --build ${{ github.workspace }}/BinaryCache/sourcekit-lsp --target install
      - name: Install swift-format
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-format --target install
      - name: Install swift-docc
        #TODO: temporary until DocC has its own CI: https://github.com/compnerd/swift-build/issues/1086
        continue-on-error: true
        if: steps.build-swift-docc.outcome == 'success'
        run: cmake --build ${{ github.workspace }}/BinaryCache/swift-docc --target install

      - name: Copy build outputs to the NoAsserts layout
        run: |
          $AssertToolchainRoot = "${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts"
          $NoAssertToolchainRoot = "${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+NoAsserts"
          # Use robocopy for robust recursive copying with optimizations
          #   /E : Copy subdirectories, including empty ones
          #   /XC: Exclude existing files with same timestamp but different file sizes
          #   /XN: Exclude newer destination files
          #   /XO: Exclude older destination files
          #   /NFL: Do not list files
          #   /NDL: Do not list directories
          #   /NJH: No job header
          #   /NC: No class
          #   /NS: No size
          #   /NP: No progress
          $exitCode = &robocopy $AssertToolchainRoot $NoAssertToolchainRoot /E /XC /XN /XO /NS /NC /NFL /NDL /NJH
          if ($LastExitCode -gt 8) {
            Write-Error "robocopy failed with exit code $LastExitCode"
            exit $LastExitCode
          }
          exit 0

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-devtools
          path: ${{ github.workspace }}/BuildRoot/Library

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-swift-argument-parser
          path: ${{ github.workspace }}/BinaryCache/swift-argument-parser

      - name: Upload PDBs to Azure
        uses: thebrowsercompany/action-publish-symbols@d5f4b06266463bf9a71ef6a4e3b0b8d79eaa82a4
        if: ${{ inputs.debug_info }}
        with:
          accountName: ${{ vars.SYMBOL_SERVER_ACCOUNT }}
          personalAccessToken: ${{ secrets.SYMBOL_SERVER_PAT }}
          symbolsFolder: ${{ github.workspace }}/BinaryCache
          searchPattern: '**/*.pdb'

      - name: Upload PDBs to Public Symbol Server
        uses: ./SourceCache/ci-build/.github/actions/publish-symbols-r2
        if: ${{ inputs.debug_info }}
        with:
          symbolsFolder: ${{ github.workspace }}/BinaryCache
          searchPattern: '*.pdb'
          swift-version: ${{ inputs.swift_version }}
          arch: ${{ matrix.arch }}
          variant: 'devtools'
          build-revision: ${{ inputs.swift_build_revision }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID }}

      - name: Upload DLLs to Azure
        uses: thebrowsercompany/action-publish-symbols@d5f4b06266463bf9a71ef6a4e3b0b8d79eaa82a4
        if: ${{ inputs.debug_info }}
        with:
          accountName: ${{ vars.SYMBOL_SERVER_ACCOUNT }}
          personalAccessToken: ${{ secrets.SYMBOL_SERVER_PAT }}
          symbolsFolder: ${{ github.workspace }}/BinaryCache
          searchPattern: '**/*.dll'

      - name: Upload EXEs to Azure
        uses: thebrowsercompany/action-publish-symbols@d5f4b06266463bf9a71ef6a4e3b0b8d79eaa82a4
        if: ${{ inputs.debug_info }}
        with:
          accountName: ${{ vars.SYMBOL_SERVER_ACCOUNT }}
          personalAccessToken: ${{ secrets.SYMBOL_SERVER_PAT }}
          symbolsFolder: ${{ github.workspace }}/BinaryCache
          searchPattern: '**/*.exe'

  debugging_tools:
    if: inputs.build_os == 'Windows'
    needs: [stage1-compiler, devtools, sdk]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.host_matrix) }}

    name: Windows ${{ matrix.arch }} Debugging Tools

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Compute workspace hash
        id: workspace_hash
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 100M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-debugging-tools
      - name: Download swift-argument-parser
        uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-swift-argument-parser
          path: ${{ github.workspace }}/BinaryCache/swift-argument-parser

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library
      - name: Download Target SDK
        uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-shared-sdk
          path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/Windows.platform
      - name: Download Host SDK
        uses: actions/download-artifact@v4
        if: matrix.arch != inputs.build_arch
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-shared-sdk
          path: ${{ github.workspace }}/BinaryCache/HostSDK/Library/Developer/Platforms/Windows.platform

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift
          ref: ${{ inputs.swift_revision }}
          path: ${{ github.workspace }}/SourceCache/swift
          show-progress: false

      - name: Add Host SDK runtime to PATH
        run: |
          $RuntimeLibrary = if ("${{ matrix.arch }}" -eq "${{ inputs.build_arch }}") {
            "${{ github.workspace }}/BinaryCache/Library"
          } else {
            "${{ github.workspace }}/BinaryCache/HostSDK/Library"
          }
          $RTLPath = cygpath -w "$RuntimeLibrary/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/bin"
          $RTLPath | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

          $SDKRoot = cygpath -w "${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk"
          echo "SDKROOT=${SDKRoot}" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      # build.ps1: Build-Inspect
      - name: Configure swift-inspect
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: swift-inspect
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/swift/tools/swift-inspect
          bin-dir: ${{ github.workspace }}/BinaryCache/${{ matrix.arch }}/swift-inspect
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          swift-sdk-path: ${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cmake-defines: |
            @{
              'CMAKE_Swift_FLAGS' =  @(
                "-Xcc", "-I${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr/include",
                "-Xcc", "-I${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr/lib/swift",
                "-Xcc", "-I${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr/include/swift/SwiftRemoteMirror",
                "-L${{ github.workspace }}/BinaryCache/Library/Developer/Platforms/${{ matrix.os }}.platform/Developer/SDKs/${{ matrix.os }}.sdk/usr/lib/swift/${{ matrix.os }}/${{ matrix.cpu }}");
              'ArgumentParser_DIR' = "${{ github.workspace }}/BinaryCache/swift-argument-parser/cmake/modules";
            }

      - name: Build swift-inspect
        run: cmake --build ${{ github.workspace }}/BinaryCache/${{ matrix.arch }}/swift-inspect

      - name: Install swift-inspect
        run: cmake --build ${{ github.workspace }}/BinaryCache/${{ matrix.arch }}/swift-inspect --target install

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-debugging_tools
          path: ${{ github.workspace }}/BuildRoot/Library

  package_tools:
    name: Package Tools
    needs: [stage2-compiler, compiler-runtime, macros, debugging_tools, devtools, sdk]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false

      matrix: ${{ fromJSON(inputs.host_matrix) }}

    steps:
      - name: Select signing mode
        id: signing_mode
        if: inputs.signed
        env:
          AZURE_KEY_VAULT_URI: ${{ secrets.AZURE_KEY_VAULT_URI }}
        run: |
          if ($env:AZURE_KEY_VAULT_URI) {
            echo "azure_vault=true" >> $env:GITHUB_OUTPUT
          } else {
            echo "azure_vault=false" >> $env:GITHUB_OUTPUT
          }

      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-docc-render-artifact
          ref: ${{ inputs.swift_docc_render_artifact_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-docc-render-artifact
          show-progress: false

      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}

      - name: Download Debugging Tools
        uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-debugging_tools
          path: ${{ github.workspace }}/BuildRoot/Library

      - name: Download Compilers (Asserts)
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Windows-${{ matrix.arch }}-Asserts-compilers
          path: ${{ github.workspace }}/BuildRoot/Library

      - name: Download Compilers (NoAsserts)
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Windows-${{ matrix.arch }}-NoAsserts-compilers
          path: ${{ github.workspace }}/BuildRoot/Library

      - name: Download Developer Tools
        uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-devtools
          path: ${{ github.workspace }}/BuildRoot/Library

      - name: Download Macros
        uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-macros
          path: ${{ github.workspace }}/BuildRoot/Library

      - name: Download Windows amd64 Compiler Runtime
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Windows-amd64-compiler-runtime
          path: ${{ github.workspace }}/BuildRoot/Library
      - name: Download Windows arm64 Compiler Runtime
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Windows-arm64-compiler-runtime
          path: ${{ github.workspace }}/BuildRoot/Library
      - name: Download Windows x86 Compiler Runtime
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Windows-x86-compiler-runtime
          path: ${{ github.workspace }}/BuildRoot/Library

      - name: Download Android arm64 Compiler Runtime
        if: inputs.build_android
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Android-arm64-compiler-runtime
          path: ${{ github.workspace }}/BuildRoot/Library
      - name: Download Android armv7 Compiler Runtime
        if: inputs.build_android
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Android-armv7-compiler-runtime
          path: ${{ github.workspace }}/BuildRoot/Library
      - name: Download Android i686 Compiler Runtime
        if: inputs.build_android
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Android-i686-compiler-runtime
          path: ${{ github.workspace }}/BuildRoot/Library
      - name: Download Android x86_64 Compiler Runtime
        if: inputs.build_android
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: Android-x86_64-compiler-runtime
          path: ${{ github.workspace }}/BuildRoot/Library
      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-static-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform
      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-shared-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-installer-scripts
          ref: ${{ inputs.swift_installer_scripts_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-installer-scripts
          show-progress: false

      - name: Stage legacy Swift bridging headers
        run: |
          $ToolchainRoot = "${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts"
          $ResourceIncludes = @(
            Get-ChildItem "$ToolchainRoot/usr/lib/clang" -Directory | ForEach-Object {
              Join-Path $_.FullName "include"
            }
            Join-Path $ToolchainRoot "usr/lib/swift/clang/include"
          )
          $SourceInclude = $ResourceIncludes | Where-Object {
            (Test-Path (Join-Path $_ "swift/bridging")) -and
            (Test-Path (Join-Path $_ "swift/bridging.modulemap")) -and
            (Test-Path (Join-Path $_ "module.modulemap"))
          } | Select-Object -First 1
          if (-not $SourceInclude) {
            throw "Swift bridging headers were not found in a Clang resource directory"
          }

          # TODO: Remove after the 6.4 branch is cut and main uses matching installer scripts.
          $DestinationInclude = Join-Path $ToolchainRoot "usr/include"
          New-Item -Path (Join-Path $DestinationInclude "swift") -ItemType Directory -Force | Out-Null
          Copy-Item -LiteralPath (Join-Path $SourceInclude "swift/bridging") -Destination (Join-Path $DestinationInclude "swift/bridging") -Force
          Copy-Item -LiteralPath (Join-Path $SourceInclude "swift/bridging.modulemap") -Destination (Join-Path $DestinationInclude "swift/bridging.modulemap") -Force
          Copy-Item -LiteralPath (Join-Path $SourceInclude "module.modulemap") -Destination (Join-Path $DestinationInclude "module.modulemap") -Force

      - run: |
          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/${{ matrix.cpu }}/" -ItemType Directory -Force | Out-Null

          Get-ChildItem "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/*.lib" -File -ErrorAction Ignore | Move-Item -Force -Destination "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/${{ matrix.cpu }}/"

          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/usr" -ItemType Directory -Force | Out-Null

          Move-Item "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/bin" "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/usr"

          Move-Item "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/libexec" "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/usr"

      - run: |
          $CertificatePath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.b64
          $PFXPath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.pfx
          Set-Content -Path $CertificatePath -Value '${{ secrets.CERTIFICATE }}'
          certutil.exe -decode $CertificatePath $PFXPath
          Write-Output CERTIFICATE=$PFXPath | Out-File -FilePath ${env:GITHUB_ENV} -Encoding utf8 -Append
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'false'

      - name: Install WixToolset.Sdk
        run: |
          if ((Get-Package -Name WixToolset.Sdk -ErrorAction SilentlyContinue) -eq $null) {
            Install-Package -Name WixToolset.Sdk -RequiredVersion 4.0.1 -Force
          }

      - run: |
          $CertificatePath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.b64
          $PFXPath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.pfx
          Set-Content -Path $CertificatePath -Value '${{ secrets.CERTIFICATE }}'
          certutil -decode $CertificatePath $PFXPath
          Echo CERTIFICATE=$PFXPath | Out-File -FilePath ${env:GITHUB_ENV} -Encoding utf8 -Append
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'false'

      - name: Install WixToolset.Sdk
        run: |
          if ((Get-Package -Name WixToolset.Sdk -ErrorAction SilentlyContinue) -eq $null) {
            Install-Package -Name WixToolset.Sdk -RequiredVersion 4.0.1 -Force
          }

      - name: Complete NoAsserts layout
        run: |
          $AssertToolchainRoot = "${{ github.workspace }}\BuildRoot\Library\Developer\Toolchains\${{ inputs.swift_version }}+Asserts"
          $NoAssertToolchainRoot = "${{ github.workspace }}\BuildRoot\Library\Developer\Toolchains\${{ inputs.swift_version }}+NoAsserts"
          # Only compilers have NoAsserts enabled. Copy the rest of the Toolcahin binaries from the Asserts output
          # Use robocopy for efficient copying
          #   /E : Copies subdirectories, including empty ones.
          #   /XC: Excludes existing files with the same timestamp but different file sizes.
          #   /XN: Excludes existing files that are newer than the copy in the source directory.
          #   /XO: Excludes existing files that are older than the copy in the source directory.
          #   /NFL: Do not list coppied files in output
          #   /NDL: Do not list directories in output
          #   /NJH: Do not write a job header
          #   /NC: Do not write file classes
          #   /NS: Do not write file sizes
          #   /NP: Do not show progress indicator
          # Exit code documentationn for robocopy: https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/robocopy#exit-return-codes
          $exitCode = &robocopy $AssertToolchainRoot $NoAssertToolchainRoot /E /XC /XN /XO /NS /NC /NFL /NDL /NJH
          if ($LastExitCode -gt 8) {
              Write-Error "robocopy failed with exit code $LastExitCode"
              exit $LastExitCode
          }
          exit 0 

      - uses: actions/checkout@v4.2.2
        with:
          repository: microsoft/mimalloc
          ref: ${{ inputs.mimalloc_revision }}
          path: ${{ github.workspace }}/SourceCache/mimalloc

      - name: Configure mimalloc
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: mimalloc
          swift-version: ${{ inputs.swift_version }}
          # This is a small build, so setting up caching just for
          # this build is a net negative.
          enable-caching: false
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/mimalloc
          bin-dir: ${{ github.workspace }}/BinaryCache/${{ matrix.arch }}/mimalloc
          c-compiler: 'Host.C'
          cxx-compiler: 'Host.CXX'
          use-msvc-host-toolchain: true
          cmake-defines: |
            @{
              'CMAKE_CXX_FLAGS' = @("${{ matrix.arch == 'arm64' && '/arch:armv8.1' || '/arch:AVX2' }}");
              'MI_BUILD_OBJECT' = "NO";
              'MI_BUILD_SHARED' = "YES";
              'MI_BUILD_STATIC' = "NO";
              'MI_BUILD_TESTS' = "NO";
              'MI_NO_OPT_ARCH' = "YES";
            }

      - name: Build and install mimalloc
        run: |
          $MimallocBinaryCache = "${{ github.workspace }}\BinaryCache\${{ matrix.arch }}\mimalloc"
          $AssertToolchainRoot = "${{ github.workspace }}\BuildRoot\Library\Developer\Toolchains\${{ inputs.swift_version }}+Asserts\usr"
          $NoAssertToolchainRoot = "${{ github.workspace }}\BuildRoot\Library\Developer\Toolchains\${{ inputs.swift_version }}+NoAsserts\usr"
          cmake --build $MimallocBinaryCache
          cmake --install $MimallocBinaryCache --prefix $AssertToolchainRoot
          cmake --install $MimallocBinaryCache --prefix $NoAssertToolchainRoot

      - name: Apply mimalloc
        run: |
          $BuildSuffix = if ("${{ inputs.build_arch }}" -eq "arm64") { "-arm64" } else { "" }
          $AssertToolchainBin = "${{ github.workspace }}\BuildRoot\Library\Developer\Toolchains\${{ inputs.swift_version }}+Asserts\usr\bin"
          $NoAssertToolchainBin = "${{ github.workspace }}\BuildRoot\Library\Developer\Toolchains\${{ inputs.swift_version }}+NoAsserts\usr\bin"
          $MimallocExecutables = @("swift.exe",
                                   "swiftc.exe",
                                   "swift-driver.exe",
                                   "swift-frontend.exe",
                                   "clang.exe",
                                   "clang++.exe",
                                   "clang-cl.exe",
                                   "lld.exe",
                                   "lld-link.exe",
                                   "ld.lld.exe",
                                   "ld64.lld.exe")
          $AssertBinaries = $MimallocExecutables | ForEach-Object { [IO.Path]::Combine($AssertToolchainBin, $_) }
          $NoAssertBinaries = $MimallocExecutables | ForEach-Object { [IO.Path]::Combine($NoAssertToolchainBin, $_) }
          $Binaries = $AssertBinaries + $NoAssertBinaries
          $Minject = "${{ github.workspace }}\SourceCache\mimalloc\bin\minject$BuildSuffix"
          $PatchBinary = [IO.Path]::Combine([IO.Path]::GetDirectoryName($Minject), "binary.exe")
          try {
            foreach ($Exe in $Binaries) {
              Copy-Item -Force -LiteralPath $Exe -Destination $PatchBinary
              & $Minject -f -i -v $PatchBinary
              Copy-Item -Force -LiteralPath $PatchBinary -Destination $Exe
              & $Minject -l $Exe
            }
          } finally {
            if (Test-Path -LiteralPath $PatchBinary) {
              Remove-Item -Force -LiteralPath $PatchBinary
            }
          }

      - name: Install AzureSignTool
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'
        run: |
          dotnet tool install AzureSignTool --tool-path "${env:RUNNER_TEMP}\AzureSignTool" --add-source "https://api.nuget.org/v3/index.json"
          dotnet tool list --tool-path "${env:RUNNER_TEMP}\AzureSignTool"

      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_SP_CREDENTIALS }}
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'

      - name: Package Build Tools (Asserts)
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:WORKAROUND_MIMALLOC_ISSUE_997=false `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/bld/asserts/bld.asserts.wixproj

      - name: Package Build Tools (NoAsserts)
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:WORKAROUND_MIMALLOC_ISSUE_997=false `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/bld/noasserts/bld.noasserts.wixproj

      - name: Package CLI Tools (Asserts)
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:SWIFT_DOCC_RENDER_ARTIFACT_ROOT="${{ github.workspace }}\SourceCache\swift-docc-render-artifact" `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/cli/asserts/cli.asserts.wixproj

      - name: Package CLI Tools (NoAsserts)
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:SWIFT_DOCC_RENDER_ARTIFACT_ROOT="${{ github.workspace }}\SourceCache\swift-docc-render-artifact" `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/cli/noasserts/cli.noasserts.wixproj

      - name: Package Debugging Tools (Asserts)
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/dbg/asserts/dbg.asserts.wixproj

      - name: Package Debugging Tools (NoAsserts)
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/dbg/noasserts/dbg.noasserts.wixproj

      - name: Package IDE Tools (Asserts)
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/ide/asserts/ide.asserts.wixproj

      - name: Package IDE Tools (NoAsserts)
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/ide/noasserts/ide.noasserts.wixproj

      - name: Package Shared Runtime
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              -p:WindowsRuntimeARM64="${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/" `
              -p:WindowsRuntimeX64="${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/" `
              -p:WindowsRuntimeX86="${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/" `
              -p:VCRedistDir="$([IO.Path]::Combine(${env:VCToolsRedistDir}, "${{ matrix.arch == 'amd64' && 'x64' || matrix.arch }}", "Microsoft.VC143.CRT"))" `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/rtl/shared/msi/rtl.shared.msi.wixproj

      - if: ${{ inputs.release }}
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/bld.asserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/bld.noasserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/bld.asserts.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/bld.noasserts.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/cli.asserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/cli.noasserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/cli.asserts.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/cli.noasserts.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/dbg.asserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/dbg.noasserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/dbg.asserts.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/dbg.noasserts.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/ide.asserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/ide.noasserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/ide.asserts.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/ide.noasserts.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/rtl.${{ matrix.arch }}.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/rtl.${{ matrix.arch }}.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-bld-asserts-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/bld.asserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/bld.asserts.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-bld-noasserts-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/bld.noasserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/bld.noasserts.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-cli-asserts-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/cli.asserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/cli.asserts.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-cli-noasserts-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/cli.noasserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/cli.noasserts.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-dbg-asserts-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/dbg.asserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/dbg.asserts.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-dbg-noasserts-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/dbg.noasserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/dbg.noasserts.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-ide-asserts-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/ide.asserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/ide.asserts.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-ide-noasserts-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/ide.noasserts.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/ide.noasserts.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-rtl-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/rtl.${{ matrix.arch }}.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/rtl.${{ matrix.arch }}.cab

  package_windows_platform:
    name: Package Windows SDK & Runtime
    needs: [sdk]
    runs-on: ${{ inputs.default_build_runner }}
    outputs:
      expected-dlls-amd64: ${{ steps.write-expectations.outputs.expected-dlls-amd64 }}
      expected-dlls-arm64: ${{ steps.write-expectations.outputs.expected-dlls-arm64 }}
      expected-dlls-x86: ${{ steps.write-expectations.outputs.expected-dlls-x86 }}

    steps:
      - name: Select signing mode
        id: signing_mode
        if: inputs.signed
        env:
          AZURE_KEY_VAULT_URI: ${{ secrets.AZURE_KEY_VAULT_URI }}
        run: |
          if ($env:AZURE_KEY_VAULT_URI) {
            echo "azure_vault=true" >> $env:GITHUB_OUTPUT
          } else {
            echo "azure_vault=false" >> $env:GITHUB_OUTPUT
          }

      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}

      - uses: actions/download-artifact@v4
        with:
          name: Windows-amd64-shared-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform
      - uses: actions/download-artifact@v4
        with:
          name: Windows-amd64-static-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform
      - uses: actions/download-artifact@v4
        with:
          name: Windows-amd64-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/libxml2
      - uses: actions/download-artifact@v4
        with:
          name: Windows-amd64-curl-${{ inputs.curl_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/curl
      - uses: actions/download-artifact@v4
        with:
          name: Windows-amd64-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib
      - uses: actions/download-artifact@v4
        with:
          name: Windows-amd64-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli/

      - run: |
          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/x86_64/" -ItemType Directory -Force | Out-Null
          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/x86_64/" -ItemType Directory -Force | Out-Null

          Get-ChildItem "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/*.lib" -File -ErrorAction Ignore | Move-Item -Force -Destination "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/x86_64/"
          Get-ChildItem "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/*.lib" -File -ErrorAction Ignore | Move-Item -Force -Destination "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/x86_64/"

          Move-Item ${{ github.workspace }}/BuildRoot/Library/libxml2/lib/libxml2s.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/x86_64/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/curl/lib/libcurl.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/x86_64/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/zlib/lib/zlibstatic.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/x86_64/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/brotli/lib/brotlicommon.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/x86_64/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/brotli/lib/brotlidec.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/x86_64/

          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-x86_64/usr" -ItemType Directory -Force | Out-Null

          Move-Item "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/bin" "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-x86_64/usr"

          Move-Item "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/libexec" "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-x86_64/usr"

      - uses: actions/download-artifact@v4
        with:
          name: Windows-arm64-shared-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform
      - uses: actions/download-artifact@v4
        with:
          name: Windows-arm64-static-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform
      - uses: actions/download-artifact@v4
        with:
          name: Windows-arm64-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/libxml2
      - uses: actions/download-artifact@v4
        with:
          name: Windows-arm64-curl-${{ inputs.curl_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/curl
      - uses: actions/download-artifact@v4
        with:
          name: Windows-arm64-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib
      - uses: actions/download-artifact@v4
        with:
          name: Windows-arm64-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli

      - run: |
          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/aarch64/" -ItemType Directory -Force
          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/aarch64/" -ItemType Directory -Force

          Get-ChildItem "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/*.lib" -File -ErrorAction Ignore | Move-Item -Force -Destination "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/aarch64/"
          Get-ChildItem "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/*.lib" -File -ErrorAction Ignore | Move-Item -Force -Destination "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/aarch64/"

          Move-Item ${{ github.workspace }}/BuildRoot/Library/libxml2/lib/libxml2s.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/aarch64/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/curl/lib/libcurl.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/aarch64/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/zlib/lib/zlibstatic.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/aarch64/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/brotli/lib/brotlidec.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/aarch64/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/brotli/lib/brotlicommon.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/aarch64/

          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-aarch64/usr" -ItemType Directory -Force | Out-Null

          Move-Item "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/bin" "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-aarch64/usr"

          Move-Item "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/libexec" "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-aarch64/usr"

      - uses: actions/download-artifact@v4
        with:
          name: Windows-x86-shared-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform
      - uses: actions/download-artifact@v4
        with:
          name: Windows-x86-static-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform
      - uses: actions/download-artifact@v4
        with:
          name: Windows-x86-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/libxml2
      - uses: actions/download-artifact@v4
        with:
          name: Windows-x86-curl-${{ inputs.curl_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/curl
      - uses: actions/download-artifact@v4
        with:
          name: Windows-x86-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib
      - uses: actions/download-artifact@v4
        with:
          name: Windows-x86-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli

      - run: |
          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/i686/" -ItemType Directory -Force
          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/i686/" -ItemType Directory -Force

          Get-ChildItem "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/*.lib" -File -ErrorAction Ignore | Move-Item -Force -Destination "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift/windows/i686/"
          Get-ChildItem "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/*.lib" -File -ErrorAction Ignore | Move-Item -Force -Destination "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/i686/"

          Move-Item ${{ github.workspace }}/BuildRoot/Library/libxml2/lib/libxml2s.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/i686/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/curl/lib/libcurl.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/i686/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/zlib/lib/zlibstatic.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/i686/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/brotli/lib/brotlidec.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/i686/
          Move-Item ${{ github.workspace }}/BuildRoot/Library/brotli/lib/brotlicommon.lib ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/lib/swift_static/windows/i686/

          New-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-i686/usr" -ItemType Directory -Force | Out-Null

          Move-Item "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/bin" "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-i686/usr"

          # swift-backtrace is currently disabled for Windows x86 https://github.com/swiftlang/swift/issues/87499
          # Uncomment the following when it is enabled
          # Move-Item "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk/usr/bin/libexec" "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-i686/usr"

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-installer-scripts
          ref: ${{ inputs.swift_installer_scripts_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-installer-scripts
          show-progress: false

      - run: |
          $CertificatePath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.b64
          $PFXPath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.pfx
          Set-Content -Path $CertificatePath -Value '${{ secrets.CERTIFICATE }}'
          certutil.exe -decode $CertificatePath $PFXPath
          Write-Output CERTIFICATE=$PFXPath | Out-File -FilePath ${env:GITHUB_ENV} -Encoding utf8 -Append
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'false'

      - name: Install WixToolset.Sdk
        run: |
          if ((Get-Package -Name WixToolset.Sdk -ErrorAction SilentlyContinue) -eq $null) {
            Install-Package -Name WixToolset.Sdk -RequiredVersion 4.0.1 -Force
          }

      - run: |
          $SDKRoot = "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Windows.platform/Developer/SDKs/Windows.sdk"
          foreach ($Module in @("Block", "dispatch", "os", "_foundation_unicode", "_FoundationCShims", "_FoundationInternationalizationData")) {
            foreach ($ResourceType in @("swift", "swift_static")) {
              $ModuleDirectory = "$SDKRoot/usr/lib/$ResourceType/$Module"
              if (Test-Path $ModuleDirectory) {
                Move-Item $ModuleDirectory "$SDKRoot/usr/include/"
              }
            }
          }

      - name: Install AzureSignTool
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'
        run: |
          dotnet tool install AzureSignTool --tool-path "${env:RUNNER_TEMP}\AzureSignTool" --add-source "https://api.nuget.org/v3/index.json"
          dotnet tool list --tool-path "${env:RUNNER_TEMP}\AzureSignTool"

      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_SP_CREDENTIALS }}
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'

      - name: Package Platform
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ inputs.build_arch }} `
              -p:WindowsArchitectures="`"aarch64;i686;x86_64`"" `
              -p:WindowsRuntimeARM64="${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-aarch64" `
              -p:WindowsRuntimeX64="${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-x86_64" `
              -p:WindowsRuntimeX86="${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-i686" `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/platforms/windows/windows.wixproj
      - name: Write DLL Expectations
        id: write-expectations
        run: |
          @(
            @("amd64", "x86_64"),
            @("arm64", "aarch64"),
            @("x86", "i686")
          ) | ForEach-Object {
            $arch, $suffix = $_
            $runtimes = "${{ github.workspace }}/BuildRoot/Library/Developer/Runtimes/Windows-$suffix/usr/bin"
            Get-ChildItem -Path $runtimes -Filter *.dll -File |
              # A flat list collapses down to a space-separated string, so we need to convert to JSON so we can read it later.
              ForEach-Object Name | ConvertTo-Json -Compress | ForEach-Object { "expected-dlls-$arch=$_" } |
              Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          }
      - if: ${{ inputs.release }}
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/windows.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/windows.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.windows.arm64.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.windows.x64.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.windows.x86.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/amd64/rtl.shared.amd64.msm
            ${{ github.workspace }}/BinaryCache/installer/Release/amd64/rtl.static.amd64.msm
            ${{ github.workspace }}/BinaryCache/installer/Release/arm64/rtl.shared.arm64.msm
            ${{ github.workspace }}/BinaryCache/installer/Release/arm64/rtl.static.arm64.msm
            ${{ github.workspace }}/BinaryCache/installer/Release/x86/rtl.shared.x86.msm
            ${{ github.workspace }}/BinaryCache/installer/Release/x86/rtl.static.x86.msm

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-platform-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/windows.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/windows.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.windows.arm64.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.windows.x64.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.windows.x86.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-amd64-rtl-shared-msm
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/amd64/rtl.shared.amd64.msm

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-amd64-rtl-static-msm
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/amd64/rtl.static.amd64.msm

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-arm64-rtl-shared-msm
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/arm64/rtl.shared.arm64.msm

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-arm64-rtl-static-msm
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/arm64/rtl.static.arm64.msm

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-x86-rtl-shared-msm
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/x86/rtl.shared.x86.msm

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-x86-rtl-static-msm
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/x86/rtl.static.x86.msm

  package_android_platform:
    name: Package Android SDK & Runtime
    needs: [ds2, sdk]
    runs-on: ${{ inputs.default_build_runner }}

    steps:
      - name: Select signing mode
        id: signing_mode
        if: inputs.build_android && inputs.signed
        env:
          AZURE_KEY_VAULT_URI: ${{ secrets.AZURE_KEY_VAULT_URI }}
        run: |
          if ($env:AZURE_KEY_VAULT_URI) {
            echo "azure_vault=true" >> $env:GITHUB_OUTPUT
          } else {
            echo "azure_vault=false" >> $env:GITHUB_OUTPUT
          }

      - if: inputs.build_android
        uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - if: inputs.build_android
        uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}

      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-arm64-shared-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-arm64-static-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-arm64-ds2
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/Library
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-arm64-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-arm64-curl-${{ inputs.curl_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-arm64-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-arm64-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr
      - if: inputs.build_android
        run: |
          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/lib*.so" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/aarch64/
          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/lib*.a" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/aarch64/

          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/lib*.a" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/aarch64/

          # build.ps1: Copy static dependencies (libbrotli/curl/xml2/z) into swift_static/<arch>
          Get-ChildItem -Path "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr","${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr","${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr","${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr" -Recurse -Include libbrotlicommon.a,libbrotlidec.a,libcurl.a,libxml2.a,libz.a -ErrorAction Ignore | Copy-Item -Force -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/aarch64/

      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-armv7-shared-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-armv7-static-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-armv7-ds2
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/Library
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-armv7-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-armv7-curl-${{ inputs.curl_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-armv7-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-armv7-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr
      - if: inputs.build_android
        run: |
          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/lib*.so" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/armv7/
          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/lib*.a" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/armv7/

          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/lib*.a" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/armv7/

          # build.ps1: Copy static dependencies (libbrotli/curl/xml2/z) into swift_static/<arch>
          Get-ChildItem -Path "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr","${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr","${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr","${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr" -Recurse -Include libbrotlicommon.a,libbrotlidec.a,libcurl.a,libxml2.a,libz.a -ErrorAction Ignore | Copy-Item -Force -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/armv7/

      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-x86_64-shared-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-x86_64-static-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-x86_64-ds2
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/Library
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-x86_64-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-x86_64-curl-${{ inputs.curl_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-x86_64-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-x86_64-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr
      - if: inputs.build_android
        run: |
          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/lib*.so" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/x86_64/
          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/lib*.a" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/x86_64/

          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/lib*.a" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/x86_64/

          # build.ps1: Copy static dependencies (libbrotli/curl/xml2/z) into swift_static/<arch>
          Get-ChildItem -Path "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr","${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr","${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr","${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr" -Recurse -Include libbrotlicommon.a,libbrotlidec.a,libcurl.a,libxml2.a,libz.a -ErrorAction Ignore | Copy-Item -Force -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/x86_64/

      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-i686-shared-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-i686-static-sdk
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-i686-ds2
          path: ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/Library
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-i686-brotli-${{ inputs.brotli_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-i686-curl-${{ inputs.curl_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-i686-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr
      - if: inputs.build_android
        uses: actions/download-artifact@v4
        with:
          name: Android-i686-zlib-${{ inputs.zlib_version }}
          path: ${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr
      - if: inputs.build_android
        run: |
          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/lib*.so" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/i686/
          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/lib*.a" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift/android/i686/

          Move-Item -Path "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/lib*.a" -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/i686/

          # build.ps1: Copy static dependencies (libbrotli/curl/xml2/z) into swift_static/<arch>
          Get-ChildItem -Path "${{ github.workspace }}/BuildRoot/Library/brotli-${{ inputs.brotli_version }}/usr","${{ github.workspace }}/BuildRoot/Library/curl-${{ inputs.curl_version }}/usr","${{ github.workspace }}/BuildRoot/Library/libxml2-${{ inputs.libxml2_version }}/usr","${{ github.workspace }}/BuildRoot/Library/zlib-${{ inputs.zlib_version }}/usr" -Recurse -Include libbrotlicommon.a,libbrotlidec.a,libcurl.a,libxml2.a,libz.a -ErrorAction Ignore | Copy-Item -Force -Destination ${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk/usr/lib/swift_static/android/i686/

      - if: inputs.build_android
        uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-installer-scripts
          ref: ${{ inputs.swift_installer_scripts_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-installer-scripts
          show-progress: false

      - if: inputs.build_android && inputs.signed && steps.signing_mode.outputs.azure_vault == 'false'
        run: |
          $CertificatePath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.b64
          $PFXPath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.pfx
          Set-Content -Path $CertificatePath -Value '${{ secrets.CERTIFICATE }}'
          certutil.exe -decode $CertificatePath $PFXPath
          Write-Output CERTIFICATE=$PFXPath | Out-File -FilePath ${env:GITHUB_ENV} -Encoding utf8 -Append

      - if: inputs.build_android
        name: Install WixToolset.Sdk
        run: |
          if ((Get-Package -Name WixToolset.Sdk -ErrorAction SilentlyContinue) -eq $null) {
            Install-Package -Name WixToolset.Sdk -RequiredVersion 4.0.1 -Force
          }

      - if: inputs.build_android
        run: |
          $SDKRoot = "${{ github.workspace }}/BuildRoot/Library/Developer/Platforms/Android.platform/Developer/SDKs/Android.sdk"
          foreach ($Module in @("Block", "dispatch", "os", "_foundation_unicode", "_FoundationCShims", "_FoundationInternationalizationData")) {
            foreach ($ResourceType in @("swift", "swift_static")) {
              $ModuleDirectory = "$SDKRoot/usr/lib/$ResourceType/$Module"
              if (Test-Path $ModuleDirectory) {
                Move-Item $ModuleDirectory "$SDKRoot/usr/include/"
              }
            }
          }

      - name: Install AzureSignTool
        if: inputs.build_android && inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'
        run: |
          dotnet tool install AzureSignTool --tool-path "${env:RUNNER_TEMP}\AzureSignTool" --add-source "https://api.nuget.org/v3/index.json"
          dotnet tool list --tool-path "${env:RUNNER_TEMP}\AzureSignTool"

      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_SP_CREDENTIALS }}
        if: inputs.build_android && inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'

      - if: inputs.build_android
        name: Package SDK
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:ANDROID_INCLUDE_DS2=true `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ inputs.build_arch }} `
              -p:AndroidArchitectures="`"x86_64;aarch64;i686;armv7`"" `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/platforms/android/android.wixproj

      - if: ${{ inputs.build_android && inputs.release }}
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/android.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/android.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.android.arm64.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.android.arm.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.android.x64.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.android.x86.cab

      - if: inputs.build_android
        uses: actions/upload-artifact@v4
        with:
          name: Android-platform-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/android.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/android.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.android.arm64.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.android.arm.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.android.x64.cab
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/sdk.android.x86.cab

  package_python:
    if: inputs.build_os == 'Windows'
    name: Package Embedded Python runtime
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.host_matrix) }}
  
    steps:
      - name: Select signing mode
        id: signing_mode
        if: inputs.signed
        env:
          AZURE_KEY_VAULT_URI: ${{ secrets.AZURE_KEY_VAULT_URI }}
        run: |
          if ($env:AZURE_KEY_VAULT_URI) {
            echo "azure_vault=true" >> $env:GITHUB_OUTPUT
          } else {
            echo "azure_vault=false" >> $env:GITHUB_OUTPUT
          }

      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false

      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-installer-scripts
          ref: ${{ inputs.swift_installer_scripts_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-installer-scripts
          show-progress: false

      - run: |
          $CertificatePath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.b64
          $PFXPath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.pfx
          Set-Content -Path $CertificatePath -Value '${{ secrets.CERTIFICATE }}'
          certutil.exe -decode $CertificatePath $PFXPath
          Write-Output CERTIFICATE=$PFXPath | Out-File -FilePath ${env:GITHUB_ENV} -Encoding utf8 -Append
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'false'

      - name: Install WixToolset.Sdk
        run: |
          if ((Get-Package -Name WixToolset.Sdk -ErrorAction SilentlyContinue) -eq $null) {
            Install-Package -Name WixToolset.Sdk -RequiredVersion 4.0.1 -Force
          }

      - name: Install AzureSignTool
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'
        run: |
          dotnet tool install AzureSignTool --tool-path "${env:RUNNER_TEMP}\AzureSignTool" --add-source "https://api.nuget.org/v3/index.json"
          dotnet tool list --tool-path "${env:RUNNER_TEMP}\AzureSignTool"

      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_SP_CREDENTIALS }}
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'

      - name: Download and expand Python
        env:
          INPUT_PYTHON_DOWNLOAD_LOCATIONS: ${{ inputs.python_download_locations }}
          INPUT_PYTHON_VERSION: ${{ inputs.python_version }}
        run: |
          function DownloadAndVerify($URL, $Destination, $Hash) {
            New-Item -ItemType Directory (Split-Path -Path $Destination -Parent) -ErrorAction Ignore | Out-Null
            $WebClient = New-Object Net.WebClient
            $WebClient.DownloadFile($URL, $Destination)
            $ExpectedHash = $Hash.ToUpperInvariant()
            $SHA256 = (Get-FileHash -Path $Destination -Algorithm SHA256).Hash.ToUpperInvariant()
            if ($SHA256 -ne $ExpectedHash) {
              throw "SHA256 mismatch ($SHA256 vs $Hash) for $URL"
            }
          }

          $KnownPythons = $env:INPUT_PYTHON_DOWNLOAD_LOCATIONS | ConvertFrom-Json
          $version = $env:INPUT_PYTHON_VERSION
          $archKey = "${{ matrix.arch }}".ToUpper()
          $pythonInfo = $KnownPythons."$version"."${archKey}_Embedded"
          if ($null -eq $pythonInfo) {
            throw "No python download info for version $version and architecture $archKey"
          }
          Write-Host "Downloading Python $version for $archKey"

          # Download the Python package
          $packagePath = "${{ github.workspace }}\BinaryCache\Python-$archKey-$version.zip"
          DownloadAndVerify -URL $pythonInfo.URL -Destination $packagePath -Hash $pythonInfo.SHA256
          Write-Host "Downloaded Python $version for $archKey to $packagePath"
  
          # Extract the Python package
          $extractRoot = "${{ github.workspace }}\BuildRoot\Library\Developer\Python-$version"
          New-Item -ItemType Directory -Path $extractRoot -ErrorAction Ignore | Out-Null
          Expand-Archive -Path $packagePath -DestinationPath $extractRoot -Force
          Write-Host "Extracted Python $version for $archKey to $extractRoot"

      - name: Package Python
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ImageRoot=${{ github.workspace }}/BuildRoot/Library/Developer `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:PythonVersion=${{ inputs.python_version }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/python/python.wixproj

      - if: ${{ inputs.release }}
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/python.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/python.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-python-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/python.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/python.cab

  package_resources:
    if: inputs.build_os == 'Windows'
    name: Package Resources
    runs-on: ${{ inputs.default_build_runner }}

    steps:
      - name: Select signing mode
        id: signing_mode
        if: inputs.signed
        env:
          AZURE_KEY_VAULT_URI: ${{ secrets.AZURE_KEY_VAULT_URI }}
        run: |
          if ($env:AZURE_KEY_VAULT_URI) {
            echo "azure_vault=true" >> $env:GITHUB_OUTPUT
          } else {
            echo "azure_vault=false" >> $env:GITHUB_OUTPUT
          }

      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false

      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-installer-scripts
          ref: ${{ inputs.swift_installer_scripts_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-installer-scripts
          show-progress: false

      - run: |
          $CertificatePath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.b64
          $PFXPath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.pfx
          Set-Content -Path $CertificatePath -Value '${{ secrets.CERTIFICATE }}'
          certutil.exe -decode $CertificatePath $PFXPath
          Write-Output CERTIFICATE=$PFXPath | Out-File -FilePath ${env:GITHUB_ENV} -Encoding utf8 -Append
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'false'

      - name: Install WixToolset.Sdk
        run: |
          if ((Get-Package -Name WixToolset.Sdk -ErrorAction SilentlyContinue) -eq $null) {
            Install-Package -Name WixToolset.Sdk -RequiredVersion 4.0.1 -Force
          }

      - name: Install AzureSignTool
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'
        run: |
          dotnet tool install AzureSignTool --tool-path "${env:RUNNER_TEMP}\AzureSignTool" --add-source "https://api.nuget.org/v3/index.json"
          dotnet tool list --tool-path "${env:RUNNER_TEMP}\AzureSignTool"

      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_SP_CREDENTIALS }}
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'

      - name: Package Resources
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ProductVersion=${{ inputs.swift_version }} `
              -p:ProductArchitecture=${{ inputs.build_arch }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/res/res.wixproj

      - if: ${{ inputs.release }}
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/res.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/res.cab

      - uses: actions/upload-artifact@v4
        with:
          name: Windows-res-msi
          path: |
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/res.msi
            ${{ github.workspace }}/BinaryCache/installer/Release/${{ inputs.build_arch }}/res.cab

  installer:
    if: inputs.build_os == 'Windows'
    needs: [package_tools, package_windows_platform, package_android_platform, package_python, package_resources]
    runs-on: ${{ inputs.default_build_runner }}

    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.host_matrix) }}

    steps:
      - name: Select signing mode
        id: signing_mode
        if: inputs.signed
        env:
          AZURE_KEY_VAULT_URI: ${{ secrets.AZURE_KEY_VAULT_URI }}
        run: |
          if ($env:AZURE_KEY_VAULT_URI) {
            echo "azure_vault=true" >> $env:GITHUB_OUTPUT
          } else {
            echo "azure_vault=false" >> $env:GITHUB_OUTPUT
          }

      - uses: actions/checkout@v4.2.2
        with:
          ref: ${{ inputs.ci_build_revision }}
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-docc-render-artifact
          ref: ${{ inputs.swift_docc_render_artifact_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-docc-render-artifact
          show-progress: false

      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}

      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-bld-asserts-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}
      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-bld-noasserts-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}

      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-cli-asserts-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}
      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-cli-noasserts-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}

      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-dbg-asserts-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}
      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-dbg-noasserts-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}

      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-ide-asserts-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}
      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-ide-noasserts-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}

      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-python-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}

      - uses: actions/download-artifact@v4
        with:
          name: Windows-res-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}

      - uses: actions/download-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-rtl-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}

      - uses: actions/download-artifact@v4
        with:
          name: Windows-platform-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}
      - uses: actions/download-artifact@v4
        if: inputs.build_android
        with:
          name: Android-platform-msi
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-installer-scripts
          ref: ${{ inputs.swift_installer_scripts_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-installer-scripts
          show-progress: false

      - run: |
          $CertificatePath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.b64
          $PFXPath = Join-Path -Path ${env:RUNNER_TEMP} -ChildPath CodeSign.pfx
          Set-Content -Path $CertificatePath -Value '${{ secrets.CERTIFICATE }}'
          certutil -decode $CertificatePath $PFXPath
          Echo CERTIFICATE=$PFXPath | Out-File -FilePath ${env:GITHUB_ENV} -Encoding utf8 -Append
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'false'

      - name: Install AzureSignTool
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'
        run: |
          dotnet tool install AzureSignTool --tool-path "${env:RUNNER_TEMP}\AzureSignTool" --add-source "https://api.nuget.org/v3/index.json"
          dotnet tool list --tool-path "${env:RUNNER_TEMP}\AzureSignTool"

      - name: Authenticate with Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_SP_CREDENTIALS }}
        if: inputs.signed && steps.signing_mode.outputs.azure_vault == 'true'

      # The installer bundle needs the shared project for localization strings,
      # but it won't build the dependency on its own due to -p:BuildProjectReferences=false.
      - name: Build shared project
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:ProductArchitecture=${{ matrix.arch }} `
              -p:ProductVersion=${{ inputs.swift_version }}-${{ inputs.swift_tag }} `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/shared/shared.wixproj

      - name: Build installer bundle (offline)
        env:
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          $Platforms=@("windows")
          if ("${{ inputs.build_android }}" -eq "true") {
            $Platforms=@("android") + $Platforms
          }

          msbuild -nologo -restore -maxCpuCount `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:BuildProjectReferences=false `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:BundleFlavor=offline `
              -p:Platforms="`"$($Platforms -Join ';')`"" `
              -p:AndroidArchitectures="`"aarch64;armv7;i686;x86_64`"" `
              -p:WindowsArchitectures="`"aarch64;i686;x86_64`"" `
              -p:ProductArchitecture=${{ matrix.arch }} `
              -p:ProductVersion=${{ inputs.swift_version }}-${{ inputs.swift_tag }} `
              -p:ToolchainVariants="`"asserts;noasserts`"" `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/bundle/installer.wixproj

      - name: Generate Build Provenance (offline installer)
        if: ${{ inputs.release }}
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/installer.exe

      - name: Upload installer (offline)
        uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-installer-offline
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/installer.exe

      - name: Generate online installer download URL
        if: ${{ inputs.release }}
        id: generate-base-release-download-subcomponent
        env:
          INPUT_SWIFT_TAG: ${{ inputs.swift_tag }}
        run: |
          $BaseReleaseDownloadUrlSubcomponent = "${env:INPUT_SWIFT_TAG}/${{ matrix.arch }}/${{ github.run_id }}"
          echo "url_subcomponent=$BaseReleaseDownloadUrlSubcomponent" >> $env:GITHUB_OUTPUT

      - name: Build installer bundle (online)
        if: ${{ inputs.release }}
        env:
          INPUT_BUILD_ANDROID: ${{ inputs.build_android }}
          AZURE_KEY_VAULT_URI: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_URI || '' }}
          AZURE_KEY_VAULT_CERTIFICATE_NAME: ${{ steps.signing_mode.outputs.azure_vault == 'true' && secrets.AZURE_KEY_VAULT_CERTIFICATE_NAME || '' }}
          AZURE_VAULT_SIGN_COMMAND: ${{ steps.signing_mode.outputs.azure_vault == 'true' && '"%RUNNER_TEMP%\AzureSignTool\AzureSignTool.exe" sign --azure-key-vault-url "%AZURE_KEY_VAULT_URI%" --azure-key-vault-managed-identity --azure-key-vault-certificate "%AZURE_KEY_VAULT_CERTIFICATE_NAME%" --file-digest sha256 --timestamp-rfc3161 "http://timestamp.digicert.com" --timestamp-digest sha256 --verbose' || '' }}
        run: |
          $Platforms=@("windows")
          if ($env:INPUT_BUILD_ANDROID -eq "true") {
            $Platforms=@("android") + $Platforms
          }
          $BaseReleaseDownloadUrl = "https://swift-toolchain.thebrowserco.com/${{ steps.generate-base-release-download-subcomponent.outputs.url_subcomponent }}"

          msbuild -nologo -restore -maxCpuCount `
              /t:rebuild `
              -p:BaseOutputPath=${{ github.workspace }}\BinaryCache\installer\ `
              -p:Configuration=Release `
              -p:BuildProjectReferences=false `
              -p:SignOutput=${{ inputs.signed }} `
              -p:SignCommand="${env:AZURE_VAULT_SIGN_COMMAND}" `
              -p:CERTIFICATE=${env:CERTIFICATE} `
              -p:PASSPHRASE=${{ secrets.PASSPHRASE }} `
              -p:SWIFT_DOCC_RENDER_ARTIFACT_ROOT="${{ github.workspace }}\SourceCache\swift-docc-render-artifact" `
              -p:BundleFlavor=online `
              -p:BaseReleaseDownloadUrl=$BaseReleaseDownloadUrl `
              -p:Platforms="`"$($Platforms -Join ';')`"" `
              -p:AndroidArchitectures="`"aarch64;armv7;i686;x86_64`"" `
              -p:WindowsArchitectures="`"aarch64;i686;x86_64`"" `
              -p:ProductArchitecture=${{ matrix.arch }} `
              -p:ProductVersion=${{ inputs.swift_version }}-${{ inputs.swift_tag }} `
              -p:ToolchainVariants="`"asserts;noasserts`"" `
              ${{ github.workspace }}/SourceCache/swift-installer-scripts/platforms/Windows/bundle/installer.wixproj

      - name: Prepare layout for upload
        id: prepare-layout
        if: ${{ inputs.release }}
        run: |
          # Create the target folder.
          $SourceDir = "${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}"
          $LayoutDir = "${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/layout"
          New-Item -ItemType Directory -Path $LayoutDir -Force | Out-Null

          # Copy all .txt files from src to the filtered directory
          Copy-Item -Path "$SourceDir/installer.exe" -Destination "$LayoutDir" -Force
          Copy-Item -Path "$SourceDir/*.cab" -Destination "$LayoutDir" -Force
          Copy-Item -Path "$SourceDir/*.msi" -Destination "$LayoutDir" -Force

          echo "LayoutDir=$LayoutDir"
          echo "LayoutDir=$LayoutDir" >> $env:GITHUB_OUTPUT

      - name: Generate Build Provenance (online installer)
        if: ${{ inputs.release }}
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/installer.exe

      - name: Upload installer (online)
        if: ${{ inputs.release }}
        uses: actions/upload-artifact@v4
        with:
          name: Windows-${{ matrix.arch }}-installer-online
          path: ${{ github.workspace }}/BinaryCache/installer/Release/${{ matrix.arch }}/installer.exe

      - name: Upload msi's and cab's 
        if: ${{ inputs.release }}
        env:
          SOURCE_DIR: ${{ steps.prepare-layout.outputs.LayoutDir }}
          DESTINATION_PREFIX: ${{ steps.generate-base-release-download-subcomponent.outputs.url_subcomponent }}
          R2_BUCKET: swift-toolchain
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: auto
          R2_ENDPOINT_URL: https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com
        run: |
          aws s3 sync "${env:SOURCE_DIR}" "s3://${env:R2_BUCKET}/${env:DESTINATION_PREFIX}/" `
            --endpoint-url "${env:R2_ENDPOINT_URL}" `
            --no-progress `
            --acl private

  smoke_test:
    if: inputs.build_os == 'Windows'
    needs: [installer]
    runs-on: ${{ inputs.default_build_runner }}

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          repository: compnerd/swift-win32
          ref: refs/heads/main
          path: ${{ github.workspace }}/SourceCache/swift-win32
          show-progress: false

      - uses: actions/checkout@v4.2.2
        with:
          repository: compnerd/vigil
          ref: refs/heads/main
          path: ${{ github.workspace }}/SourceCache/vigil
          show-progress: false

      - uses: actions/checkout@v4.2.2
        with:
          repository: pointfreeco/swift-issue-reporting
          ref: refs/heads/main
          path: ${{ github.workspace }}/SourceCache/swift-issue-reporting
          show-progress: false

      - name: Download Swift SDK Installer (${{ inputs.release && 'online' || 'offline' }})
        uses: actions/download-artifact@v4
        with:
          name: Windows-${{ inputs.build_arch }}-installer-${{ inputs.release && 'online' || 'offline' }}
          path: ${{ github.workspace }}/tmp

      # TODO(compnerd): migrate this to compnerd/gha-setup-swift after the work that @mangini is doing is completed
      - run: |
          function Update-EnvironmentVariables {
            foreach ($level in "Machine", "User") {
              [Environment]::GetEnvironmentVariables($level).GetEnumerator() | % {
                # For Path variables, append the new values, if they're not already in there
                if ($_.Name -Match 'Path$') {
                  $_.Value = ($((Get-Content "Env:$($_.Name)") + ";$($_.Value)") -Split ';' | Select -Unique) -Join ';'
                }
                $_
              } | Set-Content -Path { "Env:$($_.Name)" }
            }
          }

          try {
            Write-Host "Waiting for 120 seconds before starting installer..."
            Start-Sleep -Seconds 120
            Write-Host "Starting Install installer.exe..."
            $Process = Start-Process -FilePath ${{ github.workspace }}/tmp/installer.exe -ArgumentList ("-q") -Wait -PassThru
            $ExitCode = $Process.ExitCode
            if ($ExitCode -eq 0 -or $ExitCode -eq 3010) {
              Write-Host "Installation successful"
            } else {
              Write-Host "non-zero exit code returned by the installation process: $ExitCode"
              exit $ExitCode
            }
          } catch {
            Write-Host "Failed to install: $($_.Exception.Message)"
            exit 1
          }
          Update-EnvironmentVariables

          # Reset Path and environment
          echo "$env:Path" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8
          Get-ChildItem Env: | % { echo "$($_.Name)=$($_.Value)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append }

      - name: Build & Test compnerd/swift-win32
        working-directory: ${{ github.workspace }}/SourceCache/swift-win32
        run: |
          swift build
          swift test -Xswiftc -DENABLE_TESTING

      - name: Build compnerd/vigil
        working-directory: ${{ github.workspace }}/SourceCache/vigil
        run: |
          $SDK = "$(Split-Path -Path ${env:SDKROOT} -Parent)/Windows.sdk"
          swift build --build-system native --triple aarch64-unknown-windows-msvc -debug-info-format none -c release -Xswiftc -sdk -Xswiftc ${SDK} -Xswiftc -static-stdlib -Xlinker /WX

      - name: Build & Test pointfreeco/swift-issue-reporting
        working-directory: ${{ github.workspace }}/SourceCache/swift-issue-reporting
        run: |
          swift build --build-system native -c release
          swift test --build-system native -c release

  # Ensures redistributables contain all expected DLLs.
  redistributable_smoke_test:
    if: inputs.build_os == 'Windows'
    needs: [package_windows_platform]
    runs-on: ${{ inputs.default_build_runner }}
    strategy:
      fail-fast: false
      matrix:
        arch: ["amd64", "arm64", "x86"]
        variant: ["static", "shared"]
    env:
      MSM_ARTIFACT_NAME: Windows-${{ matrix.arch }}-rtl-${{ matrix.variant }}-msm
      MSM_FILE_NAME: rtl.${{ matrix.variant }}.${{ matrix.arch }}.msm

      EXPECTED: >
        ${{ matrix.variant == 'shared' && (
          matrix.arch == 'amd64' && needs.package_windows_platform.outputs.expected-dlls-amd64 ||
          matrix.arch == 'arm64' && needs.package_windows_platform.outputs.expected-dlls-arm64 ||
          matrix.arch == 'x86' && needs.package_windows_platform.outputs.expected-dlls-x86
        ) || '["BlocksRuntime.dll", "dispatch.dll"]' }}
    steps:
      - name: Download MSM
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.MSM_ARTIFACT_NAME }}
          path: ${{ runner.temp }}
      - name: Test ${{ matrix.variant }} ${{ matrix.arch }}
        run: |
          $msmPath = Join-Path $env:RUNNER_TEMP $env:MSM_FILE_NAME

          $installer = New-Object -ComObject WindowsInstaller.Installer
          $db = $installer.OpenDatabase($msmPath, 0)
          $view = $db.OpenView("SELECT FileName FROM ``File``")
          $view.Execute()

          $dlls = @()
          while ($record = $view.Fetch()) {
            $data = $record.StringData(1)
            $file = $data.Split('|')[-1]
            if ($file.EndsWith('.dll')) {
              $dlls += $file
            }
          }

          $actual = [System.Collections.Generic.HashSet[string]]::new([string[]]$dlls)
          $expected = $env:EXPECTED | ConvertFrom-Json
          $missing = $expected | Where-Object { -not $actual.Contains($_) }
          if ($missing.Count -gt 0) {
            $missing | ForEach-Object { Write-Host "::error:: '$_' not found in '$env:MSM_FILE_NAME'" }
            exit 1
          }

  smoke_test_android:
    if: inputs.build_os == 'Windows' && inputs.build_android
    needs: [installer]
    runs-on: ${{ inputs.default_build_runner }}

    env:
      ANDROID_ABI: ${{ matrix.arch == 'aarch64' && 'arm64-v8a' || 'x86_64' }}

    strategy:
      fail-fast: false
      matrix:
        arch: [ x86_64, aarch64 ]

    steps:
      - name: Checkout cassowary project
        uses: actions/checkout@v4.2.2
        with:
          repository: compnerd/cassowary
          ref: 0.0.2
          path: ${{ github.workspace }}/SourceCache/cassowary

      - name: Checkout swift-firebase project
        uses: actions/checkout@v4.2.2
        with:
          repository: compnerd/swift-firebase
          ref: main
          path: ${{ github.workspace }}/SourceCache/swift-firebase

      - name: Install Android NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: ${{ inputs.ANDROID_NDK_VERSION }}
          local-cache: true

      - name: Download Swift SDK Installer (${{ inputs.release && 'online' || 'offline' }})
        uses: actions/download-artifact@v4
        with:
          name: Windows-${{ inputs.build_arch }}-installer-${{ inputs.release && 'online' || 'offline' }}
          path: ${{ github.workspace }}/tmp

      # TODO(compnerd): migrate this to compnerd/gha-setup-swift after the work that @mangini is doing is completed
      - name: Install Swift SDK
        run: |
          function Update-EnvironmentVariables {
            foreach ($level in "Machine", "User") {
              [Environment]::GetEnvironmentVariables($level).GetEnumerator() | % {
                # For Path variables, append the new values, if they're not already in there
                if ($_.Name -Match 'Path$') {
                  $_.Value = ($((Get-Content "Env:$($_.Name)") + ";$($_.Value)") -Split ';' | Select -Unique) -Join ';'
                }
                $_
              } | Set-Content -Path { "Env:$($_.Name)" }
            }
          }

          try {
            Write-Host "Starting Install installer.exe..."
            $Process = Start-Process -FilePath ${{ github.workspace }}/tmp/installer.exe -ArgumentList ("-q") -Wait -PassThru
            $ExitCode = $Process.ExitCode
            if ($ExitCode -eq 0 -or $ExitCode -eq 3010) {
              Write-Host "Installation successful"
            } else {
              Write-Host "non-zero exit code returned by the installation process: $ExitCode"
              exit $ExitCode
            }
          } catch {
            Write-Host "Failed to install: $($_.Exception.Message)"
            exit 1
          }
          Update-EnvironmentVariables

          # Reset Path and environment
          echo "$env:Path" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8
          Get-ChildItem Env: | % { echo "$($_.Name)=$($_.Value)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append }

      - name: Setup Swift environment
        id: android-swift-env
        run: |
          echo "sysroot=$(resolve-path ${{ steps.setup-ndk.outputs.ndk-path }}\toolchains\llvm\prebuilt\windows-x86_64\sysroot)" >> $env:GITHUB_OUTPUT
          echo "sdkroot=$(resolve-path $env:SDKROOT\..\..\..\..\Android.platform\Developer\SDKs\Android.sdk)" >> $env:GITHUB_OUTPUT
          echo "clang-resource-dir=$(& $(resolve-path "${{ steps.setup-ndk.outputs.ndk-path }}\toolchains\llvm\prebuilt\windows-x86_64\bin\clang.exe") -print-resource-dir)" >> $env:GITHUB_OUTPUT

      - name: Build cassowary project
        run: |
          swift build `
            --build-system native `
            --package-path ${{ github.workspace }}/SourceCache/cassowary `
            --triple ${{ matrix.arch }}-unknown-linux-android${{ inputs.ANDROID_API_LEVEL }} `
            --sdk "${{ steps.android-swift-env.outputs.sysroot }}" `
            -Xswiftc -sdk -Xswiftc "${{ steps.android-swift-env.outputs.sdkroot }}" `
            -Xswiftc -sysroot -Xswiftc "${{ steps.android-swift-env.outputs.sysroot }}" `
            -Xswiftc -I -Xswiftc "${{ steps.android-swift-env.outputs.sdkroot }}\usr\include" `
            -Xswiftc -Xclang-linker -Xswiftc -resource-dir -Xswiftc -Xclang-linker -Xswiftc ${{ steps.android-swift-env.outputs.clang-resource-dir }}

      - uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      - uses: android-actions/setup-android@00854ea68c109d98c75d956347303bf7c45b0277 # v3.2.1
        with:
          packages: 'platforms;android-28'

      - name: Fetch firebase-cpp-sdk release
        uses: dsaltares/fetch-gh-release-asset@a40c8b4a0471f9ab81bdf73a010f74cc51476ad4 # v1.1.1
        with:
          repo: thebrowsercompany/firebase-cpp-sdk
          version: tags/20240909.0
          file: firebase-android-${{ env.ANDROID_ABI }}.zip
          target: ${{ github.workspace }}/SourceCache/swift-firebase/firebase-android-${{ env.ANDROID_ABI }}.zip

      - name: Expand firebase-cpp-sdk release
        working-directory: ${{ github.workspace }}/SourceCache/swift-firebase
        run: |
          Expand-Archive -Path firebase-android-${{ env.ANDROID_ABI }}.zip -DestinationPath third_party
          Rename-Item -Path third_party/firebase-android-${{ env.ANDROID_ABI }} -NewName firebase-development

      # TODO: Remove the workaround step below, due to `std::__voidify()` not being accessible
      #       with the current combination of Swift toolchain and Andoird NDK versions.
      - name: Patch out private symbol std::__voidify() in construct_at.h
        env:
          ANDROID_NDK_ROOT: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: |
          $construct_at_h_path="${env:ANDROID_NDK_ROOT}\toolchains\llvm\prebuilt\windows-x86_64\sysroot\usr\include\c++\v1\__memory\construct_at.h"
          (Get-Content $construct_at_h_path) -replace "std::__voidify","(void *) &" | Set-Content $construct_at_h_path

      - name: Configure firebase-swift
        env:
          ANDROID_NDK_ROOT: ${{ steps.setup-ndk.outputs.ndk-path }}
          # TODO: Remove the following workaround from `SWIFT_OPTIONS`, due to missing `libclang_rt.builtins.a`
          #       and `libunwind.a` libraries in the Swift toolchain:
          #       -Xclang-linker -resource-dir -Xclang-linker ${env:ANDROID_NDK_ROOT}\toolchains\llvm\prebuilt\windows-x86_64\lib\clang\${{ inputs.ANDROID_CLANG_VERSION }}
          SWIFT_OPTIONS: -sdk ${env:SDKROOT}..\..\..\..\Android.platform\Developer\SDKs\Android.sdk -sysroot ${env:ANDROID_NDK_ROOT}\toolchains\llvm\prebuilt\windows-x86_64\sysroot -Xclang-linker -resource-dir -Xclang-linker ${env:ANDROID_NDK_ROOT}\toolchains\llvm\prebuilt\windows-x86_64\lib\clang\${{ inputs.ANDROID_CLANG_VERSION }} -I${env:SDKROOT}\..\..\..\..\..\..\Toolchains\0.0.0+Asserts\usr\include -I${env:SDKROOT}\usr\include -Xlinker -zdefs
        working-directory: ${{ github.workspace }}/SourceCache/swift-firebase
        run: |
          cmake -B out `
                -D CMAKE_BUILD_TYPE=RelWithDebInfo `
                -G Ninja `
                -S . `
                -D CMAKE_SYSTEM_NAME=Android `
                -D CMAKE_ANDROID_ARCH_ABI=${{ env.ANDROID_ABI }} `
                -D CMAKE_ANDROID_API=28 `
                -D CMAKE_Swift_COMPILER_TARGET=${{ matrix.arch }}-unknown-linux-android28 `
                -D CMAKE_Swift_COMPILER_WORKS=YES `
                -D CMAKE_Swift_FLAGS="${{ env.SWIFT_OPTIONS }}" `
                -D SWIFT_FIREBASE_BUILD_EXAMPLES=NO

      - name: Build firebase-swift
        working-directory: ${{ github.workspace }}/SourceCache/swift-firebase
        run: cmake --build out

  swift_test:
    needs: [stage1-compiler, stage2-compiler]
    runs-on: ${{ inputs.compilers_build_runner }}

    strategy:
      fail-fast: false
      # TODO: maybe refactor without using a matrix
      matrix: ${{ fromJSON(inputs.build_matrix) }}

    name: ${{ matrix.os }} ${{ matrix.arch }} Asserts Swift Test

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          path: ${{ github.workspace }}/SourceCache/ci-build
          show-progress: false
      - uses: ./SourceCache/ci-build/.github/actions/setup-build
        with:
          setup-vs-dev-env: true
          host-arch: ${{ matrix.arch }}
          swift-version: ${{ env.PINNED_BOOTSTRAP_TOOLCHAIN_VERSION }}
      - name: Install CMake ${{ env.CMAKE_VERSION }}
        uses: lukka/get-cmake@4a7d025fc60f00db0c7b44ebf783d19b52444830 # v4.4.1
        with:
          cmakeVersion: ${{ env.CMAKE_VERSION }}

      - name: Download Stage1 Compiler
        uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-stage1-compiler
          path: ${{ github.workspace }}/BinaryCache/stage1/Library

      - name: Compute workspace hash
        id: workspace_hash
        shell: pwsh
        run: |
          $stringAsStream = [System.IO.MemoryStream]::new()
          $writer = [System.IO.StreamWriter]::new($stringAsStream)
          $writer.write("${{ github.workspace }}")
          $writer.Flush()
          $stringAsStream.Position = 0
          $hash = (Get-FileHash -Algorithm SHA256 -InputStream $stringAsStream).Hash
          echo "hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Setup sccache
        uses: ./SourceCache/ci-build/.github/actions/setup-sccache
        if: inputs.use_host_toolchain
        with:
          s3-bucket: ${{ vars.SCCACHE_S3_BUCKET }}
          aws-region: ${{ vars.SCCACHE_AWS_REGION }}
          aws-arn: ${{ vars.SCCACHE_AWS_ARN }}
          disk-max-size: 500M
          disk-cache-key: ${{ steps.workspace_hash.outputs.hash }}-${{ matrix.os }}-${{ matrix.arch }}-Asserts-compilers
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-build-tools
          path: ${{ github.workspace }}/BinaryCache/0/bin
      - uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-libxml2-${{ inputs.libxml2_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/libxml2-${{ inputs.libxml2_version }}/usr
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}-cmark-gfm-${{ inputs.swift_cmark_version }}
          path: ${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr
      - uses: thebrowsercompany/gha-download-tar-artifact@59992d91335d4ecba543c8535f7d07238e42125d # main
        with:
          name: ${{ inputs.build_os }}-${{ inputs.build_arch }}-early-swift-driver
          path: ${{ github.workspace }}/BinaryCache/swift-driver

      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/llvm-project
          ref: ${{ inputs.llvm_project_revision }}
          path: ${{ github.workspace }}/SourceCache/llvm-project
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift
          ref: ${{ inputs.swift_revision }}
          path: ${{ github.workspace }}/SourceCache/swift
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-experimental-string-processing
          ref: ${{ inputs.swift_experimental_string_processing_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-experimental-string-processing
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: swiftlang/swift-syntax
          ref: ${{ inputs.swift_syntax_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-syntax
          show-progress: false
      - uses: actions/checkout@v4.2.2
        with:
          repository: apple/swift-corelibs-libdispatch
          ref: ${{ inputs.swift_corelibs_libdispatch_revision }}
          path: ${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch
          show-progress: false

      - uses: actions/setup-python@v5
        id: python
        with:
          python-version: ${{ inputs.python_version }}
          architecture: x64 # FIXME(#1004): Remove workaround installing x64 Python on ARM64 CI hosts

      - name: Download and expand Python
        env:
          INPUT_PYTHON_DOWNLOAD_LOCATIONS: ${{ inputs.python_download_locations }}
          INPUT_PYTHON_VERSION: ${{ inputs.python_version }}
        run: |
          function DownloadAndVerify($URL, $Destination, $Hash) {
            New-Item -ItemType Directory (Split-Path -Path $Destination -Parent) -ErrorAction Ignore | Out-Null
            $WebClient = New-Object Net.WebClient
            $WebClient.DownloadFile($URL, $Destination)
            $ExpectedHash = $Hash.ToUpperInvariant()
            $SHA256 = (Get-FileHash -Path $Destination -Algorithm SHA256).Hash.ToUpperInvariant()
            if ($SHA256 -ne $ExpectedHash) {
              throw "SHA256 mismatch ($SHA256 vs $Hash) for $URL"
            }
          }

          $KnownPythons = $env:INPUT_PYTHON_DOWNLOAD_LOCATIONS | ConvertFrom-Json
          $version = $env:INPUT_PYTHON_VERSION
          $archKey = "${{ matrix.arch }}".ToUpper()
          $pythonInfo = $KnownPythons."$version"."$archKey"
          if ($null -eq $pythonInfo) {
            throw "No python download info for version $version and architecture $archKey"
          }
          Write-Host "Downloading Python $version for $archKey"

          # Download the Python package
          $packagePath = "${{ github.workspace }}\BinaryCache\Python-$archKey-$version.zip"
          DownloadAndVerify -URL $pythonInfo.URL -Destination $packagePath -Hash $pythonInfo.SHA256
          Write-Host "Downloaded Python $version for $archKey to $packagePath"

          # Extract the Python package
          $extractRoot = "${{ github.workspace }}\BuildRoot\Library\Developer\Python-$archKey-$version"
          New-Item -ItemType Directory -Path $extractRoot -ErrorAction Ignore | Out-Null
          Expand-Archive -Path $packagePath -DestinationPath $extractRoot -Force
          Write-Host "Extracted Python $version for $archKey to $extractRoot"

          # Export Python Location
          echo "PYTHON_LOCATION=$extractRoot" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Setup context
        id: setup-context
        env:
          INPUT_PYTHON_VERSION: ${{ inputs.python_version }}
        run: |
          $PythonVersion = $env:INPUT_PYTHON_VERSION
          $PythonMajor = ([System.Version]$PythonVersion).Major
          $PythonMinor = ([System.Version]$PythonVersion).Minor
          if ( "${{ matrix.os }}" -eq "Windows" ) {
            $CLANG_LOCATION = cygpath -m $(Split-Path (Get-Command swiftc).Source)
            $SDKROOT = cygpath -m ${env:SDKROOT}
            $LIBPYTHON_PATH = "${env:PYTHON_LOCATION}/tools/libs/python${PythonMajor}${PythonMinor}.lib"
            $PYTHON_INCLUDE_DIR = "${env:PYTHON_LOCATION}/tools/include"
            $PYTHON_BINARY="python.exe"
          }

          # Compute the number of parallel jobs to use for the build.
          $RamBytes = if ($IsWindows) {
            (Get-CimInstance -ClassName Win32_ComputerSystem).TotalPhysicalMemory
          } else {
              throw "Unsupported OS"
          }
          $RamGB = [math]::Round($RamBytes / 1GB)

          # Consider 24 GB per link job, to a minimum of 2 jobs.
          $LinkJobs = [math]::Max(2, [math]::Floor($RamGB / 24))

          # Output the context for the configure task.
          $Context = @"
          clang-location=${CLANG_LOCATION}
          libpython-path=${LIBPYTHON_PATH}
          python-include-dir=${PYTHON_INCLUDE_DIR}
          python-binary=${PYTHON_BINARY}
          python-executable=${PYTHON_EXECUTABLE}
          sdkroot=${SDKROOT}
          link-jobs=${LinkJobs}
          "@
          Write-Output $Context | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      # build.ps1: Test-Compilers, with Get-CompilersDefines with -Test TestSwift
      - name: Configure Compilers for testing
        uses: ./SourceCache/ci-build/.github/actions/configure-cmake-project
        with:
          project-name: Compilers
          swift-version: ${{ inputs.swift_version }}
          enable-caching: ${{ inputs.use_host_toolchain }}
          debug-info: ${{ inputs.debug_info }}
          build-os: ${{ inputs.build_os }}
          build-arch: ${{ inputs.build_arch }}
          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}
          src-dir: ${{ github.workspace }}/SourceCache/llvm-project/llvm
          bin-dir: ${{ github.workspace }}/BinaryCache/1
          install-dir: ${{ github.workspace }}/BuildRoot/Library/Developer/Toolchains/${{ inputs.swift_version }}+Asserts/usr
          c-compiler: 'Stage1.C'
          cxx-compiler: 'Stage1.CXX'
          swift-compiler: 'Stage1.Swift'
          cache-script: ${{ github.workspace }}/SourceCache/swift/cmake/caches/${{ matrix.os }}-${{ matrix.cpu }}.cmake
          cmake-defines: |
            @{
              'SWIFT_BUILD_DYNAMIC_SDK_OVERLAY' = "YES";
              'SWIFT_BUILD_DYNAMIC_STDLIB' = "YES";
              'SWIFT_BUILD_REMOTE_MIRROR' = "YES";
              'SWIFT_NATIVE_SWIFT_TOOLS_PATH' = "";
              'CLANG_TABLEGEN' = "${{ github.workspace }}/BinaryCache/0/bin/clang-tblgen${ExeSuffix}";
              'CLANG_TIDY_CONFUSABLE_CHARS_GEN' = "${{ github.workspace }}/BinaryCache/0/bin/clang-tidy-confusable-chars-gen${ExeSuffix}";
              'CMAKE_STATIC_LIBRARY_PREFIX_Swift' = "lib";
              'LibXml2_DIR' = "${{ github.workspace }}/BinaryCache/Library/libxml2-${{ inputs.libxml2_version }}/usr/lib/cmake/libxml2-${{ inputs.libxml2_version }}";
              'LLDB_LIBXML2_VERSION' = "${{ inputs.libxml2_version }}";
              'LLDB_PYTHON_EXE_RELATIVE_PATH' = "${{ steps.setup-context.outputs.python-binary }}";
              'LLDB_PYTHON_EXT_SUFFIX' = ".pyd";
              'LLDB_PYTHON_RELATIVE_PATH' = "lib/site-packages";
              'LLDB_PYTHON_DLL_RELATIVE_PATH' = "../../../../Python-$PythonVersion/usr/bin";
              'LLDB_TABLEGEN' = "${{ github.workspace }}/BinaryCache/0/bin/lldb-tblgen${ExeSuffix}";
              'LLVM_CONFIG_PATH' = "${{ github.workspace }}/BinaryCache/0/bin/llvm-config${ExeSuffix}";
              'LLVM_ENABLE_ASSERTIONS' = "YES";
              'LLVM_ENABLE_LLD' = "${{ inputs.use_host_toolchain && 'NO' || 'YES' }}";
              'LLVM_EXTERNAL_SWIFT_SOURCE_DIR' = "${{ github.workspace }}/SourceCache/swift";
              'LLVM_HOST_TRIPLE' = "${{ matrix.triple }}";
              'LLVM_NATIVE_TOOL_DIR' = "${{ github.workspace }}/BinaryCache/0/bin";
              'LLVM_TABLEGEN' = "${{ github.workspace }}/BinaryCache/0/bin/llvm-tblgen${ExeSuffix}";
              'LLVM_USE_HOST_TOOLS' = "NO";
              'Python3_EXECUTABLE' = "${{ steps.python.outputs.python-path }}";
              'Python3_INCLUDE_DIR' = "${{ steps.setup-context.outputs.python-include-dir }}";
              'Python3_LIBRARY' = "${{ steps.setup-context.outputs.libpython-path }}";
              'Python3_ROOT_DIR' = $env:pythonLocation;
              'SWIFT_TOOLCHAIN_VERSION' = "${{ inputs.swift_version}} ${{ inputs.swift_tag }}";
              'SWIFT_BUILD_SWIFT_SYNTAX' = "YES";
              'SWIFT_CLANG_LOCATION' = "${{ steps.setup-context.outputs.clang-location }}";
              'SWIFT_EARLY_SWIFT_DRIVER_BUILD' = "${{ github.workspace }}/BinaryCache/swift-driver/bin";
              'SWIFT_ENABLE_EXPERIMENTAL_CONCURRENCY' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_CXX_INTEROP' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_DIFFERENTIABLE_PROGRAMMING' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_DISTRIBUTED' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_OBSERVATION' = "YES";
              'SWIFT_ENABLE_EXPERIMENTAL_STRING_PROCESSING' = "YES";
              'SWIFT_ENABLE_SYNCHRONIZATION' = "YES";
              'SWIFT_ENABLE_VOLATILE' = "YES";
              'SWIFT_PATH_TO_LIBDISPATCH_SOURCE' = "${{ github.workspace }}/SourceCache/swift-corelibs-libdispatch";
              'SWIFT_PATH_TO_STRING_PROCESSING_SOURCE' = "${{ github.workspace }}/SourceCache/swift-experimental-string-processing";
              'SWIFT_PATH_TO_SWIFT_SDK' = "${{ steps.setup-context.outputs.sdkroot }}";
              'SWIFT_PATH_TO_SWIFT_SYNTAX_SOURCE' = "${{ github.workspace }}/SourceCache/swift-syntax";
              'SWIFT_STDLIB_ASSERTIONS' = "NO";
              'SWIFTSYNTAX_ENABLE_ASSERTIONS' = "NO";
              'cmark-gfm_DIR' = "${{ github.workspace }}/BinaryCache/Library/cmark-gfm-${{ inputs.swift_cmark_version }}/usr/lib/cmake";

              # GHA-specific options.
              'CLANG_VENDOR' = 'compnerd.org';
              'CLANG_VENDOR_UTI' = 'org.compnerd.dt';
              'LLVM_APPEND_VC_REV' = "NO";
              'LLVM_VERSION_SUFFIX' = "";
              'LLVM_PARALLEL_LINK_JOBS' = "${{ steps.setup-context.outputs.link-jobs }}";
              'SWIFT_PARALLEL_LINK_JOBS' = "${{ steps.setup-context.outputs.link-jobs }}";
              'CMAKE_C_FLAGS' = "${{ !inputs.use_host_toolchain && '-fuse-ld=lld' || '' }}";
              'CMAKE_CXX_FLAGS' = "${{ !inputs.use_host_toolchain && '-fuse-ld=lld' || '' }}";
              'CMAKE_Swift_FLAGS' = "${{ !inputs.use_host_toolchain && '-use-ld=lld' || '' }}";
            }

      - name: Test Compiler
        run: |
          $env:Path = "${{ github.workspace }}\BinaryCache\Library\cmark-gfm-${{ inputs.swift_cmark_version }}\usr\bin;${{ github.workspace }}\BinaryCache\1\tools\swift\libdispatch-windows-${{ matrix.cpu }}-prefix\bin;${{ github.workspace }}\BinaryCache\1\bin;$env:Path;$env:VSInstallRoot\DIA SDK\bin\${{ matrix.arch }}"
          cmake --build ${{ github.workspace }}/BinaryCache/1 --target check-swift
