name: toolchain
schedules:
  - cron: "0 * * * *"
    branches:
      include:
        - master
    always: true
    displayName: "Hourly Build"
jobs:
  - template: templates/toolchain.yml
    parameters:
      host: x64
      platform: linux
      triple: x86_64-unknown-linux-gnu
