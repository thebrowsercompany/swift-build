name: icu
jobs:
  - template: templates/icu.yml
    parameters:
      host: 'arm64'
      platform: 'windows'
      alt_platform: 'win'

  - template: templates/icu.yml
    parameters:
      host: 'x64'
      platform: 'windows'
      alt_platform: 'win'

  - template: templates/icu.yml
    parameters:
      alt_arch: 'armv7'
      host: 'armeabi'
      platform: 'android'
      alt_platform: 'android'

  - template: templates/icu.yml
    parameters:
      alt_arch: 'aarch64'
      host: arm64
      platform: 'android'
      alt_platform: 'android'

  - template: templates/icu.yml
    parameters:
      alt_arch: 'x86_64'
      host: 'x64'
      platform: 'android'
      alt_platform: 'android'
