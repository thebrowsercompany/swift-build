name: icu
jobs:
  - template: templates/icu.yml
    parameters:
      host: 'arm64'
      platform: 'win'

  - template: templates/icu.yml
    parameters:
      host: 'x64'
      platform: 'win'

  - template: templates/icu.yml
    parameters:
      alt_arch: 'armv7'
      host: 'armeabi'
      platform: 'android'

  - template: templates/icu.yml
    parameters:
      alt_arch: 'aarch64'
      host: arm64
      platform: 'android'

  - template: templates/icu.yml
    parameters:
      alt_arch: 'x86_64'
      host: 'x64'
      platform: 'android'
