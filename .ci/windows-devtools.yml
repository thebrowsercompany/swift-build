name: 'Windows DevTools'
schedules:
  - cron: "0 8 * * *"
    branches:
      include:
        - master
    always: true
    displayName: "Nightly (8:00 AM) Build"
jobs:
  - template: templates/windows-devtools.yml
    parameters:
      arch: 'x86_64'
      build: 'x64'
      triple: 'x86_64-unknown-windows-msvc'

  - template: templates/windows-devtools.yml
    parameters:
      arch: 'aarch64'
      build: 'arm64'
      triple: 'aarch64-unknown-windows-msvc'
