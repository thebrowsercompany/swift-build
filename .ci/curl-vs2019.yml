name: curl
jobs:
  - template: templates/curl.yml
    parameters:
      host: 'arm64'
      platform: 'win'

  - template: templates/curl.yml
    parameters:
      host: 'x64'
      platform: 'win'

  - template: templates/curl.yml
    parameters:
      arch: 'armv7'
      host: 'arm'
      platform: 'android'

  - template: templates/curl.yml
    parameters:
      arch: 'aarch64'
      host: arm64
      platform: 'android'

  - template: templates/curl.yml
    parameters:
      arch: 'x86_64'
      host: 'x64'
      platform: 'android'
